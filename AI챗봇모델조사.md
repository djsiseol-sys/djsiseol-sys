# AI 챗봇 — 모델·보안 자료 조사

작성 2026-09-21 · **조사 문서입니다. 아직 아무것도 만들지 않았습니다.**

[`AI도입검토.md`](AI도입검토.md)(2026-08-25)의 후속입니다.
그 문서가 **"AI 를 넣을 값어치가 있나"** 를 따졌다면, 이 문서는
**"어느 모델을, 어떤 보안 조건으로 붙이나"** 를 따집니다.

앞 문서의 결론 두 개는 그대로 유효하고, 이 문서도 그 위에 얹습니다.

> **AI 에게 금액을 계산시키지 않습니다.** 계산은 `calcFee()` 가 합니다.
> **브라우저에서 AI API 를 직접 부르지 않습니다.** 키가 페이지 소스에 그대로 남습니다.

---

## 0. 결론부터

| | |
|---|---|
| **어느 모델** | **Claude Sonnet 5** 를 1순위로 봅니다. 조문 인용 품질 · 학습 안 함 · ISO 42001 인증 |
| **결재가 막히면** | **Azure OpenAI (Korea Central)** — **국내 저장 + CSAP 인증**. 국외이전 쟁점이 아예 없어집니다 |
| **어떻게 붙이나** | Supabase Edge Function 프록시 (앞 문서 B안 그대로). **모델을 갈아끼울 수 있게** 만듭니다 |
| **지식은 어떻게 먹이나** | 조례 전문을 **프롬프트에 통째로** 넣습니다. **벡터DB·RAG 는 필요 없습니다** |
| **돈** | 월 1만~2만 원 (Sonnet 5 기준). 앞 문서의 6,000원은 낙관적이었습니다 — 6장 |
| **가장 큰 걸림돌** | ① 조례 전문이 저장소에 없습니다 ② **비로그인 금액 비공개 결정과 정면으로 부딪칩니다** — 7장 |
| **먼저 할 일** | 앞 문서 0단계(2주 손으로 써 보기)는 **여전히 건너뛰지 않습니다** |

가장 중요한 한 줄은 모델 이야기가 아닙니다.

> **챗봇이 "토요일 야간이면 20만 2,500원입니다" 라고 답하는 순간,
> 2026-08-27 에 정한 「로그인 안 한 사람에게 금액을 안 보여준다」 가 무너집니다.**

---

## 1. 앞 문서에서 바로잡는 것

조사하면서 앞 문서(2026-08-25)와 어긋나거나 낡은 것이 네 개 나왔습니다.

| 앞 문서 | 지금 확인된 것 |
|---|---|
| 2단계 조례 질의응답을 **"조회자에게도 열어 줍니다"** | **이틀 뒤(2026-08-27) 담당자가 반대로 결정했습니다.** 비로그인에게는 금액을 아예 그리지 않습니다. 7장에서 다시 봅니다 |
| 캐싱하면 월 6,000원 | **모든 요청이 캐시에 맞는다고 가정한 값**입니다. 캐시는 기본 5분짜리라 하루에 몇 건 쓰는 사용 방식에서는 거의 안 맞습니다. 현실값은 6장 |
| (언급 없음) | **AWS Bedrock 서울 리전으로 국외이전을 푸는 길은 지금 없습니다.** 서울(`ap-northeast-2`)은 Claude 를 **Global 엔드포인트로만** 서빙합니다 — 4장 |
| (언급 없음) | **조례 전문이 이 저장소에 없습니다.** `index.html` 에 있는 것은 `별표 2`·`제13조` 같은 **근거 표시 문자열**뿐이고 조문 본문이 아닙니다 — 7장 |

나머지(프록시 구조, 금액 계산 금지, 개인정보 안 보내기, 망분리)는 조사해도 결론이 그대로였습니다.

---

## 2. 후보 모델

"메이저이면서 보안 문제가 적은" 것으로 넷을 놓고 봤습니다.
가격은 100만 토큰당 미국 달러, 2026-09-21 확인값입니다.

| | Claude (Anthropic) | GPT (OpenAI 직접) | GPT (Azure OpenAI) | Gemini (Google) | HyperCLOVA X (네이버) |
|---|---|---|---|---|---|
| 대표 모델 | Sonnet 5 · Haiku 4.5 | GPT 계열 | 같은 모델을 Azure 가 서빙 | Gemini 3.x | HCX 계열 |
| 입력/출력 | **$2 / $10** (Sonnet 5)<br>$1 / $5 (Haiku 4.5) | 공개 요금표 확인 필요 | Azure 요금표 | $2 / $12 내외 | CLOVA Studio 요금표 |
| API 입력을 학습에 쓰나 | **안 씁니다** | **안 씁니다**(API·Enterprise) | **안 씁니다** | **유료 티어는 안 씁니다**<br>(무료 AI Studio 는 **씁니다**) | 상품별 확인 필요 |
| 기본 보존 | **대화 내용 기본 미보존**<br>(Fable·Mythos 계열만 30일 강제) | 남용 감시 목적 **최대 30일** | Azure 정책(미보존 설정 가능) | 안전·남용 목적 제한적 로깅 | 확인 필요 |
| 주요 인증 | SOC 2 Type I·II · ISO 27001 · **ISO 42001** | SOC 2 등 | **CSAP (Low, 그룹 C)** + Azure 인증군 | ISO 계열 | 국내 인증 |
| **국내(한국) 저장** | **없습니다** | **있습니다**(저장 기준) | **있습니다** (Korea Central) | 확인 필요 | **국내** |
| 국내 추론(연산) | 없습니다 | 준비 중이라 밝힘 | Korea Central | 확인 필요 | **국내** |
| 조문 인용 품질 | **가장 믿을 만합니다** | 좋습니다 | 같음 | 좋습니다 | 한국어 강점, 조문 인용은 실측 필요 |
| 지자체 결재 난이도 | 중간 | 중간 | **가장 낮습니다** | 중간 | 낮습니다 |

**Claude Sonnet 5 를 1순위로 둡니다.**

- 긴 조례에서 **조문 번호를 정확히 인용**하는 일이 이 챗봇의 존재 이유인데, 여기가 가장 강합니다
- **대화 내용을 기본적으로 보관하지 않습니다.** "30일 보관 후 삭제" 가 아니라 애초에 안 남깁니다
  (`Fable`·`Mythos` 계열만 30일 보관이 강제됩니다 — **그 모델을 쓰지 않으면 됩니다**)
- **ISO/IEC 42001**(AI 경영시스템) 을 받았습니다. 결재 문서에 적을 것이 하나 더 생깁니다
- $2/$10 은 원래 2026-08-31 까지의 도입 요금이었는데 **그대로 정식 요금이 됐습니다.** 오를 걱정이 줄었습니다

**다만 Claude 에는 한국 리전이 없습니다.** 4장에서 따로 봅니다.

**Haiku 4.5 도 후보에 남깁니다.** 값이 절반이고, "쉬운 말로 설명" 같이
이미 나온 표를 풀어 쓰는 일에는 충분합니다. 조례 질의응답만 Sonnet 5 로 가르면 됩니다.

---

## 3. "보안 문제가 적다" 를 다섯 가지로 쪼갭니다

한 덩어리로 물으면 답이 안 나옵니다. 실제로 따져야 하는 것은 다섯 개고,
**그중 두 개는 모델을 고르는 문제가 아니라 우리가 어떻게 만드느냐의 문제**입니다.

| | 무엇을 묻나 | 누가 정하나 |
|---|---|---|
| ① **학습** | 내가 보낸 것으로 모델을 다시 학습시키나 | 업체 |
| ② **보존** | 얼마나, 왜 보관하나 | 업체 |
| ③ **인증** | 누가 검증했나 (SOC 2 · ISO 27001 · ISO 42001 · CSAP) | 업체 |
| ④ **위치** | 데이터가 어느 나라에 있나 | 업체 (지자체는 **여기가 제일 큽니다**) |
| ⑤ **키·남용** | API 키가 새지 않나 · 아무나 우리 요금을 태우지 않나 | **우리** |

### ①②③ — 메이저 업체는 대체로 비슷합니다

API 로 보낸 것을 학습에 쓰는 메이저 업체는 지금 없습니다. 차이는 **보존**에서 납니다.

- **Claude**: 대화 내용 기본 미보존. 단 **안전 시스템에 걸린 건은 최대 2년** 보관될 수 있습니다
- **OpenAI**: 남용 감시 목적으로 **기본 최대 30일**. 조건이 맞으면 무보존(ZDR) 계약 가능
- **Gemini**: **유료 티어만** 학습 제외입니다. **무료 AI Studio 키로 시험하면 그 내용이 학습에 쓰입니다** —
  시험 단계에서 실수하기 가장 쉬운 자리입니다

> **여기서 실무적으로 제일 중요한 한 줄:** 무료 티어로 "잠깐 테스트" 하지 않습니다.
> 조례와 대장 내용을 무료 티어에 붙여넣는 순간 되돌릴 수 없습니다.

### ⑤ — 이건 업체가 막아 주지 않습니다

앞 문서가 이미 정한 대로입니다. 브라우저에 키를 두지 않고 Edge Function 뒤에 둡니다.
여기에 조사로 확인한 것을 더합니다.

- Supabase Edge Function 은 **`verify_jwt` 가 기본 켜져 있습니다.** 로그인한 사용자만 부르게 할 수 있습니다
- 무료 플랜 기준 **한 요청 최대 150초**, **CPU 시간 2초** 입니다.
  CPU 시간에는 **바깥 API 를 기다리는 시간이 안 들어갑니다** — 중계기로 쓰는 데는 문제가 없습니다.
  다만 답이 아주 길어지면 150초에 걸립니다. **스트리밍으로 흘려보내면 됩니다**
- 호출 상한을 **우리가 걸어야 합니다.** 업체가 알아서 막아 주지 않습니다 (7장 ③)

---

## 4. 국외이전 — 지자체에서는 이게 가장 큰 벽입니다

앞 문서 4-2 가 정리한 그대로입니다. 미국 서버로 보내면
**개인정보 보호법 제26조(처리위탁)와 제28조의8(국외이전)이 동시에** 걸립니다.
길은 세 갈래고, **셋 다 쓸 수 있습니다.**

### A안 — 개인정보를 애초에 안 보낸다 ✅ 여전히 1순위

이 대장은 연락처를 의도적으로 뺐습니다. 단체명·이용 목적·인원·메모뿐입니다.
**개인정보를 보내지 않으면 제28조의8 이 발동하지 않습니다.**

챗봇에서는 더 쉽습니다 — **조례 질의응답은 예약 자료를 아예 안 보냅니다.**
보내는 것이 조례 전문과 요금 기준값뿐이라, **개인정보가 들어갈 자리가 없습니다.**

> 가장 값싸고 가장 깔끔한 길입니다. **1단계는 이것만으로 충분합니다.**

### B안 — 국내에 저장되는 서비스를 쓴다

A안으로 안 되는 기능(자연어 등록 등)까지 가게 되면 필요합니다.

| | 무엇이 국내인가 |
|---|---|
| **Azure OpenAI (Korea Central)** | **저장·처리 모두 국내.** 게다가 **CSAP(Low, 그룹 C)** 를 받았고 여기에 Azure OpenAI 가 포함됩니다. **공공기관 결재에서 가장 통과하기 쉬운 형태입니다** |
| **OpenAI 데이터 레지던시 (한국)** | **저장(at rest)이 국내.** API 플랫폼·Enterprise 대상. **추론(연산)은 아직** — 국내 추론을 준비 중이라고 밝힌 단계입니다 |
| **HyperCLOVA X (네이버)** | 저장·추론 모두 국내. **국외이전이 애초에 없습니다** |

**"저장이 국내" 와 "추론도 국내" 는 다릅니다.** 결재 문서에 이 둘을 섞어 쓰면 나중에 문제가 됩니다.

### C안 — Claude 를 쓰면서 국내 리전? — **지금은 없습니다**

AWS Bedrock 을 거치면 서울 리전으로 갈 수 있지 않냐는 길을 확인했는데, **막혀 있습니다.**

| | |
|---|---|
| 서울 `ap-northeast-2` | Claude 는 **Global 엔드포인트로만** 제공됩니다. 특정 리전으로 묶는 In-region 이 없습니다 |
| Global 엔드포인트 | 가용한 리전으로 **동적으로 분산**됩니다 — 어디서 처리될지 우리가 정하지 못합니다 |
| 리전 고정이 되는 곳 | 도쿄(JP) · 미국 · 유럽 · 호주 등. **한국은 없습니다** |
| Anthropic 직접 API | `inference_geo: "us"` 로 **미국 고정**은 됩니다 (요금 1.1배). 한국 고정은 없습니다 |

> **결론: Claude 로 가면 A안(개인정보를 안 보낸다)으로 푸는 수밖에 없습니다.**
> 그런데 조례 질의응답은 애초에 개인정보를 안 보내므로, **1단계에서는 아무 문제가 없습니다.**

### 망분리는 상황이 풀리는 중입니다

앞 문서가 본 N2SF 전환이 실제로 진행됐습니다.

- KISA 가 **2026년 4월 「국가 망 보안체계(N2SF) 실증 사례집」** 을 냈고,
  **「업무 환경에서 생성형 AI 활용」** 이 여섯 모델 중 하나로 들어가 있습니다
- **2026년 도입 지원사업**으로 6개 연합체가 실제 업무환경에 적용 중입니다

**우리 기관에 물어볼 것이 더 구체적으로 바뀌었습니다** — "언젠가 되나요" 가 아니라
**"KISA 실증 사례집의 생성형 AI 모델을 우리도 적용할 계획이 있나요"** 로 묻습니다.

---

## 5. "내 웹사이트 정보 기반" — 무엇을 어떻게 먹이나

챗봇이 알아야 할 것이 네 가지인데, **성격이 전부 다릅니다.**

| 지식원 | 크기 | 어디 있나 | 어떻게 |
|---|---|---|---|
| **조례 전문** (제6358호 + 별표) | 약 1만 5천 토큰 | **저장소에 없습니다** ← 먼저 확보 | 프롬프트에 통째로 · 캐싱 |
| **지금 요금 기준값** (시설 단가·감면·부속시설·시간대) | 1~2천 토큰 | `state.settings` · `facilities` · `discounts` | 매 요청에 JSON 으로 |
| **사용 안내** (등록 방법·정산서·요금 담당 계정) | 3~4천 토큰 | `README.md` · `배포안내.md` · `행사정산서안내.md` 등 | 프롬프트에 · 캐싱 |
| **예약 자료** | 큼 · 변동 | `state.reservations` | **기본적으로 안 보냅니다** (6장·7장) |

### 벡터DB·RAG 는 쓰지 않습니다

조사하면 으레 "RAG 를 붙여야 한다" 는 말이 나오는데, **이 규모에서는 손해입니다.**

- 다 합쳐 **2만 토큰 남짓**입니다. Sonnet 5 의 문맥 창은 그보다 훨씬 큽니다
- **통째로 넣으면 조문을 놓치지 않습니다.** RAG 는 검색이 조문 하나를 못 집어오면 그대로 틀립니다.
  근거 조문을 정확히 인용하는 것이 이 기능의 전부인데, **거기에 실패 지점을 하나 더 만드는 셈입니다**
- Supabase 에 `pgvector` 가 있어 기술적으로는 됩니다. **그래도 지금은 쓰지 않습니다.**
  조례가 열 배로 늘어나면 그때 다시 봅니다

### 캐시는 이렇게 끊습니다

프롬프트 앞에서 뒤로 **안 바뀌는 것 → 바뀌는 것** 차례로 놓고, **안 바뀌는 것 끝에서 끊습니다.**

```
[1] 조례 전문 + 사용 안내        ← 여기까지 캐시 (몇 달에 한 번 바뀝니다)
[2] 지금 요금 기준값 (JSON)      ← 설정을 고치면 바뀝니다
[3] 사용자 질문                  ← 매번 바뀝니다
```

**[1] 뒤에서 끊습니다.** 맨 뒤에서 끊으면 질문이 매번 달라서 아무것도 재사용되지 않고,
캐시 기록 요금만 더 냅니다.

### 실시간 예약 조회는 나중에, 도구(tool)로

"이번 주 충무체육관 언제 비어요?" 같은 질문은 예약 자료가 있어야 답합니다.
이건 **자료를 통째로 보내는 게 아니라, AI 가 필요할 때 조회 함수를 부르게(tool use)** 하는 것이 맞습니다.
그래야 보내는 양이 최소가 됩니다. **다만 3단계 이야기입니다 — 1단계에 넣지 않습니다.**

---

## 6. 돈 — 다시 계산합니다

앞 문서의 "캐싱하면 월 6,000원" 은 **모든 요청이 캐시에 맞는다고 본 값**이라 낙관적이었습니다.

**캐시는 기본 5분짜리입니다.** 5분 안에 다음 질문이 안 오면 지워지고 다시 기록합니다.
1시간짜리로 늘릴 수 있지만 **기록 요금이 2배**입니다.
하루 스무 건을 띄엄띄엄 쓰는 우리 사용 방식에서는 **캐시가 자주 빗나갑니다.**

### 전제

- 프롬프트 1만 6천 토큰(조례+안내) · 질문 300 토큰 · 답 600 토큰
- **월 400건** (하루 20건 × 20일) — 이 부서 규모에서 넉넉하게 잡은 값
- 1,450원/달러

| | 캐시 안 맞을 때 (띄엄띄엄) | 캐시 맞을 때 (한 자리에서 몇 건 이어서) |
|---|---|---|
| **Haiku 4.5** | 약 11,000원 | 약 5,500원 |
| **Sonnet 5** | **약 22,000원** | **약 11,000원** |
| Opus 5 | 약 55,000원 | 약 27,000원 |

> **현실값은 그 사이, Sonnet 5 로 월 1만~2만 원입니다.**
> 앞 문서의 결론("비용은 걸림돌이 아니다")은 **그대로 유효합니다.**
> 걸림돌은 여전히 **망과 결제 절차**입니다.

**질문을 이어서 하면 저절로 싸집니다.** 한 사람이 5분 안에 서너 개를 물으면 뒤의 것은 캐시에 맞습니다.
일부러 뭘 더 만들 필요가 없습니다.

---

## 7. 이 저장소에만 있는 걸림돌 셋

일반론이 아니라 **이 `index.html` 이라서 생기는 문제**입니다. 셋 다 만들기 전에 정해야 합니다.

### ① 비로그인 금액 비공개와 정면으로 부딪칩니다 ★ 가장 중요

`CLAUDE.md` 가 정한 것입니다 (2026-08-27 담당자 결정).

> 로그인하지 않은 사람(`viewer`)에게는 요금을 아예 그리지 않는다.
> **감추기만 하면 개발자도구로 들여다볼 수 있기 때문이다.**

그래서 지금 `renderSummary` · `renderCalendar` · `renderList` · `renderRail` · `renderFees` ·
`buildPrintDetail` 여섯 곳이 **금액을 만들어 넣기 전에 `currentRole() !== "viewer"` 를 확인**합니다.

**챗봇은 이 여섯 곳과 성격이 다릅니다.** 화면 요소가 아니라 **자유롭게 말하는 창**이고,
조례 질의응답은 그 본질이 "얼마인지 설명하는 것" 입니다.
`"토요일 야간이면 202,500원입니다"` 라고 답하는 순간 **여섯 곳을 막은 의미가 없어집니다.**

앞 문서 2단계는 **"조회자에게도 열어 줍니다"** 라고 썼는데, 그건 이 결정 **이틀 전**에 쓴 것입니다.
지금은 셋 중 하나를 골라야 합니다.

| 안 | 내용 | 평 |
|---|---|---|
| **가** | **로그인한 사람만 챗봇을 쓴다** | **가장 단순하고 일관됩니다.** 화면 여섯 곳과 같은 규칙이 됩니다. 요금 남용도 함께 막힙니다(③). **권고** |
| 나 | 비로그인에게도 열되 **금액이 든 답을 막는다** | 자유 문장에서 금액만 걸러내는 것은 **확실하게 되지 않습니다.** 새면 조용히 샙니다 |
| 다 | 비로그인 금액 비공개 결정을 되돌린다 | **담당자가 정한 것이라 우리가 뒤집을 일이 아닙니다.** 물어볼 수는 있습니다 |

> **가안을 권고합니다.** Edge Function 의 `verify_jwt` 를 켜면 **서버에서** 막히므로,
> 화면에서 감추는 것과 달리 개발자도구로 뚫리지 않습니다.
> 앞 문서 2단계의 "조회자에게도" 는 **이 문서로 무효가 됩니다.**

### ② 조례 전문이 저장소에 없습니다

`index.html` 에 있는 것은 `별표 2 · 한밭종합운동장 충무체육관`, `제13조` 같은
**근거를 가리키는 짧은 문자열**입니다. 조문 본문이 아닙니다.

**챗봇이 조문을 인용하려면 조문 본문이 있어야 합니다.** 지금은 없습니다.

- 대전광역시 자치법규는 **자치법규정보시스템(ELIS)** 이나 국가법령정보센터에서 받습니다.
  (이 작업 환경에서는 두 곳 모두 접속이 막혀 있어 **직접 확인하지 못했습니다.**
  담당자 PC 에서 받아 주셔야 합니다)
- 받은 전문은 `조례전문.md` 같은 파일로 저장소에 둡니다. **`index.html` 안에 넣지 않습니다** —
  파일이 이미 340KB 인데 조례까지 넣으면 열 때마다 받아야 합니다
- Edge Function 이 그 파일을 읽어 프롬프트에 싣습니다

**조문을 확보하기 전에는 2단계(조례 질의응답)를 시작할 수 없습니다.** 여기가 실제 출발점입니다.

### ③ 주소만 알면 누구나 여는 화면입니다

GitHub Pages 로 서빙되고, 지금도 로그인 없이 달력을 볼 수 있습니다.
챗봇을 비로그인에 열면 **누구나 우리 요금을 태울 수 있습니다.**

①의 가안(로그인 필수)을 택하면 대부분 해결되지만, 그래도 더 겁니다.

- Edge Function 에서 **계정당 하루 호출 수 상한** (예: 50건)
- 질문 길이 상한 (예: 2,000자) — 긴 글을 붙여넣어 요금을 태우는 것을 막습니다
- Anthropic 콘솔에서 **월 지출 한도**를 겁니다. 이게 마지막 안전판입니다

한 가지 더: **예약 메모나 단체명을 프롬프트에 실어 보내면, 거기 적힌 글이 AI 에게는 지시처럼 읽힐 수 있습니다**
(프롬프트 인젝션). 1단계에서 예약 자료를 안 보내면 생기지 않는 문제이므로,
**3단계에서 예약 자료를 보내기 시작할 때 다시 봅니다.**

---

## 8. 만든다면 이 순서로

앞 문서 6장을 이 조사 결과로 고쳐 쓴 것입니다.

### 0단계 — 코드를 고치지 않고 2주 (0원) · **그대로 둡니다**

앞 문서의 0단계가 여전히 맞습니다. 정해진 질문 문구로 손으로 써 보고,
**어떤 질문을 실제로 몇 번 했는지** 적어 둡니다. 그게 1·2단계의 사양이 됩니다.

여기에 하나 더합니다 — **0단계 중에 조례 전문을 받아 둡니다**(7장 ②).
어느 길로 가든 필요하고, 받는 데 결재가 필요 없습니다.

### 1단계 — 프록시 하나, 기능 하나

- Supabase Edge Function `ask` 하나. 키는 Supabase Secrets 에. **`verify_jwt` 를 켭니다**
- 화면에 붙는 것은 **단추 하나** — 우측 상세 `산정 상세` 옆의 `쉬운 말로 설명`
- 보내는 것은 **이미 계산된 `lines[]`(금액 + 근거 조문)뿐.** 단체명·메모는 안 보냅니다
- 실패하면 단추가 사라질 뿐, 달력·계산·저장은 지금 그대로

**여기서 확인하는 것: 망이 열리나 · 결제가 되나 · 담당자가 실제로 쓰나.**
셋 중 하나라도 안 되면 2단계로 가지 않습니다.

### 2단계 — 조례 질의응답 (여기가 "챗봇")

- 조례 전문을 프롬프트에 싣고 캐싱. **조문 번호 인용 필수, 못 찾으면 "조례에 없습니다"**
- **로그인한 사람만** 씁니다 (7장 ①)
- 금액을 물으면 **AI 가 계산하지 않고 `calcFee()` 를 부르게** 합니다.
  "직접 계산해 보세요" 가 아니라 **등록창을 열어 주는 쪽**이 맞습니다

### 3단계 — 예약 자료까지 (나중)

- 실시간 조회는 tool use 로. 개인정보 필터가 여기서 **필수**가 됩니다
- 프롬프트 인젝션도 여기서부터 따집니다 (7장 ③)

### 모델은 갈아끼울 수 있게 만듭니다

조사하면서 가장 실무적인 결론이 이것입니다.

**어느 업체를 쓸지는 지금 확정하지 않아도 됩니다.** 보내는 모양(시스템 프롬프트 + 질문 → 글)이
업체마다 크게 다르지 않아서, **Edge Function 안의 한 함수만 갈면 바뀝니다.**

- `index.html` 은 **업체를 모르게** 만듭니다. `/functions/v1/ask` 만 부릅니다
- Edge Function 안에서 **업체 이름을 환경변수로** 둡니다 (`AI_PROVIDER`)
- 그러면 **결재 과정에서 업체가 바뀌어도 대장은 한 줄도 안 고칩니다**

> 이게 이 조사의 실질적인 답입니다. **"어느 모델이 제일 안전한가" 를 지금 확정하려 애쓰지 말고,
> 나중에 바꿀 수 있게 만들어 두고 1순위(Claude Sonnet 5)로 시작합니다.**

---

## 9. 결정 전에 확인할 것

앞 문서 7장에서 **답이 구체화된 것**과 **새로 생긴 것**입니다.

| 누구에게 | 무엇을 | 상태 |
|---|---|---|
| 기관 정보보안 담당 | 업무망에서 `*.supabase.co` 가 열려 있나 | **앞 문서 그대로. AI 와 무관하게 지금 확인해야 합니다** |
| 〃 | **KISA N2SF 실증 사례집의 「업무 환경에서 생성형 AI 활용」 모델을 우리도 적용할 계획이 있나** | 새로 생김 — 4장 |
| 기관 개인정보 담당 | **조례 전문과 요금 기준값만** 보내는 것(예약 자료 없음)에 대한 의견 | 1단계는 개인정보가 안 들어가므로 **답이 쉬워졌습니다** |
| 회계·계약 담당 | 해외 종량제 결제가 되나 / **공단·시 차원의 공용 AI 구독이 이미 있나** | 앞 문서 그대로 |
| **담당자 본인** | **비로그인에게 챗봇을 열 것인가** (7장 ①) | **새로 생김 · 만들기 전에 답이 있어야 합니다** |
| 〃 | 조례 전문을 ELIS 에서 받아 주기 (7장 ②) | **새로 생김 · 지금 할 수 있습니다** |

---

## 10. 한 문단 요약

메이저 업체는 **API 입력을 학습에 쓰지 않는다는 점에서 이미 비슷합니다.**
차이는 보존 기간과 **데이터가 어느 나라에 있는가**에서 나고, 지자체에서는 뒤쪽이 훨씬 큽니다.
품질(조문 인용)로는 **Claude Sonnet 5** 가 1순위이고, 결재가 막히면
**국내 저장 + CSAP 인증이 있는 Azure OpenAI (Korea Central)** 가 대안입니다.
**Claude 에는 한국 리전이 없으므로**(AWS 서울은 Global 엔드포인트뿐) 개인정보를 안 보내는 설계로 풀어야 하는데,
**조례 질의응답은 애초에 예약 자료를 안 보내므로 1단계에서는 문제가 없습니다.**
지식은 조례 전문 1만 5천 토큰을 **통째로 프롬프트에 넣으면 되고, 벡터DB 는 필요 없습니다.**
비용은 월 1만~2만 원이라 여전히 걸림돌이 아닙니다.

진짜 걸림돌은 두 개고, 둘 다 코드 바깥에 있습니다.

> ① **조례 전문이 아직 없습니다.** ELIS 에서 받는 것부터가 출발점입니다.
> ② **로그인 안 한 사람에게 챗봇을 열 것인가** — 열면 2026-08-27 의 금액 비공개 결정이 무너집니다.
> **로그인한 사람만 쓰게 하는 쪽을 권고합니다.**

마지막으로 앞 문서의 한 줄을 그대로 옮겨 둡니다.

> **AI 에게 금액을 계산시키지 않습니다.**
> 챗봇은 조례를 **찾아 주는 것**까지입니다. 금액은 `calcFee()` 가 냅니다.

---

## 출처

조사 시점 2026-09-21. 요금·정책은 자주 바뀌므로 결재 직전에 다시 확인합니다.

**모델·요금**
- [Claude 요금](https://platform.claude.com/docs/en/about-claude/pricing) — 모델별 단가, 캐시 기록·읽기 배수
- [Claude API 데이터 보존](https://platform.claude.com/docs/en/manage-claude/api-and-data-retention) — 기본 미보존, Covered Models 30일, 안전 시스템에 걸린 건 최대 2년
- [Anthropic 인증 현황](https://privacy.claude.com/en/articles/10015870-what-certifications-has-anthropic-obtained) · [Trust Center](https://trust.anthropic.com/) — SOC 2 · ISO 27001 · ISO 42001
- [Claude in Amazon Bedrock](https://platform.claude.com/docs/en/build-with-claude/claude-in-amazon-bedrock) — **리전 표: 서울 `ap-northeast-2` 는 Global 전용**
- [OpenAI 데이터 관리](https://developers.openai.com/api/docs/guides/your-data) — API 학습 제외, 기본 30일
- [OpenAI 아시아 데이터 레지던시](https://openai.com/index/introducing-data-residency-in-asia/) · [레지던시 확대](https://openai.com/index/expanding-data-residency-access-to-business-customers-worldwide/) — 한국 포함(저장 기준)
- [CLOVA Studio](https://www.ncloud.com/product/aiService/clovaStudio) · [HyperCLOVA X](https://clova.ai/hyperclova)

**국내 규제·망**
- [Microsoft Azure 한국 CSAP](https://learn.microsoft.com/en-us/azure/compliance/offerings/offering-korea-csap) · [Azure OpenAI Q&A (한국)](https://microsoft.github.io/azurekorea/azure-openai-qna/) — CSAP(Low, 그룹 C), Korea Central
- [KISA 국가 망 보안체계(N2SF) 실증 사례집](https://www.kisa.or.kr/20604/form?lang_type=KO&page=&postSeq=23171) (2026.4) · [보도](https://byline.network/2026/04/13-653/) — 「업무 환경에서 생성형 AI 활용」 포함
- [개인정보위 생성형 AI 개발·활용 개인정보 처리 안내서](https://www.privacy.go.kr/front/bbs/bbsView.do?bbsNo=BBSMSTR_000000000049&bbscttNo=20836)

**구현**
- [Supabase Edge Functions 제한](https://supabase.com/docs/guides/functions/limits) — 무료 150초 · CPU 2초(바깥 API 대기 제외)
- [Supabase Edge Functions 보안](https://supabase.com/docs/guides/functions/auth) — `verify_jwt` 기본값

> ⚠️ **이 작업 환경에서 확인하지 못한 것**: 국가법령정보센터(`law.go.kr`)와 자치법규정보시스템(`elis.go.kr`)이
> 모두 접속이 막혀 있어 **조례 전문과 조문 번호를 직접 대조하지 못했습니다.**
> 조례 내용은 `CLAUDE.md` 에 정리된 값을 그대로 인용했습니다. 담당자 PC 에서 원문 확인이 필요합니다.
