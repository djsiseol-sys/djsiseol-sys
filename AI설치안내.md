# AI 도우미 설치 안내

윗줄의 **`AI 도우미`** 단추를 켜는 절차입니다. 한 번만 하면 됩니다.

> **지금 상태**: 대장에는 이미 다 들어가 있습니다. **키를 넣고 함수를 올리기 전까지는
> 단추가 아예 안 보입니다** — 그게 정상이고, 그동안 달력·계산·저장은 지금 그대로 돕니다.

---

## 0. 먼저 — 키를 어디에 두는가

**이것부터 읽어 주세요.** 나머지는 절차일 뿐입니다.

| 어디 | 되나 | 왜 |
|---|---|---|
| `index.html` | ❌ | 저장소가 공개이고 Pages 로 서빙됩니다. 주소만 알면 누구나 읽습니다 |
| **설정 화면 · `ledger` 표** | ❌ | **이 대장은 로그인 안 해도 조회가 됩니다.** 설정에 넣으면 누구나 받아 읽습니다 |
| **Supabase Edge Function Secrets** | ✅ | 키가 **Supabase 서버에만** 있습니다. 브라우저로 내려오지 않고 저장소에도 안 들어갑니다 |

그래서 키는 **어느 파일에도 적지 않고** 대시보드에 직접 타이핑합니다 (3단계).

- **키를 대화창·메신저·문서에 붙여넣지 마세요.** `sb_secret_` 키와 같은 규칙입니다
- 실수로 올렸다면 **그 키를 폐기하고 새로 발급**합니다. 지웠다고 끝이 아닙니다 (git 이력에 남습니다)

---

## 1. AI 업체에서 키 받기

지금 기본값은 **OpenAI `gpt-5.6-luna`** 입니다 (입력 $0.20 / 출력 $1.20 per 1M tokens).
다른 업체로 바꿔도 대장은 한 줄도 안 고칩니다 — 5단계 참고.

1. [platform.openai.com](https://platform.openai.com) 에서 계정을 만듭니다
2. **Billing** 에서 결제 수단을 넣고 **선불 크레딧 $5** 정도만 충전합니다
3. **Limits** 에서 **월 지출 한도**를 겁니다 (예: $10). **마지막 안전판이니 꼭 거세요**
4. **API keys → Create new secret key** 로 키를 만듭니다.
   프로젝트를 따로 만들어 그 프로젝트 키를 쓰면 나중에 쓴 양을 따로 볼 수 있습니다
5. **키는 이때 한 번만 보입니다.** 창을 닫기 전에 3단계까지 바로 진행하세요

---

## 2. 함수 올리기

Supabase 대시보드에서 합니다. CLI 를 깔지 않아도 됩니다.

1. [supabase.com/dashboard](https://supabase.com/dashboard) → 프로젝트 `facility-ledger`
2. 왼쪽 **Edge Functions** → **Deploy a new function** → **Via Editor**
3. 이름을 **`ask`** 로 합니다 (**이 이름이어야 합니다.** 대장이 이 주소를 부릅니다)
4. 저장소의 [`supabase/functions/ask/index.ts`](supabase/functions/ask/index.ts) 내용을
   **통째로 복사해 붙여넣고** 배포합니다
5. 배포된 함수의 설정에서 **`Verify JWT` 가 켜져 있는지 확인**합니다.
   **끄지 마세요** — 끄면 주소를 아는 누구나 우리 요금을 태울 수 있습니다

> CLI 를 쓰신다면: `supabase functions deploy ask` — `supabase/config.toml` 에
> `verify_jwt = true` 가 이미 적혀 있습니다.

---

## 3. 키 넣기 (여기서 키를 처음 입력합니다)

**Edge Functions → Secrets → Add new secret**

| 이름 | 값 |
|---|---|
| `AI_API_KEY` | 1단계에서 받은 키 (`sk-proj-…`) |

이것만 넣으면 나머지는 기본값으로 돕니다. 바꾸고 싶을 때만 아래를 더 넣습니다.

| 이름 | 기본값 | 무엇 |
|---|---|---|
| `AI_MODEL` | `gpt-5.6-luna` | 쓸 모델 이름 |
| `AI_BASE_URL` | `https://api.openai.com/v1` | 업체 주소 |
| `AI_STYLE` | `responses` | 요청 형식. `chat` 으로 바꿀 수 있습니다 |
| `AI_EFFORT` | `low` | 생각 깊이. `off` 로 두면 아예 안 보냅니다 |
| `AI_MAX_TOKENS` | `1200` | 답 길이 상한 |

---

## 4. 확인

1. 대장을 열고 **담당자 계정으로 로그인**합니다
2. 윗줄에 **`AI 도우미`** 단추가 생기면 성공입니다
3. 눌러서 예시 질문을 하나 눌러 봅니다

**단추가 안 보이면** 아래를 차례로 봅니다.

| 증상 | 볼 곳 |
|---|---|
| 로그인했는데도 단추가 없다 | 함수 이름이 `ask` 가 맞는지 · 배포가 끝났는지 |
| 창을 열면 붉은 글씨로 `키가 설정돼 있지 않습니다` | 3단계 Secrets 의 이름이 `AI_API_KEY` 가 맞는지 |
| `키를 거부했습니다 (401)` | 키가 잘못됐거나 폐기됐습니다. 새로 발급 |
| `모델이나 주소를 찾지 못했습니다 (404)` | `AI_MODEL` 철자 · `AI_STYLE` 을 `chat` 으로 바꿔 보기 |
| `응답 형식이 예상과 다릅니다` | `AI_STYLE` 을 `responses` ↔ `chat` 으로 바꿔 보기 |
| `429` | 한도를 넘었습니다. OpenAI 대시보드에서 잔액·한도 확인 |

오류는 **창 안에 붉은 말풍선으로 그대로 나옵니다.** 무엇을 고쳐야 할지 알 수 있게 해 둔 것입니다.

---

## 5. 업체를 바꾸려면

`index.html` 은 **업체를 모릅니다.** `/functions/v1/ask` 만 부릅니다.
그래서 바꿀 때 **대장은 한 줄도 안 고칩니다.**

| 어디로 | 넣을 값 |
|---|---|
| 다른 OpenAI 모델 | `AI_MODEL` 만 바꿉니다 |
| Azure OpenAI (국내 저장·CSAP) | `AI_BASE_URL` 을 Azure 엔드포인트로, `AI_STYLE=chat` |
| 국산 모델 (CLOVA 등) | `AI_BASE_URL`·`AI_STYLE` 을 맞춥니다. 형식이 많이 다르면 함수만 고칩니다 |

왜 이 선택지들인지는 [`AI챗봇모델조사.md`](AI챗봇모델조사.md) 에 정리해 두었습니다.

---

## 6. 무엇을 보내고 무엇을 안 보내나

| 보냅니다 | 안 보냅니다 |
|---|---|
| 지금 설정된 **요금 기준** (시설 단가·시간대·가산·감면·부속시설 금액과 근거 조문) | **메모** — 창에서 켜야 들어갑니다 |
| **대장에 적힌 예약 내역** — 날짜·요일·시각·시설·구분·신청 단체·목적·인원·감면·**금액·입금** | 전화번호·주민번호 **형태의 글자** |
| 내가 친 질문 | |

**단체명과 이용 목적은 개인정보가 아닙니다.** 이 대장이 처음부터 연락처를 담지 않기로 한
결정 덕분에, 보낼 개인정보가 애초에 없습니다.

**메모만 다릅니다.** 「김○○ 주무관 010-… 확인」 같은 것이 적히는 칸이라
**기본으로 빼 두었습니다.** 창의 `메모도 보내기` 를 켜야 들어갑니다.

그리고 **어느 칸이든 전화번호·주민번호 형태는 보내기 전에 지웁니다** (`[연락처 지움]`).
메모를 켜도 번호는 나가지 않습니다.

**무엇을 보내는지 창 위에 적혀 있습니다** — `예약 51건 (2026-09-01 ~ 2026-12-20) · 금액 포함 · 메모 제외`
처럼요. 보이지 않게 나가는 자료가 없습니다.

`자료 범위` 에서 **전체 기간 / 올해 / 이 달** 을 고를 수 있습니다. 넓을수록 잘 답하지만
보내는 양이 늘어납니다 (요금은 그래도 건당 10원 안팎입니다).

주고받은 말은 **자료에 저장되지 않습니다.** 창을 닫으면 사라집니다.

## 6-1. 표와 엑셀

예약을 여러 건 물으면 **진짜 표로** 나옵니다. 금액은 오른쪽으로 붙습니다.

표가 든 답에는 우측 위에 **`⤓ 엑셀`** 단추가 생깁니다. 누르면 **그 답의 표만** `.xlsx` 로 떨어집니다 —
창에 있는 다른 답은 들어가지 않습니다.

- 첫 줄에 제목, 둘째 줄에 **물어본 질문**이 적혀 있어 나중에 무슨 파일인지 알 수 있습니다
- **금액은 숫자로 들어갑니다** — 엑셀에서 드래그하면 합계가 잡힙니다
- 날짜는 글자 그대로입니다 (`46260` 같은 숫자로 바뀌지 않습니다)

---

## 7. 지켜지는 것

- **AI 가 요금을 새로 산출하지 않습니다.** 조례의 가산·감산을 적용해 금액을 내는 일은
  `calcFee()` 가 합니다. 「이렇게 쓰면 얼마예요?」 에는 계산하지 않고 「요금 계산」 탭으로 안내합니다.
  **대장에 이미 적힌 금액은 그대로 알려 줍니다** (그게 이 창을 쓰는 이유입니다)
- 여러 건을 합칠 때는 **각 건을 먼저 나열한 뒤** 합계를 내고, 「목록 → 수익집계용」 과
  대조해 보라고 덧붙입니다. 월별 합계는 **대장이 미리 더해서 넘겨줍니다**
- **근거 조문 없이 답하지 않습니다.** 못 찾으면 「조례에서 확인하지 못했습니다」 라고 답합니다
- **로그인한 사람만 씁니다.** 화면에서 감추는 데 그치지 않고 `verify_jwt` 가 서버에서 막습니다
- **AI 가 꺼져도 대장은 그대로 돕니다.** 키가 없거나 요금이 끊기면 단추만 사라집니다

## 8. 돈이 얼마나 드나

`gpt-5.6-luna` 기준입니다. 질문 한 건에 요금 기준(약 3~4천 토큰)과 답(약 500토큰)이 오갑니다.

| 쓰는 양 | 한 달 요금 |
|---|---|
| 하루 20건 × 20일 = 월 400건 | **약 $0.5 안팎 (700원 정도)** |

거의 들지 않습니다. 그래도 **3단계의 월 지출 한도는 꼭 걸어 두세요** — 실수나 남용을 막는 것은
요금 자체가 아니라 그 한도입니다.
