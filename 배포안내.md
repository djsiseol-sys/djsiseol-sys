# GitHub Pages 배포 안내

`index.html` 하나를 인터넷 주소로 열 수 있게 만드는 절차입니다.
빌드나 설치가 필요 없고, 저장소에 파일을 올려두면 GitHub 이 그대로 서빙합니다.

배포된 주소: **https://djsiseol-sys.github.io/djsiseol-sys/**

---

## 1단계 — Pages 켜기 (최초 1회)

1. 저장소 화면에서 **Settings**(설정) 탭
2. 왼쪽 메뉴 **Pages**
3. *Build and deployment* 의 **Source** 를 `Deploy from a branch` 로 둔다
4. **Branch** 를 **`main`** 으로 고르고, 폴더는 `/ (root)` 로 둔다
5. **Save**

> **브랜치를 `main` 으로 맞추는 것이 중요합니다.** 예전에 다른 브랜치를 골라 두면
> 새로 고친 내용이 올라가도 주소에는 옛 화면이 그대로 나옵니다.
> 화면 왼쪽 위 제목 옆의 **판 번호**를 보면 최신 파일인지 바로 알 수 있습니다.

1~2분 뒤 같은 화면 위쪽에 주소가 뜹니다. 이후에는 저장소에 파일을 올릴 때마다
자동으로 다시 배포되므로, 이 설정을 다시 만질 일은 없습니다.

> Actions 워크플로 파일은 두지 않았습니다. 정적 파일 하나뿐이라
> `Deploy from a branch` 방식이 가장 단순하고 고장날 곳이 없습니다.

## 2단계 — 서버 접속 정보 넣기

이 단계를 하지 않으면 **각자 브라우저에만 저장**되어 서로 자료가 보이지 않습니다.

`index.html` 의 섹션 1-B 위쪽에 있는 두 줄을 채웁니다.

```js
const SUPABASE_URL = "https://<프로젝트ref>.supabase.co";
const SUPABASE_ANON_KEY = "sb_publishable_...";
```

- Supabase 대시보드 → **Project Settings → API** 에서 두 값을 복사합니다
- 주소는 **`https://xxxx.supabase.co` 까지만** 넣습니다.
  대시보드에는 `.../rest/v1` 이 붙은 형태로 보이는 경우가 있는데, 그대로 넣으면 **로그인이 안 됩니다**
  (코드가 `/auth/v1`, `/rest/v1` 을 스스로 붙이기 때문입니다. 붙여 넣어도 자동으로 떼어내지만,
  처음부터 기본 주소만 넣는 쪽이 확실합니다)
- `sb_publishable_` 로 시작하는 키를 넣습니다. **공개돼도 되는 키**입니다
- `sb_secret_` / `service_role` 키는 **절대 넣지 않습니다**
- 값을 채워 넣고 저장소에 올리면, 직원들은 주소만 열면 바로 대장을 봅니다

값을 코드에 넣지 않고 각자 설정 화면에서 입력해도 동작하지만,
그러면 사람마다 한 번씩 입력해야 하므로 코드에 넣는 쪽을 권합니다.

## 3단계 — 저장소가 공개일 때 반드시 확인할 것

publishable 키는 공개돼도 되는 키지만, 그것은 **신규 가입이 막혀 있을 때만** 그렇습니다.

- Supabase → **Authentication → Sign In / Providers** 에서 신규 가입을 꺼둡니다
  (`서버설치안내.md` 3단계)
- 켜져 있으면 주소를 아는 누구나 계정을 만들어 자료를 고칠 수 있습니다
- 쓰기 권한은 서버의 RLS 정책이 정합니다. 로그인하지 않으면 조회만 됩니다

가입 차단을 아직 안 했다면, **그것부터 하고 Pages 를 켭니다.**

## 4단계 — 열어서 확인

배포된 주소를 열고 아래를 봅니다.

- [ ] 달력이 그려진다
- [ ] 우측 상단 저장 상태가 "서버에 연결됨" 이다 (2단계를 했을 때)
- [ ] 로그인 전에는 조회만 되고, 담당자 계정으로 로그인하면 등록 버튼이 보인다
- [ ] 다른 PC 에서 열었을 때 같은 자료가 보인다
- [ ] 업무망 PC 에서도 열린다 — 망 정책상 `*.supabase.co` 가 막히면 저장이 안 됩니다.
      이 경우 조회는 되지만 서버 연결 안 됨 표시가 뜹니다

## 자주 나오는 문제

| 증상 | 원인 |
|---|---|
| 404 | Pages 가 아직 켜지지 않았거나 배포 중입니다. 1~2분 기다립니다 |
| 화면은 뜨는데 "이 브라우저에 저장" | 2단계를 안 했습니다 |
| "서버 연결 안 됨" | URL·키가 틀렸거나, 망에서 supabase.co 가 막혀 있습니다 |
| 로그인이 안 됨 · "연결 실패" | Project URL 뒤에 `/rest/v1` 이 붙어 있는 경우가 가장 많습니다. 기본 주소만 남기고 다시 저장하세요 |
| 로그인 401 | 키가 틀렸습니다. `sb_publishable_` 키를 다시 확인하세요 |
| 로그인 400 · "Invalid login credentials" | 이메일·비밀번호가 다르거나, 계정이 Auto Confirm 으로 만들어지지 않았습니다 (`서버설치안내.md` 4단계) |
| 글꼴이 다르게 보임 | Google Fonts 를 못 불러온 것입니다. 동작에는 지장이 없습니다 |

## 주소를 바꾸고 싶다면

저장소 이름을 `djsiseol-sys.github.io` 로 만들면 주소가
`https://djsiseol-sys.github.io/` 로 짧아집니다. 기관 도메인을 붙이려면
Settings → Pages → *Custom domain* 에서 설정하고 DNS 를 등록합니다.
