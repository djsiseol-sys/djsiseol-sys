# 예전 엑셀 대장 가져오기

시스템을 쓰기 전에 **엑셀로 적어 두던 대관대장**을 올리는 절차입니다.
몇 해치를 올려 두면 「분석」 탭에서 해마다의 흐름을 볼 수 있습니다.

> **양식을 맞출 필요가 없습니다.** 열 이름이 무엇이든, 해마다 모양이 달라도 됩니다.
> 올리면 열을 맞춰 보여 드리고, **맞는지 확인한 뒤에야** 들어갑니다.

---

## 1. 준비

| | |
|---|---|
| 파일 형식 | **`.xlsx`** 또는 **`.csv`** |
| 예전 `.xls` 라면 | 엑셀에서 열고 **다른 이름으로 저장 → Excel 통합 문서(.xlsx)** |
| 누가 | **담당자 계정으로 로그인**해야 합니다 |

**먼저 `⚙ 설정 → JSON 내보내기` 로 지금 대장을 받아 두세요.** 잘못 들어가도 되돌릴 수 있습니다.

---

## 2. 올리기

1. 윗줄 **`⚙ 설정`** → **데이터 관리** → **`엑셀·CSV 로 예약 가져오기`**
2. 파일을 **끌어다 놓거나** 눌러서 고릅니다
3. 시트가 여러 개면 **시트**에서 고릅니다 (해마다 시트를 나눠 적었다면 한 해씩 올립니다)

---

## 3. 확인할 네 가지

### ① 머리글 줄

열 이름이 적힌 줄입니다. 제목이나 빈 줄이 위에 있어도 저절로 찾지만, 다르면 골라 주세요.

### ② 기준 연도

날짜를 **`3.5`** 처럼 연도 없이 적은 경우에만 씁니다.
시트 이름에 `2019` 가 있으면 저절로 들어갑니다. **맞는지 꼭 보세요.**

### ③ 열 맞추기

어느 열이 무엇인지입니다. 저절로 맞춰 두지만 틀린 것이 있으면 고칩니다.

- **`날짜` 와 `시설` 은 반드시** 골라야 합니다
- 쓰지 않을 항목은 `— 가져오지 않음 —` 으로 둡니다
- **`담당자 연락처`·`성명` 같은 열은 아예 고를 수 없습니다.**
  이 대장은 개인정보를 담지 않기로 한 구조라서 그렇습니다 —
  무엇을 뺐는지 파일 이름 옆에 빨간 글씨로 나옵니다

> 열 이름이 아주 낯설어 잘 못 맞추면, AI 도우미가 켜져 있을 때 **`AI 로 열 맞추기`** 를
> 눌러 볼 수 있습니다. AI 는 **어느 열이 무엇인지 짚어 줄 뿐**이고,
> 결과는 그대로 미리보기에 채워지니 **확인은 똑같이 하셔야 합니다.**

### ④ 시설 이름 맞추기

엑셀에 `제1경기장` 처럼 지금 시설 이름과 다르게 적어 두었다면 여기에 모입니다.
어느 시설인지 골라 주세요. **고르지 않으면 그 줄은 건너뜁니다.**

`충무 체육관`·`야구장` 처럼 알아볼 수 있는 이름은 저절로 붙습니다.

---

## 4. 금액은 어떻게 되나

기본은 **`쓰지 않음 — 조례대로 다시 계산합니다`** 입니다. 권장합니다.

엑셀 금액 열을 고르면 **조례 계산과 몇 건이 다른지** 바로 알려 드립니다.
예전 기준으로 받았거나 적어 둔 값이 다를 수 있습니다.

| 고르는 것 | 무엇이 들어가나 |
|---|---|
| **조례대로 다시 계산** (권장) | 금액을 넣지 않습니다. 조례대로 산정되고 `산정 상세` 에 근거 조문이 나옵니다 |
| 직접 입력 금액으로 | 엑셀에 적힌 금액이 **`이용료 직접 입력`** 자리에 들어갑니다 (계산값 대신 그 금액을 씁니다) |

예전에 실제로 받은 금액을 그대로 남겨야 하는 자료라면 뒤쪽을,
지금 기준으로 다시 보려면 앞쪽을 고릅니다.

---

## 5. 미리보기

줄마다 들어갈 값과 **맨 오른쪽에 확인 사항**이 나옵니다.

| 줄 색 | 뜻 |
|---|---|
| 보통 | 들어갑니다 |
| 흐림 | **이미 대장에 있는 건**입니다. 건너뜁니다 |
| 붉음 | 날짜나 시설을 못 읽어 **건너뜁니다** |

아래에 `가져올 건 N건 · 이미 있어 건너뜀 N건 · 읽지 못해 건너뜀 N건` 이 나옵니다.

**`가져오기 N건` 을 누르기 전에는 대장이 바뀌지 않습니다.** 마음껏 고쳐 보세요.

---

## 6. 가져온 뒤

- 서버에 **바로 올라갑니다.** 다른 PC 에서도 10초 안에 보입니다
- 달력·목록·분석에 곧바로 나옵니다
- 잘못 들어갔으면 **2단계에서 받아 둔 JSON 으로 되돌립니다**
  (`⚙ 설정 → JSON 불러오기`)

---

## 자주 겪는 것

| 증상 | 어떻게 |
|---|---|
| 「엑셀 파일이 아닙니다」 | 예전 `.xls` 입니다. 엑셀에서 `.xlsx` 로 저장해 올리세요 |
| 날짜가 다 「읽지 못함」 | **기준 연도**를 보세요. 또는 열 맞추기에서 `날짜` 가 엉뚱한 열을 가리킵니다 |
| 날짜가 한 해 어긋남 | 시트 이름에서 가져온 **기준 연도**가 다릅니다 |
| 시설이 다 비어 있음 | 열 맞추기에서 `시설` 을 고르고, 아래 **시설 이름 맞추기**에서 이름을 붙이세요 |
| 감면이 다 「해당 없음」 | `감면` 열을 골랐는지 보세요. `80%`·`전액면제` 같은 말을 읽습니다 |
| 준비 건이 행사로 들어옴 | `건 구분` 열을 고르거나, 「구분」 칸에 `준비` 라고 적혀 있어야 합니다 |
| 한글이 깨진 CSV | 엑셀에서 **CSV UTF-8** 로 저장하거나, `.xlsx` 로 올리세요 |
| 두 번 올려 겹쳤나 걱정 | 같은 건(날짜·시설·시각·단체가 같음)은 **저절로 건너뜁니다** |
