# 이 대장에 AI 를 넣는다면 — 도입 검토

작성 2026-08-25 · **기획 문서입니다. 아직 아무것도 만들지 않았습니다.**

이 도구가 지금 무엇인지부터 짚고 시작합니다.
**대전광역시 체육시설 관리운영 조례를 코드로 옮겨 놓은, 서버 없는 HTML 파일 하나**입니다.
만든 이유는 하나였습니다 — 이용료를 계산할 때마다 담당자 한 명만 조례를 찾아볼 수 있어서
생기는 병목을 없애는 것. 그래서 AI 를 넣을지 말지도 **"이 병목을 더 없애 주는가"**
하나로만 판단합니다. 그렇지 않은 기능은 아무리 그럴듯해도 넣지 않습니다.

---

## 결론부터

| | |
|---|---|
| **넣을 가치가 있나** | 있습니다. 다만 **두 가지 기능만** 입니다 |
| **어떤 기능** | ① 산정 내역을 쉬운 말로 풀어 주기 ② 조례 질의응답 |
| **어디에 놓나** | Supabase Edge Function 프록시. **브라우저에서 직접 부르면 안 됩니다** |
| **돈** | 월 1만 원 안쪽 (아래 5장에 계산 있음). 비용은 걸림돌이 아닙니다 |
| **진짜 걸림돌** | ① 업무망에서 외부 API 가 막힐 가능성 ② 해외 종량제 결제 절차 |
| **먼저 할 일** | **0단계 — 코드를 고치지 말고**, 정해진 질문 문구로 2주 써 보기 (비용 0) |

가장 중요한 한 줄:

> **AI 에게 금액을 계산시키지 않습니다.** 계산은 지금처럼 `calcFee()` 가 합니다.
> AI 는 이미 나온 숫자를 **설명**하거나, 입력칸을 **채워 주는 것**까지만 합니다.

돈이 걸린 대장에서 AI 가 산수를 틀리면, 이 도구가 지금까지 쌓은 신뢰가 통째로 없어집니다.
그리고 LLM 은 산수를 틀립니다. 이건 성능이 좋아지면 해결되는 문제가 아니라
**애초에 그 일을 시키지 않아야 하는 문제**입니다.

---

## 1. AI 가 실제로 쓸모 있는 자리

후보를 넓게 늘어놓고 하나씩 잘랐습니다.

### 넣을 만한 것

#### ① 산정 내역을 쉬운 말로 풀어 주기 ★★★

지금 우측 상세의 `산정 상세` 는 이렇게 나옵니다.

```
① 기본 전용사용료   체육경기 · 별표 2            90,000
② 토·공휴일 가산    기본액의 50% · 별표 2 비고   45,000
＝ 당일 주간 전용사용료                          135,000
③ 야간 가산         당일 주간 × 1.5 · 별표 2 비고 67,500
합계                                            202,500
```

조례를 아는 사람에게는 완벽합니다. 처음 보는 직원에게는 아직 어렵습니다.
AI 가 이걸 받아서 이렇게 바꿔 줍니다.

> 토요일 저녁 7시부터 쓰셨기 때문에 두 가지가 붙었습니다.
> 토요일이라 기본요금 9만 원의 절반인 4만 5천 원이 더해져 13만 5천 원이 되고(별표 2 비고),
> 여기에 야간 사용이라 다시 1.5배가 되어 20만 2,500원입니다.
> 평일 낮이었다면 9만 원이었습니다.

**이 도구의 존재 이유와 정확히 같은 방향입니다.** 그리고 위험이 거의 없습니다 —
AI 에게 넘기는 것은 **이미 계산이 끝난 표**뿐이고, 숫자를 새로 만들 여지가 없습니다.
예약 원문(단체명·메모)은 보내지 않아도 됩니다.

#### ② 조례 질의응답 ★★★

> "고등학교 야구부가 토요일 야간에 야구장 쓰면 얼마예요?"
> "학교 수업으로 쓰면 감면 몇 프로였죠?"
> "관람료 받는 행사는 뭘 더 내야 해요?"

지금은 이 질문에 담당자가 답합니다. 그게 병목입니다.
조례 전문을 AI 에게 통째로 주고 **반드시 조문 번호와 함께 답하게** 하면,
담당자가 아닌 사람도 스스로 확인할 수 있습니다.

조건이 하나 있습니다 — **조문을 못 찾으면 "조례에 없습니다" 라고 답하게** 해야 합니다.
지어내는 순간 이 기능은 없느니만 못합니다.

#### ③ 자연어로 예약 등록 ★★

공문이나 메일 문구를 붙여넣으면 날짜·시간·시설·목적이 채워진 등록창이 열립니다.

> "9월 12일(토) 14:00~18:00 충무체육관, 대전시배드민턴협회 가을리그"
> → 날짜 2026-09-12 / 14:00–18:00 / 충무체육관 / 체육경기 / 대전시배드민턴협회

**저장은 사람이 확인한 뒤에만** 합니다. AI 가 `state` 를 직접 고치지 않습니다.
값어치는 확실한데, **여기서 개인정보가 새어 들어옵니다** — 공문에는 담당자 이름과
전화번호가 붙어 있습니다. 4장의 필터가 반드시 먼저 있어야 하는 기능입니다.

#### ④ 이상 징후 알려 주기 ★★

시간 중복은 이미 코드가 잡습니다(`conflictIds`). AI 가 잡을 것은 **"규칙으로 쓰기 애매한 것"** 입니다.

- 목적이 "○○ 콘서트" 인데 `useType` 이 `체육경기` 로 등록됨 (금액이 11만 원 틀립니다)
- 감면 100% 면제인데 근거가 될 만한 단서가 목적·단체명에 없음
- 전기료가 평소의 10배 — 0 을 하나 더 친 것으로 보임
- 준비 건만 있고 정작 행사가 등록 안 된 날

**고치지 않고 알려만 줍니다.** 표시는 지금 중복 경고와 같은 자리에 붙이면 됩니다.

#### ⑤ 월간 보고·공문 초안 ★

수익집계용 표를 문장으로 바꾸거나, 사용료 산정 통지서 초안을 씁니다.
값어치는 있지만 이건 **대장에 넣을 기능이 아니라 담당자가 따로 쓰면 되는 일**입니다
(3장 C안). 도구를 고쳐서 넣을 이유가 약합니다.

### 넣지 말아야 할 것

| 후보 | 왜 안 되나 |
|---|---|
| **대체공휴일 자동 판정** | `LUNAR_HOLIDAYS` 에 한 줄 더하는 게 더 정확하고 더 쌉니다. 공휴일을 틀리면 가산이 통째로 틀립니다 — AI 가 손댈 자리가 아닙니다 |
| **AI 가 금액을 계산** | 위에 쓴 대로. 절대 안 됩니다 |
| **AI 가 자동으로 고치기** | 이 대장은 자료 전체가 서버의 한 행입니다. AI 가 잘못 고치면 되돌릴 방법이 마땅치 않습니다 |
| **전화 접수 받아쓰기** | 통화에는 이름·연락처가 반드시 나옵니다. 이 도구가 연락처를 뺀 결정을 되돌리는 셈입니다 |
| **조례 해석 다툼을 AI 가 결정** | 야간 가산 방식(`surchargeMode`), 여러 시간대에 걸친 사용 — 이건 **시에 질의해서 문서로 받을 일**이지 AI 가 정할 일이 아닙니다. AI 에게 물어서 참고는 하되, 근거로 삼지 않습니다 |

### 한 표로

| | 값어치 | 위험 | 공수 | 권고 |
|---|---|---|---|---|
| ① 쉬운 말 설명 | 높음 | **거의 없음** | 작음 | **1순위** |
| ② 조례 질의응답 | 높음 | 낮음(조문 인용 강제) | 중간 | **2순위** |
| ③ 자연어 등록 | 중간 | **높음**(개인정보) | 중간 | 3순위, 필터 먼저 |
| ④ 이상 징후 | 중간 | 낮음 | 중간 | 나중 |
| ⑤ 보고 초안 | 중간 | 낮음 | — | **도구 밖에서** (C안) |
| 대체공휴일 자동 | 낮음 | 높음 | — | ✕ |
| 금액 계산 | — | **매우 높음** | — | ✕ |
| 전화 받아쓰기 | 낮음 | **매우 높음** | — | ✕ |

---

## 2. 어디에 놓을 것인가 — 이게 진짜 문제입니다

지금 구조를 다시 봅니다.

```
브라우저(index.html 한 개)  ──fetch──▶  Supabase REST  ──▶  ledger 표 한 행
        ↑
   GitHub Pages 가 그대로 서빙 · 서버 코드가 없음
```

**우리에게는 서버가 없습니다.** 이 사실 하나가 AI 도입 방식을 거의 다 결정합니다.

### A안 — 브라우저에서 Anthropic API 를 직접 부른다 ✕

```js
fetch("https://api.anthropic.com/v1/messages", {
  headers: { "x-api-key": "sk-ant-..." }   // ← 페이지 소스에 그대로 남습니다
})
```

**절대 안 됩니다.** 저장소가 공개이고 GitHub Pages 로 서빙되므로,
주소를 아는 누구나 개발자도구로 키를 꺼내 우리 요금으로 씁니다.
CLAUDE.md 에 이미 적혀 있는 **"`sb_secret_` / `service_role` 키를 코드에 넣지 않는다"** 와
정확히 같은 종류의 금지입니다.

> Supabase publishable 키는 코드에 박아도 되는데 왜 이건 안 되냐 — publishable 키는
> 서버의 RLS 가 뒤에서 막아 주기 때문입니다. Anthropic API 키는 **키 자체가 곧 권한**이라
> 뒤에서 막아 줄 것이 없습니다.

예외가 하나 있습니다. **담당자가 자기 키를 설정 화면에 직접 입력하는 방식(BYOK)** 이면
코드에 키가 남지 않습니다. 담당자가 한 명이니 실무상 불가능하지는 않습니다.
다만 키가 그 PC 의 localStorage 에만 남아 다른 PC 에서 또 넣어야 하고,
조회하는 직원은 AI 기능을 아예 못 씁니다 — **②번(조례 질의응답)의 목적과 정면으로 어긋납니다.**
시험 삼아 며칠 써 보는 용도로만 씁니다.

### B안 — Supabase Edge Function 프록시 ✅ 권장

```
브라우저 ──fetch──▶ Supabase Edge Function ──▶ Anthropic API
                     (ANTHROPIC_API_KEY 는 여기 Secrets 에)
```

- **이미 쓰고 있는 Supabase 안에서 끝납니다.** 새 업체·새 계약·새 서버가 없습니다
- 키가 브라우저에 절대 안 나옵니다
- 함수에서 **로그인한 사용자인지 확인**하고 부르게 하면, 아무나 우리 요금을 태울 수 없습니다
- 무료 플랜이 **월 50만 호출**입니다. 하루 수십 건 쓰는 이 도구에는 사실상 무제한입니다
  ([Supabase 요금 문서](https://supabase.com/docs/guides/functions/pricing))
- **단일 파일 제약을 깨지 않습니다.** `index.html` 은 여전히 하나이고 `fetch` 만 늘어납니다.
  Edge Function 은 "같은 화면의 복사본" 이 아니라 **화면이 없는 중계기**입니다 —
  CLAUDE.md 가 금지한 것(번들러·프레임워크·CDN 라이브러리)에 해당하지 않습니다

단점도 분명합니다.

- **배포 절차가 하나 늘어납니다.** 지금은 "파일 하나 올리면 끝" 인데,
  함수는 Supabase CLI 나 대시보드에서 따로 올려야 합니다
- 담당자가 혼자 유지보수하기 어려울 수 있습니다. `배포안내.md` 수준의 안내서가 하나 더 필요합니다
- 함수 안의 프롬프트를 고치려면 **다시 배포**해야 합니다 (index.html 처럼 즉시 반영이 아닙니다)

### C안 — 도구를 고치지 않고 사람이 손으로 ✅ 가장 싸고 가장 빠릅니다

Claude 나 ChatGPT 웹 화면에 조례 전문과 산정 내역을 붙여넣고, 답을 읽고, 필요하면 옮겨 적습니다.

**코드를 한 줄도 고치지 않고 오늘 당장 됩니다. 비용 0원. 결제 절차도 망 승인도 필요 없습니다.**

이게 우스워 보이지만 그렇지 않습니다. **①②⑤번 값어치의 대부분을 여기서 얻습니다.**
필요한 것은 정해진 질문 문구 대여섯 개를 적어 둔 문서 하나입니다 (6장 0단계).

그리고 이걸 2주 써 보면 **정말 자주 나오는 질문이 무엇인지**가 기록으로 남습니다.
그게 B안을 만들 때의 사양이 됩니다. 지금 상상으로 기능을 정하는 것보다 훨씬 정확합니다.

### D안 — AI 가 아닌 것으로 푼다 ✅ 일부 항목에는 이게 정답

솔직하게 짚습니다. **"AI 를 넣자" 는 요구의 절반은 사실 UI 문제입니다.**

- 이상 징후 ④의 대부분은 **규칙으로 잡힙니다** — 전기료 자릿수, 목적에 "콘서트·공연" 이 들어갔는데
  `체육경기` 로 등록됨, 감면 100% 인데 감면 근거 메모가 비어 있음. AI 없이 `conflictIds` 옆에
  검사 몇 개 더 붙이면 됩니다. 더 싸고 더 확실하고 오프라인에서도 됩니다
- 조례 질의응답 ②의 절반은 **설정 화면에 이미 있는 값을 잘 보여주는 것**으로 해결됩니다.
  "토요일 야간 얼마?" 는 요율표를 한 장 그려 두면 끝입니다

**B안을 만들기 전에 D안으로 되는 것부터 골라내야 합니다.** 그러면 AI 로 남는 것이
생각보다 훨씬 적어지고, 그 적은 것이 진짜 AI 가 필요한 일입니다.

### E안 — 사내 LLM / 온프레미스 (참고)

업무망에서 외부 API 가 끝내 안 열리면 이 길뿐입니다.
다만 **부서 단위로는 과합니다.** 대전시나 공단 차원에서 공용 AI 서비스가 생기면
그때 거기에 붙는 것이 현실적입니다. 지금 이 도구가 먼저 나설 자리가 아닙니다.

### 정리

| | 키 안전 | 조회자도 쓰나 | 배포 부담 | 오늘 되나 | 권고 |
|---|---|---|---|---|---|
| A 브라우저 직접 | ✕ | — | 없음 | — | **금지** |
| A' BYOK | △ | ✕ | 없음 | 예 | 시험용만 |
| **B 프록시** | ✅ | ✅ | 중간 | 아니오 | **본 도입** |
| **C 손으로** | ✅ | △ | 없음 | **예** | **먼저 이것부터** |
| **D AI 아님** | ✅ | ✅ | 작음 | — | **골라내기** |
| E 온프레미스 | ✅ | ✅ | 큼 | 아니오 | 기관 차원 |

---

## 3. 넘지 말아야 할 선

만들기로 한다면 CLAUDE.md 의 「하지 말 것」 에 아래를 더해야 합니다.
지금 그 목록이 이 도구를 지켜 온 방식 그대로입니다.

1. **AI 에게 금액을 계산시키지 않는다.** 계산은 `calcFee()` 가 한다.
   AI 는 이미 나온 숫자를 설명하거나 입력칸을 채우는 것까지만 한다
2. **AI 출력은 사람이 확인한 뒤에만 저장된다.** AI 가 `state` 를 직접 고치지 않는다
3. **근거 조문 없는 답을 내보내지 않는다.** 지금 `why` 규칙과 같다.
   조문을 못 찾으면 "조례에 없습니다" 라고 답하게 한다 — 지어내면 없느니만 못하다
4. **개인정보를 새로 만들지 않는다.** AI 기능 때문에 이름·연락처가 들어오게 되면 안 된다.
   보내기 전에 거른다 (4장)
5. **AI 가 꺼져도 대장은 그대로 동작한다.** 망이 막히든 키가 만료되든 요금이 끊기든,
   달력·계산·저장은 지금과 똑같이 된다. AI 는 **덤으로 붙는 것**이지 뼈대가 아니다
6. **AI 응답을 자료에 남기지 않는다.** `state` 에 저장하면 그때부터 그 문장이
   "대장에 적힌 것" 이 된다. 화면에 잠깐 띄우고 버린다

---

## 4. 법·망 — 결재 전에 걸리는 것들

> ⚠️ **이 장의 조문은 이 환경에서 법제처 국가법령정보 API 로 확인하지 못했습니다**
> (망에서 `law.go.kr` 접근이 막혀 있습니다). 아래는 공개 자료로 정리한 것이므로,
> 실제 결재 전에 **기관 개인정보 담당자와 법무 검토**를 반드시 거쳐야 합니다.

### 4-1. 애초에 무엇을 보내는가

이 대장의 가장 큰 자산이 여기서 나옵니다.

**연락처 항목을 의도적으로 뺀 결정이 두 번째 배당금을 줍니다.**

- 개인정보 보호법 제2조제1호는 개인정보를 **"살아 있는 개인에 관한 정보"** 로 정의합니다.
  단체명·법인명은 원칙적으로 여기에 들어가지 않습니다
- 이 대장이 가진 것은 **신청 단체명 · 이용 목적 · 인원 · 메모** 뿐입니다.
  성명도 전화번호도 주소도 없습니다
- → **원칙적으로 개인정보를 보내는 것이 아닙니다.** 그러면 아래 4-2 가 아예 발동하지 않습니다

다만 **새는 구멍이 세 곳** 있습니다. 여기를 막아야 위 문장이 유지됩니다.

| 구멍 | 예 | 막는 법 |
|---|---|---|
| 메모 자유입력 | "김○○ 주무관 010-…에게 확인" | **메모는 기본적으로 안 보낸다.** 보낼 때는 사용자가 명시적으로 켜야 한다 |
| 단체명에 사람 이름 | "홍길동배드민턴교실" | 개인 사업자·강사 이름이 상호에 들어간 경우. 완전히 막기 어려우니 **위험으로 기록**해 둔다 |
| 자연어 등록(③) | 공문 전문 붙여넣기 | **보내기 전에 정규식으로 전화번호·주민번호를 지운다.** 지운 것을 사용자에게 보여준다 |

전화번호(`01[016-9]-?\d{3,4}-?\d{4}`)와 주민번호(`\d{6}-?[1-4]\d{6}`) 패턴은
**보내기 직전에 브라우저에서** 막습니다. 서버까지 갈 필요가 없습니다.

### 4-2. 개인정보가 실제로 섞이면 무슨 일이 생기나

미국 서버(Anthropic API)로 보내는 것이므로 **두 가지가 동시에** 걸립니다.

- **처리위탁 — 제26조**: 위탁계약서, 위탁사실 공개, 수탁자 관리·감독 의무
- **국외 이전 — 제28조의8**: 원칙 금지. 예외는 ① 법률·조약에 규정 ② 정보주체와의 계약 이행에 필요
  ③ 이전받는 자가 개인정보 보호 인증 ④ 이전 국가가 국내법과 실질적으로 동등한 보호 수준
  ⑤ **정보주체의 별도 동의** 중 하나

**지자체가 시설 사용 신청자에게 "당신의 신청 내용을 미국 AI 서비스로 보내도 되겠습니까"
별도 동의를 받는 것은 현실적으로 어렵습니다.** 받는다 해도 동의를 거부한 신청자는
AI 기능을 못 쓰게 되어 기능이 반쪽이 됩니다.

→ **결론: 개인정보를 안 보내는 설계를 지키는 것이 유일하게 깔끔한 길입니다.**
이건 회피가 아니라, 이 도구가 처음부터 잡아 온 방향을 그대로 이어가는 것입니다.

### 4-3. 공공기관 특칙

- **개인정보 영향평가(제33조)** — 일정 규모 이상 개인정보파일을 운용하는 공공기관 대상입니다.
  이 도구는 개인정보를 처리하지 않으므로 해당하지 않을 가능성이 높습니다
- **개인정보파일 등록(제32조)** — 같은 이유로 해당하지 않을 가능성이 높습니다
- 둘 다 **기관 개인정보 담당자 확인이 필요합니다.** "해당 없다" 는 판단도 문서로 남겨 두는 편이 낫습니다

참고: 개인정보보호위원회 [생성형 AI 개발·활용을 위한 개인정보 처리 안내서](https://www.kisa.or.kr/2060301/form?postSeq=41&lang_type=KO&page=1) (2025. 8.),
[생성형 AI 서비스 이용자를 위한 개인정보 보호 가이드](https://www.kisnews.net/709634) (2026. 5.)

### 4-4. 망분리 — 아마 여기가 가장 큰 벽입니다

업무망 PC 에서 `api.anthropic.com` 이 막혀 있을 가능성이 높습니다.
사실 지금 `*.supabase.co` 도 같은 문제를 안고 있습니다 —
**업무망에서 이 대장이 서버 모드로 도는지부터 확인이 안 된 상태입니다.**

다만 상황이 바뀌고 있습니다.

- 국정원이 기존 망분리를 **N2SF(신 국가망보안체계)** 로 바꾸는 중입니다.
  정보 등급에 따라 통제를 **차등 적용**해서, 공공기관 업무 PC 에서도 생성형 AI 와
  클라우드를 쓸 수 있게 하는 방향입니다
  ([뉴시스](https://www.newsis.com/view/NISX20260313_0003547805))
- 2026년 국가·공공기관 사이버보안 평가에서 망분리 지표가 **N2SF 적용으로 바뀌었고**,
  KISA 가 55억 원 규모 도입·실증 사업을 진행 중입니다
  ([보안뉴스](https://m.boannews.com/html/detail.html?idx=141870),
  [드림시큐리티](https://www.dreamsecurity.com/pr/news/1091))

**그래서 지금 서둘러 결정할 이유가 없습니다.** 기관 정보보안 담당에게 물을 것은 두 가지뿐입니다.

1. 업무망에서 `*.supabase.co` 가 지금 열려 있는가 (이건 AI 와 무관하게 확인이 필요합니다)
2. 우리 기관에 N2SF 전환 계획이 있는가, 있다면 언제인가

**현실적인 회피책**: 인터넷망 PC 에서만 AI 버튼이 뜨게 하고,
업무망에서는 조용히 감춥니다. 지금 서버 연결 실패를 다루는 방식과 똑같이 하면 됩니다
(`fetch` 가 실패하면 버튼을 감춘다). 이러면 **업무망 사용자에게 아무 손해가 없습니다.**

### 4-5. 조달·회계

비용은 작지만 **"쓴 만큼 내는 해외 서비스" 는 지자체 회계에서 가장 다루기 번거로운 형태**입니다.
월 1만 원이라도 예산 항목과 결제 수단이 필요합니다.

| 길 | 현실성 |
|---|---|
| 기관 카드로 Anthropic 직접 결제 | 가능하지만 절차가 번거롭습니다. 소액이라 오히려 결재가 더 귀찮을 수 있습니다 |
| **공단·시 차원의 공용 AI 구독에 얹기** | **가장 깔끔합니다. 이미 있는지부터 확인하세요** |
| 담당자 개인 계정으로 C안만 | **비용 0. 결재 없음.** 0단계에 딱 맞습니다 |

---

## 5. 돈은 얼마나 드나

실제 숫자로 봐야 판단이 됩니다. 아래는 **2026-06-24 기준 공개 요금**입니다.

| 모델 | 모델 ID | 입력 $/100만 토큰 | 출력 $/100만 토큰 |
|---|---|---|---|
| Haiku 4.5 | `claude-haiku-4-5` | $1 | $5 |
| Sonnet 5 | `claude-sonnet-5` | $2 | $10 |
| Opus 5 | `claude-opus-5` | $5 | $25 |

### 전제

- 조례 전문 + 별표를 프롬프트에 넣으면 대략 **1만 5천 토큰** 정도입니다 (한글은 토큰이 후하게 잡힙니다)
- 질문 한 건 = 입력 약 16,000 토큰 + 출력 약 600 토큰
- **하루 20건 × 월 20일 = 월 400건** — 이 부서 규모에서는 넉넉하게 잡은 값입니다

### 그냥 쓰면

| 모델 | 월 요금 | 원화(1,450원 기준) |
|---|---|---|
| Haiku 4.5 | $7.6 | 약 11,000원 |
| Sonnet 5 | $15.2 | 약 22,000원 |
| Opus 5 | $38.0 | 약 55,000원 |

### 프롬프트 캐싱을 걸면

조례 본문은 매번 똑같습니다. 캐싱하면 그 부분이 **입력 요금의 10%** 로 떨어집니다.

| 모델 | 월 요금 | 원화 |
|---|---|---|
| Haiku 4.5 | 약 $2.2 | 약 3,000원 |
| **Sonnet 5** | **약 $4.4** | **약 6,000원** |
| Opus 5 | 약 $11 | 약 16,000원 |

### 결론

> **월 1만 원 안쪽입니다. 비용은 이 결정의 걸림돌이 아닙니다.**
> 걸림돌은 **망과 결제 절차**입니다.

### 어느 모델을 쓰나

| 기능 | 모델 | 왜 |
|---|---|---|
| ② 조례 질의응답 | **Sonnet 5** | 조문 번호를 정확히 인용해야 합니다. 긴 조례에서 Haiku 는 조문을 흘립니다 |
| ① 쉬운 말 설명 | **Sonnet 5** | 이미 계산된 표를 풀어 쓰는 일이라 Haiku 로도 되지만, ②와 같은 함수를 쓰면 관리가 단순합니다 |
| ③ 자연어 등록 | **Haiku 4.5** | 날짜·시간·시설을 뽑는 일. 구조화 출력으로 형식을 고정하면 충분합니다. 가장 자주 불릴 기능이라 싼 쪽이 맞습니다 |
| ④ 이상 징후 일괄 점검 | Haiku 4.5 + **Batch API** | 실시간이 아니어도 되므로 배치로 돌리면 **요금이 절반**입니다 |

---

## 6. 만든다면 이 순서로

### 0단계 — 코드를 고치지 않고 2주 (지금 당장, 0원)

**이걸 건너뛰지 마세요.** 여기서 얻는 것이 나머지 전부를 결정합니다.

1. `AI질의문구.md` 를 하나 만듭니다 — 조례 전문과 산정 내역을 붙여넣는 정해진 문구 5~6개
2. 담당자가 2주 동안 그걸로 씁니다 (Claude·ChatGPT 웹 화면)
3. **어떤 질문을 실제로 몇 번 했는지 적어 둡니다**

2주 뒤에 나오는 것:
- 정말 자주 나온 질문 목록 → **1·2단계의 사양**
- AI 답이 틀린 경우 → **어디까지 믿을 수 있는지의 실측값**
- 애초에 안 쓰게 되더라고 하면 → **여기서 멈춥니다. 아무것도 안 만들었으니 손해가 없습니다**

### 1단계 — 프록시 하나, 기능 하나

- Supabase Edge Function `ask` 를 하나 만듭니다. `ANTHROPIC_API_KEY` 는 Supabase Secrets 에
- 화면에 붙는 것은 **버튼 하나뿐** — 우측 상세 `산정 상세` 옆의 `쉬운 말로 설명`
- 보내는 것은 **이미 계산된 `lines[]`(금액 + 근거 조문)뿐**. 단체명·메모는 안 보냅니다
- 실패하면 버튼이 사라질 뿐 나머지는 지금 그대로

이 단계에서 확인하는 것: **망이 열리나 · 결제가 되나 · 담당자가 실제로 쓰나.**
셋 중 하나라도 안 되면 2단계로 가지 않습니다.

### 2단계 — 조례 질의응답

- 설정 화면 `시설 · 요금 기준` 탭 아래에 질문 칸
- 조례 전문을 프롬프트에 넣고 캐싱. **조문 번호 인용 필수, 못 찾으면 "조례에 없습니다"**
- **조회자에게도 열어 줍니다.** "왜 이 금액인지 누구나 볼 수 있게" 라는 이 도구의 목적과 같은 방향입니다
- 호출량이 걱정되면 그때 로그인 필요로 바꿉니다 (실측 후 결정)

### 3단계 — 자연어 등록 · 이상 징후

- 등록창에 붙여넣기 칸. AI 가 채우고 **사람이 확인 후 저장**
- **여기서 개인정보 필터가 필수**가 됩니다 (4-1)
- 이상 징후는 **D안(규칙)으로 되는 것을 먼저 골라내고** 남은 것만 AI 로

### 지금은 하지 않기로 하는 것

대체공휴일 자동 판정 · AI 에 의한 금액 산정 · AI 의 자동 수정 · 전화 접수 받아쓰기

---

## 7. 결정 전에 물어봐야 할 것

| 누구에게 | 무엇을 |
|---|---|
| **기관 정보보안 담당** | 업무망에서 `*.supabase.co` 가 열려 있나 / `api.anthropic.com` 은? / N2SF 전환 계획이 있나 |
| **기관 개인정보 담당** | 단체명·이용 목적만 외부 AI API 로 보내는 것에 대한 의견 / 영향평가·파일등록 대상인지 |
| **회계·계약 담당** | 해외 종량제 결제가 가능한가 / **공단·시 차원의 공용 AI 구독이 이미 있나** |
| **담당자 본인** | 0단계 2주 기록 — 어떤 질문을 가장 많이 했나 |

첫 줄의 "업무망에서 supabase 가 열려 있나" 는 **AI 와 무관하게 지금 확인해야 하는 항목**입니다
(CLAUDE.md 「아직 확인이 안 된 것」 과 이어집니다).

---

## 8. 한 문단 요약

이 도구에 AI 를 넣을 값어치는 있습니다. 다만 **넣을 자리는 두 곳뿐**이고
(산정 내역 쉬운 말 설명 · 조례 질의응답), **넣는 방법은 Supabase Edge Function 프록시** 하나입니다.
브라우저에서 API 를 직접 부르는 것은 키가 새므로 절대 안 됩니다.
비용은 월 1만 원 안쪽이라 문제가 아니고, 진짜 문제는 **업무망과 결제 절차**입니다.
그리고 무엇을 만들지 정하기 전에, **코드를 한 줄도 고치지 말고 2주 동안 손으로 써 보는 것**이
가장 값싸고 가장 정확한 다음 걸음입니다.

마지막으로 다시 한 번:

> **AI 에게 금액을 계산시키지 않습니다.**
> 이 도구가 하는 일은 "왜 이 금액인지" 를 담당자가 아닌 사람도 볼 수 있게 하는 것입니다.
> AI 는 그 설명을 **더 쉽게** 만들어 줄 수는 있어도, 그 자리를 **대신할 수는 없습니다.**
