# 전기료·수도료 엑셀을 올리면 알아서 채워 주기 — 도입 검토

작성 2026-08-25 · **기획 문서입니다. 아직 아무것도 만들지 않았습니다.**
같이 볼 것: [`AI도입검토.md`](AI도입검토.md) (AI 전반 검토)

## 무엇을 하고 싶은가

9~12월 일정은 이미 다 짜여 있습니다. 전기료·수도료는 행사가 끝나고 고지서가 나온 뒤에야
알 수 있어서 지금은 **한 건씩 손으로 넣고** 있습니다.

> 정산 엑셀 파일을 올리면, 대장에 이미 있는 일정과 맞춰서 알아서 채워 준다.
> 엑셀 양식이 파일마다 다 달라도 내용을 이해해서 정확하게 넣는다.

---

## 결론부터

| | |
|---|---|
| **기술적으로 되나** | **됩니다.** 라이브러리 없이 브라우저에서 엑셀을 읽을 수 있다는 것을 실제로 확인했습니다 (2장) |
| **AI 가 양식을 알아볼 수 있나** | 알아봅니다. 다만 **매달 AI 를 부르는 방식은 틀렸습니다** |
| **핵심 설계** | AI 는 **양식을 처음 볼 때 한 번만** 부릅니다. 두 번째부터는 AI 없이 돌아갑니다 |
| **가장 중요한 안전장치** | **AI 에게 금액을 옮겨 적게 하지 않습니다.** "몇 행 몇 열에서 가져와라" 는 **좌표**만 받습니다 |
| **자동으로 저장하나** | **아니오.** 대조 화면에서 사람이 확인한 뒤에만 들어갑니다 |
| **먼저 물어볼 것** | **"그 엑셀은 누가 만듭니까? 우리가 양식을 정할 수 있습니까?"** — 답에 따라 AI 가 아예 필요 없어집니다 |

---

## 1. 먼저 — AI 가 필요 없을 수도 있습니다

기능을 설계하기 전에 물어야 할 것이 하나 있습니다.

> **그 엑셀 파일은 누가 만듭니까?**

| 누가 만드나 | 우리가 양식을 정할 수 있나 | 그러면 |
|---|---|---|
| 체육관 관리사무소 직원 | **정할 수 있습니다** | **AI 불필요.** 아래 「역방향」 |
| 한전·상수도사업본부 고지서 | 못 정합니다 | AI 가 필요합니다 |
| 위탁업체·타 기관 | 대개 못 정합니다 | AI 가 필요합니다 |

### 역방향 — 우리가 양식을 내주고 채워 오게 한다

대장에서 **「전기·수도 기입 양식」 엑셀을 내보냅니다.** 9~12월 일정이 이미 다 들어 있으니,
날짜·시설·단체·목적이 미리 채워진 표가 나옵니다. 빈 칸은 전기료·수도료 두 열뿐입니다.

```
예약번호(숨김)  날짜        시설      신청 단체            목적          전기료  수도료
r_a91f...       2026-09-12  충무체육관  대전시배드민턴협회   가을리그       [    ]  [    ]
r_c02b...       2026-09-12  충무체육관  한밭고 농구부        연습경기       [    ]  [    ]
```

이걸 채워서 돌려받으면 **맨 앞의 예약번호로 100% 정확하게 들어갑니다.**
양식을 알아볼 필요도, 단체명을 맞춰 볼 필요도, AI 도 없습니다.

이미 있는 `exportTable()` 을 조금 늘리면 되는 일이라 **공수가 가장 작고 정확도가 가장 높습니다.**
상대가 우리 양식을 받아 줄 수만 있다면 **이게 정답입니다.**

**그래서 이 질문을 먼저 하셔야 합니다.** 답이 "우리 양식으로 받을 수 있다" 면
아래 2~6장은 안 읽으셔도 됩니다.

---

## 2. 브라우저에서 엑셀을 읽을 수 있나 — 실제로 해 봤습니다

이 도구는 **라이브러리를 못 씁니다.** CDN 도, npm 도 안 됩니다 (CLAUDE.md 의 단일 파일 제약).
보통 엑셀을 읽을 때 쓰는 SheetJS 같은 것을 못 쓴다는 뜻입니다.

그래서 **정말 안 되는지 직접 확인했습니다.**

### 원리

`.xlsx` 는 사실 **XML 파일 몇 개를 담은 ZIP 파일**입니다.
그리고 요즘 브라우저에는 압축을 푸는 기능이 내장돼 있습니다 — `DecompressionStream`.
ZIP 이 쓰는 방식(`deflate-raw`)을 그대로 지원합니다.

```
.xlsx  →  ZIP 목록 훑기  →  DecompressionStream 으로 압축 풀기  →  DOMParser 로 XML 읽기
                                (브라우저 내장)                      (브라우저 내장)
```

**셋 다 브라우저에 이미 있습니다. 가져다 붙일 것이 없습니다.**

### 실험 결과

앞뒤 안 맞는 실제 정산표 비슷한 파일을 만들어 헤드리스 Chromium 에서 읽혔습니다.
제목 줄, 병합된 2단 헤더, 소계 행, 비고란까지 일부러 넣었습니다.

```
ZIP 안의 파일: xl/worksheets/sheet1.xml, xl/sharedStrings.xml, xl/styles.xml …
읽는 데 걸린 시간: 22 ms

 1 ["2026년 9월 충무체육관 사용료 정산 내역"]
 2 ["일자","사용단체","공과금(원)","","합계","비고"]
 3 ["","","전기","수도"]
 4 ["46277","대전시배드민턴협회","84300","12100","96400",""]
 5 ["46277","한밭고 농구부","31200","4800","36000","오전"]
 6 ["46284","(사)대전생활체육회","120500","18300","138800",""]
 7 ["46291","우리동네 배드민턴","45000","6200","51200","담당 김OO 010-1234-5678"]
 8 ["","소   계","281000","41400","322400"]

5행 A열 날짜 변환: 2026-09-12
```

**됩니다. 코드 200줄 남짓, 읽는 데 22밀리초, 라이브러리 0개.**
엑셀이 쓰는 두 가지 문자열 저장 방식(`sharedStrings` / `inlineStr`) 모두 확인했습니다.

### 실험에서 같이 드러난 것들

이 짧은 실험에서 이미 **실무에서 걸릴 것들이 다 나왔습니다.**

| 나온 것 | 뜻 |
|---|---|
| `46277` | **엑셀은 날짜를 숫자로 저장합니다.** 코드가 변환해야 합니다 (1900년 윤년 버그 보정 포함) |
| 2행 `공과금(원)` + 3행 `전기`/`수도` | **병합된 2단 헤더.** 한 줄만 봐서는 열이 뭔지 모릅니다 |
| 8행 `소   계` | **소계 행을 데이터로 착각하면 32만 원이 한 건에 들어갑니다** |
| `담당 김OO 010-1234-5678` | **비고란에 개인정보가 있습니다.** 이대로 외부로 나가면 안 됩니다 (5장) |
| 4행과 5행이 **같은 날 같은 시설** | 날짜만으로는 어느 건인지 못 정합니다 (4장) |
| 빈 행 건너뛰기 시험 | 엑셀은 빈 행을 아예 저장하지 않습니다. 순서대로 세면 **원본 행 번호가 어긋납니다** — `r` 속성을 읽어야 합니다 |

마지막 항목은 그냥 버그가 아닙니다. 사람이 확인하려면
**"원본 7행에서 가져왔습니다" 를 보여줘야** 하는데, 행 번호가 어긋나면 그 확인이 거짓말이 됩니다.

### 못 읽는 것

| 형식 | 되나 |
|---|---|
| `.xlsx` (2007 이후) | ✅ 확인함 |
| `.csv` | ✅ 훨씬 쉽습니다 |
| **`.xls`** (97–2003 구형) | ❌ ZIP 이 아니라 옛날 바이너리입니다. **「다른 이름으로 저장 → .xlsx」 안내가 필요합니다** |
| **PDF 고지서** | ❌ 브라우저 단독으로는 안 됩니다 |
| **한글(.hwp) 표** | ❌ 안 됩니다 |

**PDF·한글로 오면 이 기능이 아예 못 씁니다.** 어떤 형식으로 오는지 먼저 확인하세요.

---

## 3. 핵심 — AI 를 매달 부르지 않습니다

여기가 이 기획에서 가장 중요한 부분입니다.

### 흔히 생각하는 방식 (권하지 않습니다)

```
매달 파일 올림 → 표 전체를 AI 에게 보냄 → AI 가 읽고 값을 채워 줌
```

문제가 셋입니다.

1. **매달 결과가 달라질 수 있습니다.** 같은 양식을 올려도 AI 답이 미묘하게 바뀔 수 있습니다.
   돈을 다루는 대장에서 "지난달엔 맞았는데 이번달엔 틀렸다" 는 최악입니다
2. **개인정보가 매달 나갑니다.** 비고란까지 통째로
3. **AI 가 숫자를 옮겨 적습니다.** `84300` 을 `843000` 으로 쓸 여지가 생깁니다

### 권하는 방식 — 양식당 한 번만

```
1회차  새 양식 → AI 가 "어느 열이 무엇인지" 만 알아봄 → 사람이 확인 → 양식으로 저장
2회차~ 같은 양식 → 저장해 둔 규칙으로 코드가 읽음 → AI 부르지 않음
```

AI 가 돌려주는 것은 값이 아니라 **표를 읽는 규칙**입니다.

```json
{
  "머리행": 3,
  "자료시작행": 5,
  "열": { "날짜": 1, "단체": 2, "전기료": 3, "수도료": 4 },
  "단위": "원",
  "건너뛸행": "B열이 '소계' 또는 '합계' 로 시작하는 행",
  "합계행": 8
}
```

이렇게 되면,

- **두 번째부터는 AI 가 개입하지 않습니다.** 같은 파일을 열 번 올리면 열 번 같은 결과입니다
- **AI 호출은 시설 3곳 × 양식이 바뀔 때만** — 1년에 열 번도 안 됩니다. **비용이 사실상 0원입니다**
- **개인정보 노출 면적이 확 줄어듭니다.** 열이 뭔지 알아보는 데는 **머리행과 예시 2~3행이면 충분**합니다.
  표 전체를 보낼 이유가 없습니다
- AI 가 틀려도 **사람이 1회차에 확인하고 고칩니다.** 그 뒤로는 고쳐진 규칙이 계속 쓰입니다

**양식이 전부 달라도 괜찮은 이유가 여기 있습니다.** 양식이 다섯 가지든 열 가지든,
**한 번씩만 등록하면 그 뒤로는 전부 자동**입니다. 그리고 실무에서 양식은
**매달 바뀌는 게 아니라 보내는 곳마다 다를 뿐**입니다.

### AI 는 금액을 옮겨 적지 않습니다

한 번 더 강조합니다.

> **AI 가 돌려주는 것은 "3열이 전기료다" 라는 좌표뿐입니다.**
> `84300` 이라는 숫자는 **코드가 직접 그 칸에서 읽습니다.**

이러면 AI 가 자릿수를 틀릴 방법 자체가 없습니다.
`AI도입검토.md` 의 **"AI 에게 금액을 계산시키지 않는다"** 를 한 단계 더 밀어붙인 것입니다.

---

## 4. 어느 예약에 넣을 것인가 — 여기가 진짜 어렵습니다

열을 알아보는 것보다 **"이 84,300원이 어느 예약 것이냐"** 가 훨씬 어렵습니다.
그리고 여기서 틀리면 **엉뚱한 단체에게 남의 요금을 물립니다.** 조용히.

### 실제로 걸리는 것들

| 상황 | 예 |
|---|---|
| 같은 날 같은 시설에 여러 건 | 위 실험의 4·5행 — 둘 다 9/12 충무체육관 |
| 단체명 표기가 다름 | 대장 `대전광역시배드민턴협회` / 엑셀 `대전시배드민턴협회` |
| 괄호·법인 표기 | `(사)대전생활체육회` vs `대전생활체육회` |
| 대장에 없는 행 | 취소된 행사, 대장에 안 올린 사용 |
| 한 행이 여러 예약 | 월 단위로 뭉쳐 온 고지 |
| 날짜 표기 | `9/12` `9.12` `2026-09-12` `46277` |

### 3단계로 좁힙니다 — AI 는 마지막에만

```
1단계  날짜 + 시설 로 후보 찾기          → 대부분 여기서 1건. 코드가 처리. AI 불필요
2단계  후보가 여럿이면 단체명 맞춰보기   → 공백·괄호·(사)·(주) 지우고 비교. 코드가 처리
3단계  그래도 애매하면                   → 여기서만 AI. 또는 사람이 직접
```

**대부분의 행이 1단계에서 끝납니다.** AI 는 정말 애매한 몇 줄에만 씁니다.

그리고 **한 번 확인한 별칭은 기억합니다.**
`대전시배드민턴협회 = 대전광역시배드민턴협회` 를 사람이 한 번 확정하면
`settings.orgAliases` 에 저장되어 다음부터는 자동입니다.
양식 기억과 같은 구조입니다 — **쓸수록 AI 를 덜 부르게 됩니다.**

### 등급을 매겨서 보여줍니다

| 등급 | 언제 | 화면에서 |
|---|---|---|
| **A 확실** | 후보 1건 + 합계 맞음 | 접어 둡니다. 건수만 보여줍니다 |
| **B 확인 필요** | 후보 여럿 → 단체명으로 골랐음 | **펼쳐서 위에 놓습니다.** 왜 골랐는지 함께 |
| **C 사람이 직접** | 못 찾음 / 합계 안 맞음 / 이미 값이 있음 | **맨 위.** 손으로 고르게 합니다 |

**C → B → A 순으로 위에서부터 쌓습니다.** 담당자가 볼 것부터 눈에 들어와야 합니다.
A 를 스무 줄 스크롤해서 C 를 찾게 만들면 안 됩니다.

---

## 5. 정확도를 어떻게 담보하나

"AI 가 잘하겠지" 로는 부족합니다. **틀려도 잡히는 구조**가 필요합니다.

### ① 합계 대조 — 가장 강한 장치

정산표에는 거의 항상 **소계·합계 행**이 있습니다 (실험 파일에도 있었습니다).

```
읽어낸 전기료를 다 더한 값   281,000
엑셀 8행의 소계             281,000   ✅ 일치
```

**안 맞으면 한 건도 적용하지 않습니다.** 부분 적용은 금지합니다 —
어디까지 들어갔는지 모르게 되는 것이 안 들어간 것보다 나쁩니다.

이 한 줄이 **열을 잘못 잡은 것, 소계 행을 데이터로 센 것, 천원 단위를 원으로 읽은 것을
거의 다 잡아냅니다.** AI 의 정확도와 무관하게 작동하는 검산입니다.

### ② 단위 확인

`전기료(천원)` 이면 1,000을 곱해야 합니다. 실험 파일 2번에 `84.3` 이 있었습니다.

- 머리글에 `천원`·`천 원`·`단위:천원` 이 있으면 표시하고 **사람에게 확인받습니다**
- 값에 소수점이 있으면 **거의 확실히 천원 단위입니다** — 경고를 띄웁니다
- 부가세 별도/포함도 같이 물어봅니다

### ③ 원본 근거를 함께 보여줍니다

```
84,300원   ←  올린 파일 7행 C열
```

이 도구가 산정 내역에 **근거 조문(`why`)** 을 붙이는 것과 같은 규칙입니다.
**어디서 온 숫자인지 못 보여주면 넣지 않습니다.**

### ④ 못 넣은 행은 반드시 보여줍니다

매칭 실패한 행을 조용히 버리면, 담당자는 다 들어간 줄 압니다.
**"3행은 맞는 예약을 못 찾았습니다" 를 화면에 남깁니다.**

### ⑤ 되돌리기 한 번

적용 직전 상태를 담아 뒀다가 `되돌리기` 한 번은 되게 합니다.
대장은 자료 전체가 서버의 한 행이라, 잘못 들어가면 되돌릴 방법이 마땅치 않습니다.

### ⑥ 개인정보를 걸러서 보냅니다

실험 파일 7행에 **`담당 김OO 010-1234-5678`** 이 있었습니다. 실제 정산표에 흔히 있습니다.

- **3장 방식이면 애초에 표 전체를 보내지 않습니다** — 머리행 + 예시 몇 줄이면 됩니다
- 그 몇 줄에도 전화번호·주민번호 패턴이 있으면 **보내기 전에 브라우저에서 지웁니다**
- **지운 것을 사용자에게 보여줍니다** — 뭘 지웠는지 모르게 하면 안 됩니다
- **비고 열은 기본적으로 안 보냅니다.** 정산에 필요한 것은 날짜·단체·금액뿐입니다

> 이 대장이 처음부터 연락처를 안 받기로 한 것이 여기서 또 값을 합니다.
> 우리가 만든 자료에는 개인정보가 없고, **남이 만든 엑셀에만 섞여 들어옵니다.**
> 그 경계에서 막으면 됩니다.

---

## 6. 화면은 이렇게

**자동으로 저장하지 않습니다.** 대조 화면을 반드시 거칩니다.

```
┌ 전기·수도 정산표 올리기 ─────────────────────────────────────┐
│ 파일: 2026년 9월 충무체육관 정산.xlsx                        │
│ 양식: 「충무 관리사무소 양식」 으로 읽었습니다  [양식 바꾸기] │
│ 합계 대조: 전기 281,000 = 281,000 ✅  수도 41,400 = 41,400 ✅ │
├──────────────────────────────────────────────────────────────┤
│ ⚠ 확인이 필요합니다 (2건)                                    │
│                                                              │
│  원본 7행  9/26  우리동네 배드민턴   45,000 / 6,200          │
│    → 대장에 「우리동네배드민턴클럽」 이 있습니다. 같은 곳입니까? │
│       [예, 같은 곳입니다]  [아니오]  [직접 고르기]           │
│                                                              │
│  원본 5행  9/12  한밭고 농구부       31,200 / 4,800          │
│    → 9/12 충무체육관에 2건이 있습니다                        │
│       ( ) 09:00–12:00 한밭고등학교 농구부  ← 이것 같습니다   │
│       ( ) 14:00–18:00 대전시배드민턴협회                     │
│                                                              │
│ ✅ 확실 (2건)                                     [펼쳐 보기] │
├──────────────────────────────────────────────────────────────┤
│                        [확인한 것만 넣기]   [전부 취소]      │
└──────────────────────────────────────────────────────────────┘
```

- **이미 값이 들어 있는 건은 덮어쓰기 전에 옛 값과 새 값을 같이 보여줍니다**
- 넣고 나면 요금 관리 화면으로 넘어가서 결과를 바로 보게 합니다
- 입금 표시(`paid`·`paidPower`·`paidWater`)는 **건드리지 않습니다**

---

## 7. 그래서 정확할까 — 솔직하게

| 단계 | 잘될까 | 왜 |
|---|---|---|
| 엑셀 읽기 | **확실합니다** | 실험으로 확인. AI 가 관여하지 않습니다 |
| 열이 뭔지 알아보기 | **잘합니다** | LLM 이 가장 잘하는 일 중 하나입니다. 게다가 사람이 1회차에 확인합니다 |
| 숫자 옮기기 | **틀릴 수 없습니다** | AI 가 옮기지 않습니다. 코드가 좌표에서 읽습니다 |
| **어느 예약인지 맞추기** | **여기가 약합니다** | 같은 날 같은 시설이 여럿이면 단체명 표기에 기댈 수밖에 없습니다 |
| 합계 검산 | **확실합니다** | 산수는 코드가 합니다 |

**약한 곳은 딱 한 군데 — 매칭입니다.** 그래서 매칭만은 사람이 확인하게 만들어 둔 것입니다.

### 만들었다면 이렇게 시험해 보세요

기능을 켜기 전에 **실제 파일 10개**로 확인합니다.

- [ ] 열을 잘못 잡은 파일이 있는가 → 있으면 그 양식만 손으로 등록하면 됩니다
- [ ] **엉뚱한 예약에 들어간 건이 한 건이라도 있는가**
- [ ] 합계가 안 맞는데 그냥 넘어간 파일이 있는가
- [ ] 못 넣은 행을 빠짐없이 보여줬는가

> **엉뚱한 예약에 들어간 건이 한 건이라도 나오면, 자동 적용을 켜지 않습니다.**
> 대조 화면에서 사람이 전부 확인하는 방식으로만 씁니다.
> 그래도 손으로 다 치는 것보다 훨씬 빠릅니다.

---

## 8. 만든다면 이 순서로

**단계마다 그 자체로 쓸모가 있게** 나눴습니다. 중간에서 멈춰도 손해가 아닙니다.

### 0단계 — 물어보기 (오늘)

**"그 엑셀은 누가 만듭니까? 우리 양식으로 받을 수 있습니까?"**
"우리 양식으로 된다" 면 → **1'단계로 가고 여기서 끝납니다.**

또 하나: **어떤 형식으로 옵니까?** `.xls`·PDF·한글이면 이 기능이 안 됩니다 (2장).

### 1'단계 — 역방향 (AI 없음, 공수 가장 작음)

「전기·수도 기입 양식」 엑셀 내보내기. 숨긴 열에 예약번호.
채워서 돌려받으면 **100% 정확하게** 들어갑니다.

### 1단계 — 붙여넣기 (AI 없음)

엑셀에서 표를 복사해 붙여넣고, **어느 열이 무엇인지 사람이 고릅니다.**
그 선택을 양식으로 저장합니다. **두 번째 파일부터는 그냥 붙여넣기만 하면 끝입니다.**

여기까지가 값어치의 **절반 이상**입니다. AI 가 전혀 없습니다.

### 2단계 — 파일 올리기 (AI 없음)

1단계에 2장의 엑셀 읽기를 붙입니다. 붙여넣기 대신 파일을 끌어다 놓습니다.
합계 대조·등급·대조 화면이 여기서 들어갑니다.

### 3단계 — 처음 보는 양식만 AI 가 (`AI도입검토.md` 1·2단계 이후)

처음 보는 양식일 때만 AI 가 열을 추측하고, 사람이 확인하면 양식으로 저장합니다.
**AI 가 붙는 곳은 여기 한 군데뿐입니다.**

앞 단계에서 프록시(Edge Function)가 이미 서 있어야 합니다 — 이 기능만을 위해
프록시를 새로 세우는 것은 배보다 배꼽이 큽니다.

### 하지 않을 것

- 확인 없이 자동 저장
- 합계가 안 맞는데 되는 것만 부분 적용
- AI 에게 금액 숫자를 옮겨 적게 하기
- 비고란을 통째로 외부로 보내기
- 매칭 실패한 행을 조용히 버리기
- 입금 표시를 같이 건드리기

---

## 9. 비용

3장 방식이면 AI 호출이 **양식당 1회**입니다. 머리행 + 예시 3줄이면 1,000토큰이 안 됩니다.

| | |
|---|---|
| 양식 등록 1회 | 0.2원 안팎 (Haiku 4.5 기준) |
| 시설 3곳 × 양식 2~3개 | 1회성 |
| 양식이 바뀔 때 | 연 몇 회 |
| **연간** | **100원이 안 됩니다** |

**비용은 아예 고려 대상이 아닙니다.** 1·2단계는 AI 가 없어서 0원입니다.

---

## 10. 한 문단 요약

**됩니다.** 브라우저에서 라이브러리 없이 엑셀을 읽는 것은 실제로 확인했고 22밀리초 걸립니다.
양식이 전부 달라도 괜찮은데, 그 이유는 AI 가 매번 알아보기 때문이 아니라
**양식을 한 번 등록하면 그 뒤로는 AI 없이 돌아가기 때문**입니다.
AI 는 금액을 옮겨 적지 않고 **"몇 열이 전기료다" 는 좌표만** 말하며,
숫자는 코드가 직접 읽고 **엑셀 자체의 합계 행으로 검산**합니다.
가장 약한 곳은 **"어느 예약 것이냐"** 이고, 그래서 그것만은 사람이 확인하는 화면을 반드시 거칩니다.

그리고 무엇보다 — **그 엑셀을 누가 만드는지부터 물어보세요.**
우리가 양식을 정할 수 있다면, **AI 는 필요 없습니다.**
