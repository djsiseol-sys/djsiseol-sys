# 시설 예약 관리대장

지자체 체육시설(충무체육관·한밭체육관·야구장) 예약을 월별 달력으로 관리하고,
조례에 따른 이용료를 자동 산정하는 내부용 도구.

## 배경 — 왜 만드는가

이용료를 계산할 때마다 담당자가 조례를 찾아봐야 해서, **담당자 한 명만 처리할 수 있는 병목**이
있었다. 요금 규칙을 설정 화면에 값으로 빼내고 산정 내역에 근거 조문을 함께 표시해서,
담당자가 아닌 사람도 "왜 이 금액인지" 볼 수 있게 하는 것이 이 도구의 목적이다.

**이 목적을 해치는 변경은 하지 않는다.** 계산 과정을 숨기거나, 결과 금액만 보여주거나,
근거 조문 표시를 빼는 방향은 안 된다.

**다만 이것은 「로그인한 직원」 에게 해당한다** (2026-08-27 담당자 결정).
주소만 알면 누구나 열 수 있는 화면이라, **로그인하지 않은 사람에게는 금액을 아예 보여주지 않는다.**
조회만 하는 직원도 금액을 보려면 계정이 있어야 한다 — 「역할」 절 참고.

## 지금 어디까지 왔나 (2026-08-26)

작업을 이어받을 때 이 절부터 읽는다.

**끝난 것**

- 달력·목록·설정 화면, 예약 등록/수정/삭제/복제, 시간 중복 검사
- **이용료 계산 엔진 — 대전광역시 체육시설 관리운영 조례(제6358호)를 그대로 옮겼다.**
  별표 2 기본 전용사용료, 별표 1의2 시간대(조기·주간·야간), 별표 2 비고의 가산·감산,
  제11조제3항 초과사용료, 제12조 관람료 수입 사용료, 제13조 감면까지 자동 계산
- 산정 내역을 근거 조문과 함께 표시 (우측 상세 `산정 상세` / 목록 수익집계용)
- 준비시간(별도 건, 체육관만) 처리
- 연락처 항목 제거 — 개인정보를 담지 않는 구조로 확정
- Supabase 연결. 자동 저장 + 저장 버튼, 10초 폴링으로 다른 사람 변경 반영
- **공휴일 2040년까지 자동 판정** + 달력에서 손으로 지정·해제 (대체공휴일용)
- **목록 두 가지** — 조회용 / 수익집계용. 이용료·부속시설·공과금을 따로 뗀다
- **인쇄 세 가지** — 달력(가로 용지, 행사·준비 따로) / 날짜별 상세 / 목록
- **날짜별 확인 표시** — 그 날 처리를 다 마치면 달력 칸과 목록에 ✓ 가 붙는다
- **요금 관리 화면** — 전기료·수도료를 따로 보고, 건마다 입금을 확인해 표시한다.
  요금 담당 전용 계정으로 역할을 나눈다
- **엑셀 내보내기** — 같은 날짜 병합, 열 너비 자동, 준비 건 파랑·토일공휴일 금색 칠
- **행사 정산서** — 달력·목록에서 체크한 건으로 부서 서식 그대로 엑셀을 낸다.
  보관용·제출용 두 장, 금액은 엑셀 수식, 입금 계좌는 설정 화면에서 넣는다

**Supabase 프로젝트**

- 프로젝트명 `facility-ledger`, 리전 서울, 무료 플랜
- URL·publishable key 를 `index.html` 섹션 1-B 위에 **박아 넣었다.** 각자 입력하지 않아도 된다.
  값을 바꾸려면 그 두 줄만 고친다 (설정 화면 입력이 있으면 그쪽이 우선한다)
- 담당자 계정: `hanjeong0303@djsiseol.or.kr`

**아직 확인이 안 된 것 — 여기부터 이어서 한다**

1. Supabase 에서 `ledger` 표 생성 SQL 을 실행했는지 (`서버설치안내.md` 2단계)
2. 신규 가입 차단을 켰는지 (3단계) — **안 켜면 누구나 계정을 만들어 자료를 고칠 수 있다**
3. 담당자 계정을 Auto Confirm 으로 만들었는지 (4단계)
4. 로컬에서 `index.html` 을 열어 로그인·저장이 되는지
5. Pages 켜기 — 저장소에 파일은 올라갔다. Settings → Pages 에서 브랜치를 고르는
   화면 조작만 남았다 (`배포안내.md` 1단계)

**그다음 할 일**

- 시설별 `1회 기준 시간` 확정 — 별표 2 에 없어 0(제한 없음)으로 두었다.
  넣어야 초과사용료(제11조제3항)가 계산된다
- 주간+야간에 걸친 사용의 처리를 시에 확인

## 사용자와 환경

- 부서 직원 내부용. 시민 대상 공개 서비스가 아니다
- **담당자 1명이 등록·수정**, 나머지 직원은 조회
- 업무망/인터넷망 PC를 함께 쓴다. 브라우저는 Edge·Chrome 기준
- IT 지원이나 서버가 없다. 파일 하나로 돌아가야 한다

## 파일 구조

```
index.html            # 전부. HTML + CSS + JS 단일 파일, 빌드 없음
                      #   GitHub Pages 가 그대로 서빙한다
                      #   SUPABASE_URL / SUPABASE_ANON_KEY 를 섹션 1-B 위에 박아 넣었다
배포안내.md           # GitHub Pages 로 올리고 주소를 켜는 절차
요금담당계정안내.md   # 요금 담당 전용 계정을 만들고 관리하는 절차
행사정산서안내.md     # 정산서를 뽑는 절차와 손으로 적는 칸 (담당자용)
AI도입검토.md         # AI 를 넣는다면 어떻게 할지 검토한 기획 문서 (아직 구현 없음)
AI챗봇모델조사.md     # 어느 모델을 어떤 보안 조건으로 붙이나 조사한 문서
AI설치안내.md         # AI 도우미를 켜는 절차 (키 발급 · 함수 배포 · Secrets)
엑셀가져오기안내.md   # 예전 엑셀 대장을 올리는 절차 (담당자용)
supabase/
  config.toml         #   함수 설정. ask 는 verify_jwt = true (끄지 않는다)
  functions/ask/index.ts  # AI 중계기. **키는 여기에 없다** — Supabase Secrets 에만 있다
.gitignore            # .env 류를 막는다 (AI 키가 실수로 올라가지 않게)
공과금엑셀기입검토.md # 전기·수도 정산 엑셀을 올려 자동 기입하는 방안 검토 (아직 구현 없음)
README.md             # 저장소 첫 화면 설명
.nojekyll             # Pages 의 Jekyll 처리를 끈다 (지우지 않는다)
서버설치안내.md       # Supabase 설정 절차와 SQL  ※ 아직 저장소에 없음
샘플데이터.json       # 테스트용 예시 자료 (단가는 임의값)  ※ 아직 저장소에 없음
```

**같은 화면의 복사본을 여러 파일로 두지 않는다.** `index.html` 하나만 고친다.

빌드 도구, 프레임워크, npm 의존성 **없음**. 브라우저에서 파일을 바로 열면 동작한다.
이 제약은 의도된 것이다 — 공유폴더에 파일 하나 두고 각자 여는 방식이 이 부서에서 유일하게
현실적인 배포 수단이다. **번들러·React·CDN 라이브러리를 도입하지 않는다.**

외부 리소스는 Google Fonts 링크 하나뿐이고, 그것도 못 불러오면 시스템 폰트로 떨어진다.

## 코드 지도

`<script>` 안이 번호 붙은 섹션으로 나뉘어 있다. 주석 헤더로 찾으면 된다.

| 섹션 | 내용 | 자주 건드리는 함수 |
|---|---|---|
| 1 | 데이터 구조·저장 | `DEFAULT_STATE`, `migrate`, `save`, `saveNow`, `pushShared` |
| 1-B | 서버 연결 | `serverCfg`, `serverLoad`, `serverSave`, `serverPoll`, `serverLogin` |
| 2 | 유틸 | `toMin`, `ymd`, `won`, `esc` |
| 3 | **이용료 계산** | `calcFee`, `feeOf`, `dayBands`, `bandsOf`, `isSurchargeDay`, `feeLinesHtml` |
| 4 | 중복 검사 | `conflictIds` |
| 5 | 달력 | `calCellsHtml`, `renderCalendar`, `renderSummary`, `byTime` |
| 6 | 우측 상세 | `renderRail` |
| 7 | 목록 | `renderList`, `listRows`, `listRow`, `LIST_MODES`, `LIST_COLS`, `colsOf` |
| 7-B | 요금 관리 | `renderFees`, `feeRows`, `currentRole`, `applyRole` |
| 7-C | **요금 계산** | `renderQuote`, `quoteRes`, `fillQuoteSelects` |
| 7-E | **분석 (그래프)** | `renderStats`, `statsData`, `stChart`, `stHover`, `ST_METRICS`, `ST_GEO` |
| 7-D | **AI 도우미** | `aiAsk`, `aiCall`, `aiSync`, `aiRateContext`, `aiResContext`, `aiBlocks`, `aiHtml`, `aiExportXlsx` |
| 8 | 설정 화면 | `renderSettings`, `renderServerCard`, `openSettings`, `applyRole` |
| 9 | 등록·수정 모달 | `openModal`, `formToRes`, `fillTimeSelects` |
| 9-B | 인쇄 | `openPrintBox`, `doPrint`, `buildPrintCalendar`, `buildPrintDetail`, `renderPrintDatePicker` |
| 9-C | 내보내기 | `exportTable` (엑셀·CSV 를 한 함수로 낸다) |
| 9-D | **행사 정산서** | `exportSettlement`, `settleBlock`, `settleData`, `xgPut`, `xgBox` |
| 9-E | **엑셀 가져오기** | `impLoad`, `readXlsx`, `unzip`, `impGuessAll`, `impRowRes`, `impApply`, `IMP_FIELDS` |
| 10 | 렌더 총괄 | `renderAll` |
| 11 | 이벤트 바인딩 | `boot`, `init` |

렌더는 전부 문자열 조합 후 `innerHTML`이다. **사용자 입력을 넣을 때는 반드시 `esc()`를 거친다.**

### 달력에 무엇을 띄우는가

- **달력 칸에는 행사만 띄운다.** 준비 건(`kind === "준비"`)은 칩으로 그리지 않는다.
  달력이 준비 건으로 가득 차서 정작 주요 업무가 안 보였기 때문이다
- 날짜 옆에는 **행사 건수와 준비 건수를 나눠서** 띄운다 (`행사 3` `준비 2`).
  준비만 있는 날도 `준비 1` 이 보이므로 빈 칸으로 오해할 일이 없다
- `+N건 더` 는 행사 기준으로 센다. 한 칸에 보여줄 개수는 `CAL_CHIPS`.
  **인쇄판은 감추지 않고 다 편다** (`cap: 0`)
- 칩은 두 줄이다 — 윗줄에 **시작–종료 시각**과 시설, 아랫줄에 **이용 목적**.
  아랫줄이 더 크다. 무슨 행사인지가 먼저 눈에 들어와야 한다.
  **목적을 안 적은 건은 신청 단체로 대신 띄운다** (빈 줄이 되면 안 되기 때문).
  두 값 모두 마우스를 올리면 나오는 설명(`title`)에 담는다
- **인원수는 인쇄할 때만, 그것도 고른 경우에만 붙는다** (`calCellsHtml` 의 `head` 옵션 ·
  인쇄 창의 `인원수` 선택창). 붙는 자리는 **아랫줄(목적) 끝**이다 — 윗줄은 한 줄로 자르므로
  거기에 넣으면 시설명이 잘린다. 인원을 적지 않은 건에는 나오지 않는다.
  **화면 달력에는 넣지 않는다** (기본값 `off` 도 지금까지와 같은 모양이다)
- **아랫줄은 자르지 않고 줄을 바꾼다** (`.chip>span.n`). 목적은 단체명보다 길 때가 많아
  한 줄로 자르면 무슨 행사인지 알 수 없다. 한글은 낱말 단위로 끊고
  (`word-break:keep-all`), 띄어쓰기 없이 긴 글자는 어디서든 끊는다.
  윗줄(시각·시설)은 길이가 정해져 있어 예전대로 한 줄로 자른다.
  36건에 긴 행사명을 넣어도 A4 가로 한 장에 들어가는 것을 확인했다
- **날짜를 누르면 우측 상세에는 행사와 준비가 모두 나온다.** 여기서 준비를 빼지 않는다
- **메모가 있으면 그 일정 바로 밑에 강조해서 붙인다** (`.item-memo`).
  담당자가 아닌 직원이 여기부터 본다. 이 표시를 빼지 않는다
- **달력과 우측 상세는 시설 차례로 줄 세운다** (`byFacility` — 충무 → 한밭 → 야구장,
  같은 시설 안에서는 시간순). 같은 체육관 일정이 모여 있어야 한눈에 들어온다는
  담당자 요청으로 시간순에서 바꿨다
- **목록·요금 관리·내보내기는 시간순이다** (`byTime`/`byDateTime`).
  대장은 시간 흐름대로 읽는 것이라 여기까지 바꾸지 않는다.
  분으로 비교하므로 `"9:00"` 처럼 앞의 0 이 빠져도 맞게 정렬된다
- 칩과 우측 상세 항목은 **시설별 구분색**을 입는다 (`data-fc` 속성)
- **그 날 할 일을 다 마쳤으면 칸 우측 아래에 빨간 ✓ 가 붙는다.** 날짜를 누르고 우측 상세의
  `확인` 을 누르면 켜지고, 다시 누르면 꺼진다 (`settings.doneDates`).
  예약 하나하나가 아니라 **날짜 단위**다 — 그 날 처리가 끝났는지를 보는 것이라서 그렇다.
  목록에도 맨 왼쪽 `완료` 열에 같은 ✓ 가 나온다
- 달력 칸은 `calCellsHtml()` 하나가 만든다. **화면과 인쇄가 같은 함수를 쓴다** —
  종이에 찍힌 것과 화면에 뜬 것이 달라지지 않게 하려는 것이다.
  화면은 `kinds:["행사"] · cap:CAL_CHIPS · live:true`, 인쇄는 `cap:0 · live:false` 다

**칸 바탕은 전부 흰색이다.** 체육관에 쉬는 날이 따로 없어서 주말을 회색으로 깔지 않는다.
이번 달이 아닌 앞뒤 달 날짜(`.cell.out`)만 회색으로 남겨 구분한다.
**주말 배경을 되살리지 않는다.**

토요일은 요일 글자와 날짜 숫자만 파랑(`--sat`), 일요일·공휴일은 빨강(`--sun`)이다.
칸 전체를 물들이지 않는다 — 색은 시설을 뜻하기 때문이다.

**상태도 취소도 쓰지 않는다.** 칩 테두리는 전부 실선이고, 상태 뱃지·파선·범례가 모두 없다.
등록창의 `상태` 칸과 목록의 거르기 선택창도 뺐다 — **되살리지 않는다.**
`status` 값은 자료에 남아 있고 새 예약은 늘 `"승인"` 이다. 예약을 무르려면 지운다.
예전 자료에 `취소` 가 남아 있으면 합계가 부풀지 않게 목록에서 빼되,
`취소 N건 숨김` 으로 알려서 조용히 사라지지 않게 한다.

### 공휴일

`isSurchargeDay()` 가 토·일·공휴일 가산을 판정하고, 공휴일 판정은 `holidayName()` 이 한다.

- **2040년까지 자동으로 잡힌다.** 신정·삼일절·어린이날·현충일·광복절·개천절·한글날·성탄절은
  `FIXED_HOLIDAYS`, 설날·부처님오신날·추석은 `LUNAR_HOLIDAYS` (한국천문연구원 음양력 자료로
  2026~2040년 solar 날짜를 미리 뽑아 넣었다). 값은
  `[설 전날, 설날, 설 다음날, 부처님오신날, 추석 전날, 추석, 추석 다음날]` 차례다
- **대체공휴일·임시공휴일은 미리 알 수 없다.** 달력에서 날짜를 누르고 우측 상세의
  `공휴일 지정` 을 누르면 `settings.holidays` 에 들어간다. 자동으로 잡힌 날을 빼면
  `settings.holidayOff` 에 들어간다. 설정 화면에서도 두 목록을 직접 고칠 수 있다
- 2041년 이후를 쓰게 되면 `LUNAR_HOLIDAYS` 에 줄을 더한다. 음력 계산을 코드로 넣지 않는다 —
  단일 파일 제약도 있고, 표가 더 정확하고 확인하기 쉽다

## 역할 — 담당자 / 요금 담당 / 조회

로그인한 계정에 따라 할 수 있는 일이 갈린다. `currentRole()` 이 판정한다.

| 역할 | 조건 | 할 수 있는 것 |
|---|---|---|
| `manager` 담당자 | 로그인했고 `feeManagers` 에 없는 계정 | 전부 |
| `fee` 요금 담당 | 로그인했고 `feeManagers` 에 있는 계정 | 요금 관리(입금 확인)만 |
| `viewer` | 로그인 안 함 | **금액이 없는 조회만** (아래 참고) |

- `settings.feeManagers` 는 이메일 목록이고, **담당자가 설정 화면에서 넣는다.**
  계정 자체는 Supabase 대시보드에서 따로 만든다 (Authentication → Users → Add user).
  만들고 관리하는 절차는 `요금담당계정안내.md` 에 적어 두었다
- **목록에 없는 로그인 계정은 담당자로 본다.** 계정을 만들고 목록에 넣는 것을
  빠뜨리면 그 계정이 전권을 갖는다 — 안내서에도 경고로 적어 두었다
**로그인하지 않은 사람(`viewer`)에게는 요금을 아예 그리지 않는다.**
`body.viewer` 로 화면에서 빼는 데 그치지 않고, **금액을 만들어 넣지 않는다** —
감추기만 하면 개발자도구로 들여다볼 수 있기 때문이다 (관리자 전용 칸과 같은 원칙).

| 어디 | viewer 에게 |
|---|---|
| `요금 관리` · `요금 계산` · `분석` · `시설 · 요금 기준` 탭 | 탭이 없고, 코드로 열어도 달력으로 되돌아간다 (`setPane` · `LOGIN_PANES`) |
| 요금 계산 결과 (`#qResult`) | **그리지 않는다** (`renderQuote` 첫 줄) |
| 분석 그래프·표 (`#stCharts`·`#stKpi`) | **그리지 않는다** (`renderStats` 첫 줄) |
| `AI 도우미` 단추 · `쉬운 말로 설명` | 화면에서 빼고, 눌러도 `openAi()` 가 막는다. 중계기도 `verify_jwt` 로 거부한다 |
| 요금 관리 표 · 시설 단가·부속시설 금액 | **그리지 않는다** (`renderFees`·`renderSettings` 첫 줄) |
| 달력 **부제**(`#calSubtitle`) | 건수만. **월간 이용료를 넣지 않는다** |
| 달력 요약 카드 | 건수만. 이용료·공과금·합계를 넣지 않는다 |
| 날짜별 상세 **인쇄** | 조회용 열로만 찍고, 날짜별 이용료를 넣지 않는다 |
| 우측 상세 | 금액·감면·`산정 상세`·`정산서` 체크·`수정`·`복제` 를 넣지 않는다 |
| 목록 | `조회용` 만 (금액 열이 없다). `수익집계용` 단추가 없고 눌러도 막힌다 |
| 정산서 | 단추·체크가 없고, `exportSettlement()` 가 막는다 |
| `+ 예약 등록` · `저장` · `확인` · `공휴일 지정` | 화면에서 뺀다 |

- `applyRole()` 이 `body.fee-only` · `body.no-admin` · `body.viewer` 를 붙여 고치는 UI 를 감춘다.
  **로그인 칸은 잠그지 않는다** — 잠그면 조회 전용에서 영영 빠져나오지 못한다
- **금액을 그리는 자리마다 `currentRole() !== "viewer"` 를 따로 확인한다.**
  `body.viewer` 로 감추는 것만으로는 개발자도구에 남는다. 지금 확인하는 곳은
  `renderSummary` · `renderCalendar`(부제) · `renderList` · `renderRail` · `renderFees` ·
  `renderQuote` · `renderStats` · `buildPrintDetail` 이다. **금액을 새로 띄우는 곳을 만들면 여기에도 더한다**
- **로그인해야 열리는 탭은 `LOGIN_PANES` 한 곳에 적는다** (`fees`·`settings`·`quote`·`stats`).
  `setPane` 과 `applyRole` 이 같은 목록을 쓴다 — 탭을 더하면서 한쪽만 고치면 구멍이 난다
- **인쇄도 화면과 같은 규칙을 쓴다.** 날짜별 상세 인쇄는 `view.listMode` 에 `income` 이
  남아 있어도 (앞사람이 고른 채로 로그아웃하면 남는다) 조회용 열로만 찍는다 —
  금액 열이 종이로 새어 나가면 안 된다

### 설정창 (`#settingsBox`)

로그인과 데이터 관리는 탭이 아니라 **윗줄의 `⚙ 설정` 창**에 모여 있다.
요금 기준(시설·단가, 가산·감산, 계산 방식)은 `시설 · 요금 기준` 탭에 그대로 두었다 —
**"왜 이 금액인지" 는 누구나 볼 수 있어야 하기 때문이다.** 다만 담당자가 아니면 고칠 수 없다.

그 탭은 세 묶음(**시설·단가 / 가산·감산·감면 / 계산 방식**)을 윗줄 전환 단추로
**한 번에 하나씩** 본다 (`#setSeg` · `applySetPane`). 셋을 한꺼번에 펼치면 스크롤이 너무 길어져서다.
**감추는 것이 아니라 전환일 뿐이다** — 조회하는 사람도 세 묶음을 다 볼 수 있다.
시설·부속시설·감면 목록은 `.setgrid` 로 옆으로 늘어놓는다 (세로로만 쌓으면 화면이 한없이 길어진다).
`.setgrid` 는 **`.sec-body` 뒤에 두어야 한다** — 앞에 두면 flex 로 되돌아간다.

| 칸 | 누가 보나 |
|---|---|
| 서버 연결 상태 · 로그인/로그아웃 | **누구나** (안 그러면 로그인할 방법이 없다) |
| Project URL · 키 (`#srvCfgRow`) | 담당자만 |
| 정산서 입금 계좌 · 요금 담당 계정 · 데이터 관리 (`#setAdminOnly`) | 담당자만 |

- **관리자 전용 칸은 감추는 게 아니라 화면에 넣지 않는다.** `<template id="adminTpl">`
  에 넣어 두고 담당자일 때만 꺼내 붙인다. `hidden` 으로만 두면 요금 담당 계정 이메일이
  화면 소스에 남아 조회하는 사람이 개발자도구로 들여다볼 수 있다
- 그래서 그 안의 단추에는 리스너를 직접 붙이지 않는다. 열 때마다 새로 만들어지므로
  **`#settingsBox` 에 한 번만 위임한다**
- 로그아웃하면 `#srvWho` 를 비운다. 안 비우면 다음 사람 화면에 앞사람 계정이 남는다
- 화면 잠금과 별개로 `currentRole() !== "manager"` 면 설정 입력 처리 자체를 막는다 —
  잠긴 칸을 코드로 건드려도 값이 바뀌지 않는다

**이 구분은 화면에서 가르는 것이지 서버가 막아 주는 것이 아니다.**
대장 전체가 `ledger` 한 행에 통째로 들어 있어서, RLS 로는 "이 사람이 입금 표시만
고쳤는지" 를 가려낼 수 없다. 실무상 역할을 나누고 실수를 줄이는 장치로 본다 —
**보안 경계로 설명하지 않는다.** 진짜로 막으려면 표를 쪼개고 열 단위 정책을 걸어야 하는데,
단일 파일·단일 행 구조를 버려야 해서 지금은 하지 않는다.

### 요금 관리 화면 (섹션 7-B)

돈만 보는 화면. 목록과 같은 열 정의(`LIST_COLS`)를 쓰되 `fee` 모드로 낸다.

열은 `입금 · 날짜 · 목적 · 시설 · 구분 · 신청 단체 · 감면 · 이용료 · 부속시설 · 전기료 · 수도료 · 합계`.
**요일·시간은 뺐다** — 돈을 맞추는 화면이라 시각이 필요 없고, 그 자리에 무슨 행사인지
알아볼 수 있게 이용 목적을 넣었다 (담당자 요청).

- **공과금을 전기료·수도료로 늘 나눠 보여준다** (목록의 전환과 달리 여기서는 항상 나뉜다)
- 줄마다 `산정 상세` 로 근거 조문까지 펼쳐 본다 — `feeLinesHtml()` 을 그대로 쓴다
- **입금은 건별로, 그리고 항목별로 확인한다.** 한 날에 여러 단체가 따로 내고,
  전기료·수도료는 행사가 끝난 뒤 실비로 따로 받기 때문이다 (별표 6 · 제11조제2항)
  | 표시 | 무엇의 입금인가 |
  |---|---|
  | `paid` (맨 왼쪽 `입금` 칸) | 이용료 = 전용사용료 + 부속시설 |
  | `paidPower` (전기료 칸 안의 체크) | 전기료 |
  | `paidWater` (수도료 칸 안의 체크) | 수도료 |
- 금액이 0원인 항목에는 체크를 두지 않는다 — 받을 것이 없기 때문이다
- `fullyPaid(r)` 가 그 건에서 받을 것을 다 받았는지 판정한다 (0원 항목은 뺀다).
  줄 색·거르기(`미입금만`/`입금 완료만`)·날짜 단위 안내가 모두 이 값을 쓴다
- `paidAmount(r)` 는 실제로 들어온 금액이다. 위쪽 요약의 입금·미입금 금액이 이것을 쓴다
- 날짜 단위로 다 받았는지는 화면 아래 안내에 함께 띄운다
- 입금 표시는 돈에 관한 것이라 `saveNow(true)` 로 바로 올린다.
  올린 것은 다른 사람 화면에 10초 안에 나타난다 (`serverPoll` → `renderAll` → `renderFees`)
- **올리기 직전에 남의 입금 표시와 합친다** (`mergeRemotePayments`).
  대장 전체가 한 행이라 화면에 있는 자료를 그냥 올리면, 그 사이 다른 사람이 켠 체크가
  통째로 지워진다 — 요금 담당과 담당자가 동시에 쓰는 화면이라 실제로 일어난다.
  **내가 이번에 직접 켜고 끈 것(`PAY_EDITS`)만 내 값을 지키고, 나머지는 서버를 따른다.**
  `updated_at` 이 그대로면 아무것도 받아오지 않는다 (전송량을 아낀다)
- 예약을 수정해도 `paid` 는 그대로 둔다 (`formToRes` 가 이전 값을 물려준다)

### 요금 계산 화면 (섹션 7-C)

**「토요일 저녁에 쓰면 얼마예요?」 전화에 담당자가 바로 답하려고 만든 화면.**
조건을 고르면 그 자리에서 금액과 근거 조문이 나온다. 윗줄 `요금 계산` 탭이다.

- **저장하지 않는다.** `quoteRes()` 가 **가짜 예약 객체**를 하나 만들어 `calcFee()` 에 넣을 뿐이다.
  `state` 를 건드리지 않고 `save()` 도 부르지 않는다
- **예약과 같은 함수를 쓴다** (`calcFee` · `feeLinesHtml`). 그래서 이 화면의 금액과
  대장의 금액이 **어긋날 수가 없다.** 계산을 여기에 따로 구현하지 않는다
- 고르는 것은 **시설 · 날짜 · 건 구분 · 시작·종료 시각 · 이용 구분 · 감면 유형** 여섯 가지다
- 날짜 밑에 **요일과 공휴일 이름**을 적는다 (`추석` 처럼). 왜 비싼지가 여기서 드러난다
- 야구장을 고르면 `준비` 가 막힌다 — 등록창(`updateKindUI`)과 같은 규칙이다
- 종료가 시작보다 빠르거나 기본액이 비어 있으면 금액 옆에 `--alert` 로 알린다
- 무엇을 바꾸든 바로 다시 계산한다 (`#qForm` 의 `change` 하나에 위임)

**부속시설·전기료·수도료는 넣지 않는다.** 대관 문의에서 먼저 나오는 것은 전용사용료이고,
부속시설까지 고르게 하면 이 화면이 등록창만큼 길어진다.
**대신 화면 아래에 그렇게 적어 둔다** — 빠진 금액이 있다는 것을 숨기면 안 된다.

**로그인한 사람만 쓴다.** 탭을 감추는 데 그치지 않고 `renderQuote()` 첫 줄이
`currentRole() === "viewer"` 면 **금액을 그리지 않는다** (`renderFees` 와 같은 원칙).

#### 「요금 기준」 과 「산정 결과」 는 다르다

비로그인에게 금액을 감추는 결정(2026-08-27)을 이야기할 때 이 둘을 섞지 않는다.

| | 무엇인가 | |
|---|---|---|
| **요금 기준** | 별표 2 단가 · 별표 1의2 시간대 · 제13조 감면율 | **조례다.** 자치법규정보시스템에 이미 공개돼 있다 |
| **산정 결과** | "○○협회가 9월 5일 건으로 20만 2,500원 냈다" | **거래 정보다.** 누가 얼마를 냈나 |

2026-08-27 결정이 실제로 지키는 것은 **뒤쪽**이다 — 달력 부제의 월간 이용료, 요약 카드의 수입,
목록 수익집계용의 단체별 금액, 요금 관리의 입금 현황.

요금 계산 화면은 앞쪽에 가깝지만 **지금은 로그인 전용으로 둔다** (2026-09-21 담당자 결정).
시민에게 열기로 정해지면 `renderQuote()` 첫 줄과 `LOGIN_PANES` 에서 `quote` 를 빼면 된다.
**그 결정은 담당자가 한다 — 코드에서 임의로 열지 않는다.**

### 분석 화면 (섹션 7-E)

시설별 **대관 횟수 · 수익금 · 인원**의 달별 추이를 그래프로 본다. 윗줄 `분석` 탭이다.

**수익금은 전기료·수도료를 뺀 금액이다** (2026-09-21 담당자 요청) —
`feeParts()` 의 `use`(전용사용료) + `annex`(부속시설). 공과금은 실비로 받는 것이라 수익이 아니다.
**이 정의를 바꾸지 않는다.** 화면 아래에도 그렇게 적어 두었다.

**시설마다 그래프를 따로 둔다.** 세 시설 색(주황·파랑·초록)을 한 그래프에 겹치면
**적록색약에서 주황과 초록이 구분되지 않는다** (검사값 ΔE 3.6, 안전선 6~8 아래).
색을 바꾸면 달력 칩·목록과 어긋나므로, 그래프를 나눠 **색이 구분 역할을 하지 않아도 되게** 했다.
**한 그래프에 세 시설을 겹치도록 되돌리지 않는다.**

- 대신 **어느 그래프에 올려도 그 달의 세 시설 값이 함께 뜬다** (`stHover`) —
  그래프를 나눠서 잃은 비교를 여기서 돌려준다
- **세 그래프의 세로 눈금은 같다.** 제각각이면 높이를 견줄 수 없다
- 색은 달력과 같은 시설 구분색 그대로다 (`data-fc` → `--fc-ink`). **새 색을 만들지 않았다**
- 그래프 규격 — 선 2px(`vector-effect` 로 늘어나도 2px 유지) · 점 지름 8px 에 바탕색 테두리 ·
  눈금선은 **실선 hairline** 으로 흐리게 · 면은 10% 로 옅게
- **값은 끝점에만 적는다.** 점마다 숫자를 붙이면 읽히지 않는다.
  그 딱지는 선·면 위에 겹쳐 앉으므로 **바탕색 테두리를 둘러 글자가 묻히지 않게 한다**
  (`.st-card .vl` 의 `paint-order:stroke` — 점 `.dt` 에 두른 것과 같은 방식이다)
- 달 이름은 솎아서 찍되 **마지막 달과 붙으면 앞의 것을 뺀다** (`11월 12월` 이 겹쳐 찍혔다)
- **`ST_GEO` 에 크기·여백을 한 곳에 적는다** — `stChart`(그리기)와 `stHover`(어느 달인지 찾기)가
  따로 적어 두면 크로스헤어가 밀린다. 실제로 그랬다
- 지표는 세그먼트로 **한 번에 하나씩** 보되, 위의 요약 카드는 **세 지표를 한꺼번에** 띄운다
- **표를 함께 낸다** (월 × 시설 + 합계). 마우스를 올리지 않아도 모든 값에 닿을 수 있어야 한다
- 빈 달도 0 으로 채운다 — 빠뜨리면 추이가 거짓말이 된다. 취소된 건은 세지 않는다
- **세로 눈금은 「딱 떨어지는 값」으로 잡는다** (`niceScale`). 높이를 그냥 넷으로 나누면
  최고 4건짜리 그래프에 `0 1 3 4 5` 가 찍혀 2 가 빠져 보인다 — 건수·인원은 정수라
  소수 눈금이 반올림되며 겹치기 때문이다. 간격(1·2·2.5·5·10×10ⁿ)을 먼저 고른다

#### 「한눈에」 — 세 시설을 한 판에 (2026-09-21 담당자 요청)

`시설별` 과 `한눈에` 를 윗줄에서 고른다 (`view.statView`). 기본은 `시설별` 이다.

겹쳐 그리면 **적록색약에서 주황과 초록이 갈리지 않는다는 문제가 그대로 살아 있다.**
그래서 색을 구분의 주인 자리에서 내리고 **세 가지를 함께 입혔다.**

| 무엇 | 충무 | 한밭체육관 | 한밭야구장 |
|---|---|---|---|
| 선 모양 (`ST_SHAPES`) | 실선 | 긴 점선 | 짧은 점선 |
| 점 모양 (`stMark`) | ● 원 | ■ 네모 | ▲ 세모 |
| 선 끝 | 시설 이름을 그대로 적는다 | 〃 | 〃 |

- **이 셋을 빼지 않는다.** 하나만 봐도 구분되게 하려는 것이고, 색은 거들 뿐이다
- 범례에도 같은 선·점 모양을 그린다 (색 동그라미만 두지 않는다)
- 선 끝 이름은 **겹치면 아래로 민다.** 위로 밀면 값이 낮은 시설 이름이 위에 붙어 거꾸로 읽힌다
- 그래프 판이 넓어(viewBox 720) 같은 글자·점 크기가 시설별 판(320)보다 크게 그려진다.
  `.st-card.wide` 에서 0.7 배쯤 줄여 화면에서 비슷하게 보이게 했다
- **판마다 기하값이 다르므로 `data-geo` 에 그린 쪽이 적어 둔다** — `stHover` 가 그것을 읽는다.
  숫자를 두 곳에 적으면 크로스헤어가 밀린다
- 범례에도 `<svg>` 가 있어서 `card.querySelector("svg")` 로는 범례가 잡힌다.
  **그래프 판은 `svg.st-plot` 으로 집어야 한다** (실제로 크로스헤어가 어긋났다)

#### 표 (`.st-wrap`)

격자 대신 **여백과 굵기로 읽는다** (담당자 요청: 「애플 스타일」).

- 세로줄을 긋지 않고 가로 실선 하나만 흐리게 둔다. 머리글에 바탕색을 깔지 않는다
- 달은 `2026-01` 이 아니라 **`2026년 1월`** 로 적는다 (`stYm`) — 읽는 표이지 자료 파일이 아니다
- 칸 뒤에 **아주 옅은 막대**를 깐다 (`td.bar` · `--p`). 시설 구분색의 가장 옅은 벌(`--fc-soft`)만
  써서 글자를 가리지 않는다. 길이는 **그 지표의 최고값** 기준이다
- 0 인 달은 `0` 대신 `–` 로 흐리게 둔다 (0 이 줄줄이 찍히면 읽기 나쁘다)
- **줄에 마우스를 올릴 때 `background-color` 만 바꾼다** — `background` 를 통째로 주면 막대가 지워진다
- **새 색을 만들지 않았다.** 기존 토큰을 여백·굵기만 다르게 쓴 것이다

### 엑셀 가져오기 (섹션 9-E)

이 시스템을 쓰기 전에 엑셀로 적어 두던 대장을 올린다 (2026-09-21 담당자 요청).
**⚙ 설정 → 데이터 관리 → 엑셀·CSV 로 예약 가져오기.** 담당자만 쓴다.

**양식을 요구하지 않는다.** 부서마다·해마다 열이 다르기 때문에, 정해진 서식을 내려주는 대신
**열을 맞춰 보여주고 확인받는** 방식으로 간다.

1. 파일을 읽는다 — `.xlsx`(ZIP+XML) 와 `.csv`. **라이브러리는 쓰지 않는다.**
   압축 해제는 브라우저에 있는 `DecompressionStream("deflate-raw")` 를, XML 은 `DOMParser` 를 쓴다
2. 머리글 줄을 찾고(`impFindHead`), **열 이름으로** 항목을 짐작한다 (`impGuess`)
3. 이름으로 못 찾은 것은 **값을 보고** 찾는다 (`impGuessByValue`) —
   머리글이 「월일」 처럼 우리가 모르는 말이어도 날짜·시설은 값 모양으로 알아본다
4. **담당자가 미리보기를 보고 고친다.** 들어갈 건·건너뛸 건이 이유와 함께 나온다
5. 확인을 눌러야 들어간다. 그때 `saveNow(true)` 로 바로 올린다

**지키는 것 — 이 다섯을 빼지 않는다.**

1. **연락처·성명 열은 목록에 올리지도 않는다** (`IMP_BLOCK`). 고를 수 있게 두면 언젠가 들어온다.
   무엇을 뺐는지는 화면에 「가져오지 않는 열」 로 적어 알린다
2. **어느 칸이든 전화번호·주민번호 형태는 `aiSafe()` 로 지우고 담는다** (AI 로 보낼 때와 같은 그물)
3. **금액은 조례대로 다시 계산하는 것이 기본이다.** 엑셀 금액을 쓰려면
   `직접 입력 금액`(`feeOverride`) 으로만 들어간다 — 계산을 덮어쓰지 않는다.
   **엑셀 금액과 조례 계산이 몇 건 다른지 미리 알려 준다** (`#impFeeNote`) —
   「왜 이 금액인지」 가 이 도구의 존재 이유라 차이를 숨기지 않는다
4. **이미 있는 건은 건너뛴다** (날짜·시설·시각·단체가 같으면 같은 건으로 본다).
   여러 해치를 나눠 올리다 겹쳐 올리는 일이 실제로 생긴다
5. **확인을 누르기 전에는 `state` 를 건드리지 않는다.** 미리보기는 계산만 한다

- 시설 이름은 `impFacility()` 가 붙인다. `한밭체육관` 과 `한밭야구장` 은 앞 두 글자가 같아서
  **종류 낱말(체육관·야구)까지 맞아야** 본다. 점수가 같으면 아예 고르지 않는다
- 못 알아본 이름은 **「시설 이름 맞추기」 에 모아** 담당자가 고르게 한다 (`IMP.fac`).
  「제1경기장」 처럼 우리가 모르는 이름이 섞이는 것이 오히려 흔하다
- 감면은 **비율로** 맞춘다 (`impDiscount`). 같은 id 라도 목록이 바뀌면 뜻이 달라진다
  (`migrate` 에서 겪은 그 문제와 같다).
  「없음」 류는 **칸 전체가 그 말일 때만** 없음으로 본다 — 부분으로 찾으면 「80%」 안의 `0%` 가 걸린다
- 「구분」 한 칸에 `체육경기 / 체육경기 외 / 준비` 를 섞어 적는 양식이 많아,
  건 구분을 따로 고르지 않았으면 **이용 구분 칸도 함께 본다**
- 날짜는 `2024-03-05`·`2024.3.5`·`24/3/5`·`3.5`·`20240305`·엑셀 일련번호를 모두 받는다.
  연도가 없는 `3.5` 는 **기준 연도**를 쓴다 — 시트 이름에 `2019` 가 있으면 그것을 미리 넣어 둔다
- 시간은 `09:00~18:00` 한 칸도, 시작·종료 두 칸도 받는다 (`impRange`)
- CSV 는 UTF-8 로 먼저 읽고 실패하면 **euc-kr 로 다시 읽는다** (한글 CSV 는 cp949 가 많다)
- **`.xls`(옛 이진 형식)는 읽지 않는다.** 엑셀에서 `.xlsx` 로 저장해 올리라고 안내한다

**AI 로 열 맞추기 — 거들 뿐이다**

열 이름이 우리가 아는 낱말과 전혀 다를 때를 위한 **보조 수단**이다 (`impAiMap` ·
중계기의 `mode:"map"`). AI 가 켜져 있을 때만 단추가 보인다.

- **AI 는 「어느 열이 무엇인지」 번호만 짚는다.** 값을 고치거나 금액을 만들지 않는다
- 받은 값을 **그대로 믿지 않는다** — 아는 항목·있는 열 번호만 받아들인다
- 결과는 곧바로 들어가지 않고 **미리보기에 채워져** 담당자가 확인한다
- 보내는 것은 머리글과 **표본 세 줄**뿐이고, 개인정보 열은 빼고 `aiSafe()` 를 거쳐 나간다
- 중계기에 `mode:"map"` 이 없으면(옛 함수) 그냥 실패하고 손으로 맞추면 된다 —
  **대장은 그대로 돈다**

### AI 도우미 (섹션 7-D · `supabase/functions/ask`)

조례와 **지금 설정된 요금 기준**을 보고 답하는 창. 윗줄 `AI 도우미` 단추로 연다.
우측 상세의 `쉬운 말로 설명` 은 그 건의 산정 내역을 들고 같은 창을 연다.

설치 절차는 `AI설치안내.md`. 어느 모델을 왜 골랐는지는 `AI챗봇모델조사.md`.

**넘지 않는 선 — 이 넷을 빼지 않는다.**

1. **AI 에게 금액을 계산시키지 않는다.** 계산은 `calcFee()` 가 한다.
   AI 에게는 **이미 나온 숫자와 근거 조문**을 주고 풀어 쓰게만 한다.
   금액을 물으면 계산하지 말고 「요금 계산」 탭으로 안내하도록 함수의 시스템 프롬프트에 적어 두었다
2. **메모는 기본으로 보내지 않고, 연락처 형태는 지우고 보낸다** (2026-09-21 담당자 결정 —
   아래 「무엇을 보내나」 절). 단체명·이용 목적은 개인정보가 아니므로 보낸다
3. **로그인한 사람만 쓴다.** 화면에서 감추는 데 그치지 않고
   Edge Function 의 `verify_jwt` 가 **서버에서** 막는다
4. **AI 응답을 자료에 남기지 않는다.** `AI.msgs` 는 `state` 밖이고 `save()` 를 부르지 않는다.
   창을 닫으면 사라진다

#### 무엇을 보내나 (2026-09-21 담당자 결정)

처음에는 요금 기준만 보냈더니 **「10월 태권도협회」 같은 질문에 답하지 못했다.**
담당자만 쓰는 창이므로 **대장에 적힌 예약 내역을 금액까지 함께 보낸다** (`aiResContext`).

| 보낸다 | 안 보낸다 |
|---|---|
| 요금 기준 (단가·시간대·가산·감면·부속시설과 근거 조문) | **메모** — 창에서 켜야 들어간다 |
| 예약 내역 — 날짜·요일·시각·시설·구분·**신청 단체·목적**·인원·감면·**금액·입금** | 전화번호·주민번호 **형태의 글자** (`aiSafe`) |

- **단체명·이용 목적은 개인정보가 아니다.** 「개인정보를 넣지 않는다」 절이 그렇게 정해 둔
  구조가 여기서 값을 한다 — 보낼 개인정보가 애초에 없다
- **메모만 위험하다.** 「김○○ 주무관 010-… 확인」 이 적히는 칸이라 **기본으로 뺀다.**
  담당자가 창의 `메모도 보내기` 를 켜야 들어간다. **이 기본값을 켜짐으로 바꾸지 않는다**
- `aiSafe()` 가 **어느 칸이든** 전화번호·주민번호 패턴을 지운다 (`[연락처 지움]`).
  주민번호를 먼저 지운다 — 전화번호 패턴이 그 일부를 먼저 물어가지 않게
- 줄마다 키를 되풀이하지 않게 **「열 + 줄」** 로 넣는다. 같은 내용이 훨씬 짧다
- **범위를 함께 적는다** (`범위`·`건수`). 자료 밖을 「없습니다」 라고 단정하지 않게
  함수의 시스템 프롬프트가 막는다. 창에서 `전체 기간 / 올해 / 이 달` 을 고른다
- 많으면 `AI_ROW_CAP`(500) 만 보내되 **자른 사실을 자료와 화면에 함께 적는다**
- **월별 합계를 미리 더해서 넣는다** — AI 가 산수를 하지 않아도 되게 하려는 것이다
- **무엇을 보내는지 창에 적는다** (`aiDataNote`). 보이지 않게 나가는 자료가 없어야 한다

**금액에 대해서는 선이 하나 옮겨졌다.** AI 는 **이미 나온 금액을 인용**하고,
여러 건을 합칠 때는 **각 건을 나열한 뒤** 합계를 내고 「목록 → 수익집계용」 과 대조하라고 덧붙인다.
**조례의 가산·감산을 적용해 요금을 산출하는 것은 여전히 금지다** — 그건 `calcFee()` 가 한다.

**키는 브라우저에 없다.**

- 키는 **Supabase Edge Function Secrets** 에만 있다 (`AI_API_KEY`). 저장소에도 화면에도 없다
- **설정 화면이나 `ledger` 표에 키 입력칸을 만들지 않는다** — 이 대장은 로그인 없이도 조회가
  되므로, 설정에 넣으면 누구나 `state` 를 받아 읽을 수 있다. **공개와 같다**
- `.gitignore` 가 `.env` 류를 막는다. 이 줄들을 지우지 않는다

**업체를 갈아끼울 수 있게 만들어 두었다.**

`index.html` 은 업체를 모른다 — `/functions/v1/ask` 만 부른다.
모델·주소·요청 형식은 전부 함수의 환경변수다 (`AI_MODEL`·`AI_BASE_URL`·`AI_STYLE`·`AI_EFFORT`).
**업체가 바뀌어도 `index.html` 은 고치지 않는다.**

- 기본값은 OpenAI `gpt-5.6-luna` (`AI_STYLE=responses`)
- 응답을 **여러 형식으로 받아낸다** (`pickText`) — Responses API 의 `output[]`,
  `output_text`, Chat Completions 의 `choices[].message.content` 를 모두 훑는다.
  업체마다 모양이 달라서 그렇다. **이 관용 처리를 하나로 줄이지 않는다**

**꺼져 있어도 대장은 그대로 돈다.**

`aiSync()` 가 로그인 상태가 바뀔 때만 중계기에 `mode:"ping"` 을 한 번 보낸다.
실패하면 `AI.ready = false` 가 되고 **단추를 아예 띄우지 않는다.**
키가 없든 요금이 끊기든 망이 막히든, 달력·계산·저장은 지금과 똑같이 된다.

- 확인은 **토큰이 바뀔 때만** 한다 (`AI_PROBED`). `applyRole()` 이 매번 불러도 평소에는 아무 일도 안 한다
- 오류는 **창 안에 붉은 말풍선으로 그대로 띄운다** — 무엇을 고쳐야 할지 알 수 있어야 한다

**프롬프트 인젝션.** 함수가 브라우저에서 받은 자료를 `<자료>` 로 감싸고,
**그 안의 글은 지시가 아니라 자료로만 본다**고 시스템 프롬프트에 적어 두었다.
규칙은 **함수 안에만** 있다 — 브라우저가 보내는 값으로 바꿀 수 없다.
보내는 양에도 상한을 두었다 (질문 2,000자 · 기록 12개 · 자료 24,000자).

#### 표로 그리고, 답마다 엑셀로 낸다 (2026-09-21 담당자 요청)

AI 가 마크다운 표로 답하는데 글자로 그리면 읽을 수가 없었다. **진짜 표로 그린다.**

- `aiBlocks()` 가 답을 **글 묶음과 표 묶음**으로 가른다. 머리글 바로 밑의 `|---|---:|` 줄이
  표라는 표시다. `:---:`·`---:` 로 적힌 정렬도 따라간다 (금액이 오른쪽으로 붙는다)
- `aiHtml()` 이 그린다. **표 구조는 우리가 만들고 칸 내용은 전부 `esc()` 를 거친다** —
  업체가 `<img onerror=…>` 를 돌려줘도 글자로 남는다. 살리는 서식은 **`**굵게**` 하나뿐**이다.
  링크·그림은 살리지 않는다 (살릴 이유가 없고 위험만 는다)
- 칸은 `white-space:normal` + `word-break:keep-all` 이다. 날짜·시각처럼 띄어쓰기 없는 값은
  저절로 한 줄로 남고 **긴 행사명만 낱말 단위로 접힌다.** 전부 `nowrap` 이면 오른쪽이 잘린다
- 표가 든 답은 말풍선 폭을 다 쓴다 (`.ai-msg.has-tbl`). 창도 880px 로 넓혔다

**엑셀은 답 하나씩 낸다** (`aiExportXlsx`). **창 전체를 한꺼번에 내지 않는다** — 담당자 요청이다.

- 표가 든 답에만 `⤓ 엑셀` 단추가 붙는다. 누른 **그 답의 표만** 나간다
- 목록 내보내기가 쓰는 `xwNew`·`xwStyle`·`xwPut`·`xwXlsx` 를 그대로 쓴다. 라이브러리는 없다
- **`202,500`·`202,500원` 은 숫자로 넣는다** — 글자로 넣으면 엑셀에서 합계를 못 내고
  초록 세모가 붙는다. `300명`·`07:00–18:00`·`2026-10-02` 는 글자 그대로 둔다
  (날짜를 숫자로 바꾸면 `46260` 이 된다 — 정산서에서 겪은 그 문제다)
- 첫 줄에 제목, 둘째 줄에 **그 답을 부른 질문**을 적는다. 나중에 무엇을 뽑은 파일인지 알 수 있게
- **`wb.page` 를 반드시 둔다** (가로·한 폭). 안 두면 정산서 쪽 기본값이 딸려온다

**답은 `esc()` 를 거쳐 글자로만 넣는다.** AI 가 HTML 을 돌려줘도 그대로 그려지지 않는다.

## 데이터 모델

`state` 하나에 전부 들어 있다.

```js
{
  version: 9,
  settings: {
    rounding,          // 절사 단위(원)
    // 별표 2 비고 — 모두 "기본액" 기준이다
    weekendRate,       // 토·공휴일(주간) 가산 % (50)
    nightRate,         // 야간사용 가산 %       (50)
    earlyRate,         // 조기사용 감산 %       (30)
    overRate,          // 초과 1시간당 가산 %   (20)  제11조제3항
    sundayAsHoliday,   // 일요일을 공휴일로 볼지 (기본 true)
    surchargeMode,     // "multiply" 당일 주간 × 1.5 / × 0.7 (기존 실무, 기본값)
                       // "add"      당일 주간 ± 기본액의 50% / 30% (조례 문언)
    // 별표 1의2 — 일일 사용시간
    hours: {
      summerFrom, summerTo,                      // 하계 4~9월
      summer: { early, day, night, close },      // "05:00" "08:00" "19:00" "23:00"
      winter: { early, day, night, close }       // "06:00" "09:00" "18:00" "23:00"
    },
    // 제12조 — 관람료 수입에 따른 사용료.
    //   등록창에서 관람료를 받지 않으므로 새 예약에는 쓰이지 않는다 (아래 「관람료」 절).
    //   정산서 ① 칸의 딱지와 예전 자료의 계산에만 남아 있다
    admSportsRate,     // 체육경기 %        (10)
    admOtherRate,      // 프로·일반행사 %   (12.5)
    admSchoolCut,      // 고교 이하 할인 %  (20)
    // 별표 6 — 부속시설 사용료
    annexItems: [{ id, name, fee, unit, article,
                   base, baseName, baseArticle }],
                       //   unit: "1회" | "1일" | "개" | "명"  (ANNEX_UNITS)
                       //     「개」·「명」 은 수량을 센다 (단가 × 수량) — `annexCounts()`
                       //     「1회」·「1일」 은 한 번 쓰면 그만이라 1 을 넣는다
                       //   base 기본료(원) — 수량이 하나라도 있으면 저절로 붙는다 (횟수만큼).
                       //   마이크가 그렇다. 0 이거나 없으면 붙지 않는다
                       //   차례는 `ANNEX_ORDER` 가 정한다 — 설정·등록창·정산서가 같은 차례다
    prepSurcharge,     // 준비시간에도 가산·감산을 적용할지 (기본 false)
    articles: { base, weekend, night, early, over, hours, adm, admCut, discount },
    holidays: [],      // 손으로 넣은 공휴일 (대체공휴일·임시공휴일). 법정공휴일과 토·일은 자동
    holidayOff: [],    // 자동으로 잡힌 공휴일 중 손으로 뺀 날
    doneDates: [],     // 그 날 할 일을 다 마쳤다고 표시한 날짜 ("확인"). 달력·목록에 ✓
    feeManagers: [],   // 요금 담당 전용 계정의 이메일. 위 「역할」 절 참고
    // 행사 정산서(9-D) 에 찍히는 값. 「⚙ 설정」 창에서 담당자만 고친다
    settle: { useBank, useAcct,     // 시설사용료 받는 계좌 (「은행:계좌번호」 로 찍힌다)
              utilBank, utilAcct,   // 공과금 받는 계좌 (따로 받는다)
              officers: [],         // 대관담당자 이름 목록 (여러 명이면 여기에 다 적는다)
              officer,              // 그중 이번 정산서에 넣을 사람
              stamp },              // 올린 통제 도장 그림 { b64, mime, ext, name }.
                                    //   없으면 한글 원본대로 그린다 (stampCanvas)
    orgNote
  },
  facilities: [{
    id, name,
    color,             // 달력 구분색: "orange" | "blue" | "green" | "none"
    feeSports,         // 체육경기 기본 전용사용료 (원/1회)      별표 2
    feeOther,          // 체육경기 외 기본 전용사용료 (원/1회)   별표 2
    sessionHours,      // 1회 기준 시간. 0이면 제한 없음 → 초과사용료 없음
    capacity, article, // 정원, 근거 조문
    prep: {
      allowed,         // 준비시간을 받는 시설인지 (야구장은 false)
      mode,            // "fee" 별도단가 | "percent" 본단가의 % | "free" 무료
      fee, percent, article
    }
  }],
  discounts: [{ id, name, rate, article }],   // 제13조. [0]번은 "해당 없음", 삭제 불가
  reservations: [{
    id,
    kind,              // "행사" | "준비"
    linkId,            // 예전 자료에서 행사에 딸려 만들어진 준비 건의 부모 id.
                       //   지금은 새로 만들지 않는다 (준비는 '건 구분: 준비' 로 따로 등록).
                       //   남아 있는 연결은 살려 둔다 — 행사를 지우면 함께 지워진다
    date,              // "2026-08-11"
    start, end,        // "09:00"
    facilityId,
    status,            // 신청 | 승인 | 취소. 화면에는 쓰지 않고 취소를 걸러내는 데만 쓴다.
                       //   새 예약의 기본값은 "승인" 이다
    useType,           // "체육경기" | "체육경기 외"  — 별표 2 의 어느 금액을 쓸지 가른다
    admission,         // 관람료 수입액(원). null이면 관람료 없는 행사.
                       //   **등록창에서 받지 않는다** — 예전 자료의 값만 남아 계산에 쓰인다
    schoolGame,        // 고등학교 이하의 경기인지 (제12조제3항 20% 할인). 〃
    annex,             // 부속시설 수량 { 항목id: 수량 }. 없으면 null      별표 6
                       //   「개」·「명」 이면 개수, 「1회」·「1일」 이면 횟수다
    annexTimes,        // 「개」·「명」 항목의 **횟수** { 항목id: 회수 }. 없으면 null(=1회).
                       //   금액 = 단가 × 수량 × 횟수 — `annexUse()` 가 둘을 함께 돌려준다
    power, water,      // 전기료·수도료 실비(원). 직접 입력, 자동 계산 안 함
    applicant,         // 신청 단체명
    headcount, purpose, discountId, memo,
    // 입금 확인. 요금 관리 화면에서만 켠다 (예약을 고쳐도 그대로 남는다)
    paid,              //   이용료(전용사용료+부속시설) 입금
    paidPower,         //   전기료 입금   — 행사 뒤 실비로 따로 받는다
    paidWater,         //   수도료 입금   — 〃
    feeOverride        // 직접 입력한 금액. null이면 자동 산정값 사용
  }]
}
```

### 개인정보를 넣지 않는다

연락처 항목은 **의도적으로 제거했다.** 신청 단체명만 기록하면 개인정보 보호법상
개인정보(제2조제1호)에 해당하지 않아, 이 도구 전체가 개인정보 처리에서 벗어난다.

- 성명·전화번호·주소·생년월일 입력란을 **다시 추가하지 않는다**
- `migrate()`가 예전 자료에 남은 `contact` 값을 지운다. 이 로직을 없애지 않는다
- 신청 단체 입력란의 "개인 성명·연락처는 적지 않습니다" 안내를 유지한다

## 이용료 계산 (섹션 3)

**근거: 대전광역시 체육시설 관리운영 조례 [시행 2024.12.27. 조례 제6358호]**
조례를 코드로 옮겨놓은 곳. 여기를 고칠 때가 가장 많다.

### 조례가 정하는 구조

기본 전용사용료는 **1회 금액**이다. 시간당 단가가 아니다 — 3시간을 쓰든 5시간을 쓰든
기본액은 같고, 시설에 `sessionHours`(1회 기준 시간)가 정해진 경우에만 초과분이 붙는다.
**"기본료 + 시간당 단가" 구조로 되돌리지 않는다.**

```
① 기본 전용사용료   별표 2. 체육경기 / 체육경기 외로 갈린다 (1회)
② 토·공휴일 가산    + 기본액 × 50%        별표 2 비고
③ 야간 가산         + 기본액 × 50%        별표 2 비고 (②까지의 금액에 더한다)
   조기 감산         − 기본액 × 30%        별표 2 비고
④ 초과사용료        + 기본액 × 20% × 초과시간   제11조제3항 (1시간 미만은 1시간)
⑤ 관람료 수입 사용료 + 수입 × 10%(체육경기) 또는 12.5%(프로·일반)   제12조제2항
                     고등학교 이하 경기는 그 금액의 20% 할인        제12조제3항
                     ※ 등록창에서 관람료를 받지 않으므로 새 예약에는 0 이다
⑥ 부속시설 사용료    + 기본료(있으면 횟수만큼) + 단가 × 수량 × 횟수   별표 6
⑦ 전기료 (실비)      + 직접 입력               별표 6
⑧ 수도료 (실비)      + 직접 입력               별표 6
⑨ 감면              − (①~⑤ + ⑥) × 감면율    제13조
                     ※ **⑦ 전기료·⑧ 수도료만** 감면 대상이 아니다 (설정으로 끌 수 없다)
합계 = (①~⑧ − ⑨) 을 절사 단위로 내림
```

### 야간·조기는 「당일 주간 전용사용료」에서 출발한다

`당일 주간 전용사용료` = 기본액 + (토·일·공휴일이면 기본액 × 50%).
야간·조기는 이 금액을 출발점으로 삼는다. 산정 내역에 `＝ 당일 주간 전용사용료` 줄로
중간값을 띄운다 — **이 줄을 빼지 않는다.**

출발점에 무엇을 곱하거나 더하는지가 두 가지로 읽혀서, `surchargeMode` 로 고르게 했다.

| | multiply (기존 실무·기본값) | add (조례 문언 그대로) |
|---|---|---|
| 야간 | 당일 주간 × 1.5 | 당일 주간 + 기본액 × 50% |
| 조기 | 당일 주간 × 0.7 | 당일 주간 − 기본액 × 30% |

**평일은 두 방식이 같고, 토·일·공휴일에서만 갈린다.**

| 충무·한밭 체육경기 | 당일 주간 | 야간 | 조기 |
|---|---|---|---|
| 평일 | 90,000 | **135,000** | **63,000** |
| 토·일·공휴일 (multiply) | 135,000 | **202,500** | **94,500** |
| 토·일·공휴일 (add) | 135,000 | 180,000 | 108,000 |

**기본값 `multiply` 는 부서가 지금까지 써 온 계산이다.** 담당자가 쓰던 엑셀
(한밭/충무·야구장 × 체육경기/외 × 조기·주간·야간 × 평일/휴일 24칸 + 감면 6칸)의
값과 전부 맞는 것을 확인하고 정했다. 조례 문언("기본 전용사용료의 100분의 50을 가산")만
보면 `add` 로 읽히지만, 실무를 바꾸지 않는 쪽을 기본으로 두었다. 시 해석을 확인해야 한다.

토·일·공휴일 가산은 야간·조기와 **함께 붙는다.** 야간이라고 해서 빠지지 않는다 —
야간 사용료가 "당일 주간 전용사용료"를 출발점으로 삼기 때문이다.

절사할 때 `+1e-9` 을 더한다. `90,000 × 0.7` 이 부동소수점에서 `62999.99…` 로 나와
절사 단위가 한 칸 내려가는 것을 막기 위해서다. **이 보정을 빼지 않는다.**

### 별표 2 — 기본 전용사용료 (평일 주간 기준, 1회)

| 시설 | 체육경기 | 체육경기 외 |
|---|---|---|
| 충무체육관 | 90,000 | 200,000 |
| 한밭체육관 | 90,000 | 200,000 |
| 한밭야구장 | 30,000 | 100,000 |

- **체육경기** — 국가·지방자치단체 및 대한체육회·장애인체육회 산하단체가 주관하는 경기
- **체육경기 외** — 직장단체·동호인 등 다수인이 참여하는 경기 및 행사
  (콘서트·공연·전람·전시 등 일반행사 포함)

### 별표 1의2 — 시간대 (조기·주간·야간을 가르는 기준)

| 구분 | 하계(4~9월) | 동계(10~3월) |
|---|---|---|
| 조기 | 05:00~08:00 | 06:00~09:00 |
| 주간 | 08:00~19:00 | 09:00~18:00 |
| 야간 | 19:00~23:00 | 18:00~23:00 |

`dayBands()` 가 날짜에서 계절을, `bandsOf()` 가 걸치는 시간대를 판정한다.

**걸치는 시간대마다 그 시간대의 1회 사용료를 매겨 합산한다.**
제10조제2항이 "사용시간의 일부를 사용하고 종료한 경우에도 전부 사용한 것으로 한다"고
정했기 때문이다. 조기·주간·야간은 각각 별개의 사용이다.

| 사용 시각 (하계·평일) | 계산 | 합계 |
|---|---|---|
| 05:00–08:00 (조기) | 63,000 | 63,000 |
| 10:00–13:00 (주간) | 90,000 | 90,000 |
| 19:00–22:00 (야간) | 135,000 | 135,000 |
| 07:00–12:00 (조기+주간) | 63,000 + 90,000 | **153,000** |
| 14:00–21:00 (주간+야간) | 90,000 + 135,000 | **225,000** |
| 07:00–23:00 (조기+주간+야간) | 63,000 + 90,000 + 135,000 | **288,000** |

토요일 07:00–23:00 이면 94,500 + 135,000 + 202,500 = **432,000**.

- **한 시간대만 쓰면 예전과 금액이 같다** — 담당자 엑셀 표의 값 그대로다
- `note` 는 쓴 시간대를 `+` 로 잇는다 (`조기+주간+야간`)
- 주간을 썼으면 ①②가 곧 주간 1회분이다. 나머지 시간대는 `③-n … (1회분 추가)` 줄로 더한다.
  주간을 안 썼으면 그 시간대의 가산·감산을 ③ 자리에 놓는다
- 이 판정은 보수적으로 잡았다. 시가 다르게 해석하면 여기를 고친다

### 토요일·공휴일 판정

별표 2 비고는 "토요일ㆍ공휴일(주간)" 이라고만 쓰고 일요일을 따로 적지 않았다.
일요일은 「관공서의 공휴일에 관한 규정」 제2조제1호의 공휴일이므로 **포함으로 보았고**,
설정에서 끌 수 있게 해두었다 (`sundayAsHoliday`). `isSurchargeDay()` 참고.

공휴일 자체는 `holidayName()` 이 판정하며 **2040년까지 자동으로 잡힌다** — 위 「공휴일」 절 참고.
산정 내역의 `why` 에 공휴일 이름이 그대로 나온다 (예: "광복절 · 관공서의 공휴일에 관한 규정 제2조").

### 제13조 — 감면 (자동 적용)

조례가 정한 비율은 **세 가지뿐이다.** 호별로 목록을 늘리지 않는다 —
어느 호에 해당하는지는 `article` 에 적어두고 산정 내역에 그대로 띄운다.

| 유형 | 감면율 | 해당 호 |
|---|---|---|
| 전부 면제 | 100% | 제1호~제1호의3 (국가·시 주최/주관, 전문체육 선수 훈련, 직장운동경기부 훈련) |
| 80% 감경 | 80% | 제2호~제7호 (대한체육회 계열, 국가유공자, 65세 이상·장애인·수급자, 학교 수업, 학교 밖 청소년, 전지훈련팀) |
| 50% 감경 | 50% | 제8호·제9호 (스포츠클럽, 시정조정위원회 인정) |

감면은 **①~⑤를 모두 합한 금액**에 적용된다. 제13조가 관람료 수입에 따른 사용료를
포함한다고 명시했기 때문이다.

**감면 대상은 ①~⑤ 와 ⑥ 부속시설이다. ⑦ 전기료·⑧ 수도료만 빠진다.**

부속시설(⑥)에도 **감면이 적용된다.** 제11조제1항이 부속시설 사용료도 "사용료"에 넣고 있어
제13조 대상이다 — 담당자 확인.

**전기료·수도료(⑦⑧)에는 감면을 적용하지 않는다** (2026-09-11 담당자 결정).
한전·수도사업소에서 **이미 할인이 반영된 금액**으로 청구가 와서 원가를 알 수 없기 때문이다.
여기서 또 깎으면 실제로 낸 돈보다 적게 받게 된다. 담당자가 등록창에 적은 금액이
**그대로** 더해진다.

`calcFee` 의 감면 기준은 `annexSum`(부속시설+전기+수도)이 아니라 `annexOnly`(부속시설만) 이고,
`parts.util` 에도 감면율을 곱하지 않는다. **이 구분을 되돌리지 않는다.**

**켜고 끄는 설정을 두지 않는다** (2026-09-18 담당자 요청 — 실제로 사고가 났다).

예전에는 `discountAnnex` 라는 체크가 있었고, 그 이름이
「**부속시설·전기·수도**에도 감면 적용」 이었다. 전기·수도를 감면에서 빼려고 그 체크를 끄면
**부속시설까지 함께 빠졌다.** 담당자 화면이 실제로 그렇게 꺼져 있어서
부속시설 20,000원에 80% 감면이 안 붙고 있었다.

- **`discountAnnex` 를 없앴다.** 설정 화면의 체크·입력 처리·기본값을 모두 지웠다
- `migrate`(8→9) 가 **예전 자료에 남은 값을 `delete` 한다** —
  안 지우면 나중에 같은 이름을 되살릴 때 그 값이 또 따라온다
- 설정 화면에는 **안내만** 남겼다 (무엇이 감면 대상인지는 누구나 볼 수 있어야 한다)
- **다시 값으로 만들지 않는다.** 감면 대상이 바뀌면 조례를 근거로 코드를 고친다

### 별표 6 — 부속시설 사용료

`BYULPYO6` 이 기본 목록이다 (마이크·전광판·조명·냉난방·의자·탁자 등). 설정 화면에서
금액·단위·근거를 고치고 항목을 더할 수 있다. 등록창에서 **수량만 넣으면** 자동으로 더해진다.

**마이크는 「기본료 + 개당」 을 한 항목으로 묶었다** (2026-09-11 담당자 요청).
별표 6 에는 「마이크시설·확성기 기본료(30,000원/1회)」 와 「마이크(5,000원/개)」 가 따로
있지만, **확성기를 쓰지 않아** 둘을 나눠 넣을 이유가 없었다. 등록창에서 마이크 수량만
넣으면 기본료가 저절로 1회분 붙는다 — 1개면 35,000원, 2개면 40,000원이다.

- 항목의 `base`(기본료) 가 0보다 크면 **수량이 하나라도 있을 때 1회분이 먼저 더해진다.**
  줄은 둘로 선다 — `⑥ 부속시설 · 마이크 기본료 1회` · `⑥ 부속시설 · 마이크 2개`
- `baseArticle` 에 **조례의 원래 이름을 그대로 남긴다**
  (`별표 6 · 마이크시설·확성기 기본료 (1회)`). 합쳐 놓아도 근거를 잃지 않게 하려는 것이다
- 설정 화면의 부속시설 항목에 `기본료(원)`·`기본료 이름` 칸이 있다.
  **다른 항목에도 같은 방식으로 쓸 수 있다** — 0 으로 두면 붙지 않는다
- 자료 이관(`migrate`, 5→6) 이 예전 `a1`(마이크시설·확성기 기본료) 수량을 지우고
  `a2`(마이크) 로 옮긴다. `a1` 만 있고 마이크가 0개였던 건은 **마이크 1개로 본다**
  (그 건만 30,000 → 35,000원이 된다). 실제로는 기본료만 쓰는 건이 없어 문제되지 않는다
- **정산서에서는 한 줄로 합친다** — 「마이크시설」 한 줄에 `기본료 + 개당 × 수량` 을
  단가 자리에 넣고 회수를 1 로 둔다 (2026-09-11 담당자 요청).
  서식의 금액 칸이 `단가 × 감면율 × 회수` 라 두 줄을 쓸 수 없다.
  **산정 상세는 그대로 두 줄이다** — 왜 이 금액인지 보여주는 곳이라 근거를 나눠 둔다

**목록에서 뺀 것과 더한 것** (2026-09-11 담당자 요청)

한밭종합운동장에서 쓰지 않는 네 항목을 뺐다 —
**권투링(`a7`) · 인조잔디 관수료(`a11`) · 롤러스케이트 대여(`a12`) · 빙상스케이트 대여(`a13`)**.

- `migrate`(6→7) 가 예전 자료에서도 지운다. 다만 **그 항목을 쓴 예약이 하나라도 있으면 지우지 않는다** —
  지우면 그 건의 금액이 소리 없이 줄어든다. 그런 항목은 설정 화면에서 손으로 지운다.
  **이 보호를 없애지 않는다**

담당자가 쓰는 세 항목을 더했다. **별표 6 에 없는 항목이다.**

| 항목 | 금액 | 단위 |
|---|---|---|
| 플래카드 (`a14`) | 60,000원 | 1회 |
| 중계방송료 (`a15`) | 81,000원 | 1회 |
| 입장료 (`a16`) | 100원 | **명** (입장인원 × 100원) |

- **금액은 담당자가 준 값이고 조례 조문은 아직 확인되지 않았다.** `article` 에
  `담당자 확인 금액 (조례 조문 미확인)` 이라고 적어 두었고, 산정 내역의 `why` 에 그대로 나온다.
  **이 표시를 지우지 않는다** — 조문을 확인하면 설정 화면에서 고친다
- 셋 다 **부속시설(⑥)이라 감면(제13조)이 그대로 적용된다** (담당자 요청).
  `discountAnnex` 가 켜져 있으면 함께 깎인다
- **입장료는 인원수를 손으로 넣는다.** 예약의 `headcount`(인원) 를 자동으로 끌어오지 않는다 —
  참가 인원과 입장(유료) 인원이 다를 수 있어 담당자가 정하게 두었다

**수량과 횟수를 따로 적는다 (`annexUse`)**

같은 것을 여러 번 쓰면 그만큼 받아야 해서 **횟수 칸을 더했다** (2026-09-11 담당자 요청).
**금액 = 단가 × 수량 × 횟수** 다.

| 단위 | 등록창에 뜨는 칸 | 자료 |
|---|---|---|
| 「개」·「명」 | **수량** + **횟수** 두 칸 | `annex[id]` 개수 · `annexTimes[id]` 회수 |
| 「1회」·「1일」 | **횟수** 한 칸 | `annex[id]` 가 곧 회수 (수량은 늘 1) |

- `annexUse(r, it)` 가 `{ count 수량, times 횟수 }` 를 돌려준다. **안 썼으면 `null`.**
  계산·정산서·화면이 모두 이 함수를 쓴다 — `r.annex` 를 직접 읽지 않는다
- **횟수를 안 적으면 1회로 본다.** `readAnnexTimes()` 는 2 이상만 자료에 담는다 —
  기본값을 담을 이유가 없고, 횟수가 없던 예전 자료와 금액이 그대로 맞는다
- **기본료도 횟수만큼 붙는다** — 마이크를 세 번 쓰면 기본료도 세 번이다
- 산정 내역 딱지는 `마이크 2개 × 3회` · `집중조명 2회` 처럼 나온다 (`annexUseLabel`)
- 정산서는 개수 칸(`E`)에 수량을, 회수 칸(`G`)에 횟수를 넣는다.
  여러 건을 한 장으로 낼 때 **횟수가 같으면 수량만 더하고, 다르면 곱을 합쳐 1회로 본다** —
  어느 쪽이든 금액이 어긋나지 않는다

**차례는 `ANNEX_ORDER` 가 정한다** (2026-09-11 담당자 요청)

`집중조명 · 마이크 · 전광판 · 냉난방 · 바닥깔이 · 탁자 · 의자` 뒤에
서식에 자리가 없는 것(조명타워)과 제 칸이 따로 있는 것(플래카드·중계방송료·입장료)을 둔다.

- **정산서 ⑤ 부속시설 일곱 줄과 같은 차례다** — 화면에서 적는 차례와 종이에 찍히는
  차례가 달라 헷갈렸다. 설정 화면·등록창이 모두 이 차례를 쓴다
- `DEFAULT_STATE` 도 `sortAnnexItems()` 를 거친다. `BYULPYO6` 에 적힌 차례가 아니라
  **`ANNEX_ORDER` 를 고친다**
- 여기 없는 항목(담당자가 새로 더한 것)은 뒤에 붙되 서로의 차례는 그대로 둔다
- **「명」 단위를 새로 더했다** (`ANNEX_UNITS`). 「개」 와 똑같이 수량을 세는 단위이고,
  `annexCounts()` 가 둘을 함께 판정한다. 정산서 개수 칸의 표시형식도 그 단위를 따라간다
  (`General"명"` → `800명`)
- **정산서에서는 ⑤부속시설에 넣지 않는다.** 서식에 제 칸이 따로 있어서
  ②입장료·③중계방송료·④상행위(플래카드) 칸으로 간다 (`SETTLE_OWN_ROWS`) —
  아래 「정산서의 ②③④ 칸」 절 참고. 등록창과 산정(⑥)에서는 다른 부속시설과 똑같다

- **전기료·수도료는 자동 계산하지 않는다.** 조례가 "실비"로만 정했고, 사용량을 재야
  금액이 나오기 때문이다. 등록창에서 담당자가 금액을 직접 넣는다.
  별표 6 이 정한 전기료 산식은 설정 화면에 참고로 적어두었다 — 이 안내를 지우지 않는다
- 부속시설 사용료는 행사 종료 후 7일 이내에 징수한다 (제11조제2항). 등록창에 이 안내가 뜬다
- 준비 건에는 부속시설 칸이 나오지 않는다

### 함수가 돌려주는 것

`calcFee(r)` 는 `{ total, lines[], hours, pending, note, bands, annex, parts }` 를 돌려준다.

- `lines[]` 의 각 항목은 `{ label, why, amount }`. **`why` 에 근거 조문이 들어간다.**
  새 계산 항목을 추가하면 `why` 도 반드시 채운다 — 이게 이 도구의 존재 이유다
- `note` 는 "하계 · 야간 · 토요일" 같은 한 줄 요약. 금액 옆에 띄운다
- `pending: true` 는 기본액이 입력되지 않았다는 뜻. 화면에 "기본액 미입력" 표시가 뜬다
- `subtotal: true` 인 줄은 소계다 (`＝ 당일 주간 전용사용료`). 금액을 다시 더하지 않는다
- `parts` 는 목록(수익집계용)에서 따로 떼어 보는 금액이다 —
  `{ use 전용사용료, annex 부속시설, util 공과금(전기+수도) }`.
  **세 조각을 더하면 언제나 `total` 과 같아진다** (부속시설·공과금을 먼저 절사하고
  전용사용료를 나머지로 둔다). 화면에 뜬 합계와 열 합계가 어긋나면 안 되기 때문이다
- `feeParts(r)` 는 직접 입력한 금액(`feeOverride`)까지 반영해 같은 세 조각을 돌려준다.
  **목록·요약·인쇄·내보내기는 전부 이 함수를 쓴다**
- `feeLinesHtml(f)` 가 산정 내역 표를 만든다. 우측 상세의 `산정 상세` 가 이것을 쓴다

### 제12조 — 관람료 수입 (등록창에서 받지 않는다)

**관람료 수입 입력란과 「고등학교 이하의 경기」 체크를 등록창에서 뺐다** (2026-09-11 담당자 요청).
관람료를 받는 행사가 없어 한 번도 적은 적이 없는 칸이었다.

- **계산(`calcFee` ⑤)과 정산서 ① 칸은 그대로 두었다.** 예전 자료에 관람료가 적혀 있으면
  지금도 제12조대로 계산되고 근거 조문이 뜬다. 이 계산을 지우지 않는다
- `formToRes()` 가 `admission`·`schoolGame` 을 **이전 값에서 물려받는다**
  (`paid` 와 같은 방식). 예약을 고쳐도 예전에 적힌 관람료가 지워지지 않는다
- 설정의 `admSportsRate`·`admOtherRate`·`admSchoolCut` 은 남는다 —
  정산서 ① 칸의 「사용료(체육경기 10%, 프로·체육 외 경기 12.5%)」 딱지가 이 값을 쓴다
- 관람료를 다시 받게 되면 등록창에 칸을 되살리면 된다. 계산은 이미 있다

### 화면

- **등록창에는 금액을 띄우지 않는다.** 담당자 요청으로 뺐다 —
  등록할 때는 무엇을 언제 쓰는지에 집중하고, 금액은 따로 본다.
  등록창에 남은 것은 부속시설 합계와 `이용료 직접 입력` 뿐이다
- **그 부속시설 합계는 「감면 전」 금액이다** (`calcFee(r).annex` — ⑥+⑦+⑧ 를 그대로 더한 값).
  숫자만 덩그러니 있어서 **「감면이 적용되지 않았다」 고 오해한 일이 있었다** (2026-09-18).
  그래서 감면 유형을 골라 두면 바로 밑(`#annexDcNote`)에 얼마가 되는지 함께 적는다 —
  `80% 감면을 적용하면 143,980원 — 부속시설 36,520원 + 전기·수도 107,460원`.
  **이 안내를 빼지 않는다.** 감면이 없으면(0%) 안내를 감춘다
- **금액과 근거 조문은 두 곳에서 본다.**
  ① 달력에서 날짜를 누른 뒤 우측 상세의 `산정 상세` — 담당자가 아닌 직원이 보는 곳이라 여기가 중요하다
  ② 목록 탭의 **수익집계용** — 이용료·부속시설·공과금을 열로 나눠 놓았다
- **이 두 곳을 없애면 이 도구의 존재 이유가 없어진다.** 등록창에서 뺀 것은
  같은 근거를 다른 데서 더 잘 보이게 두었기 때문이지, 계산을 감추려는 것이 아니다

### 목록 — 조회용 / 수익집계용 (섹션 7)

같은 자료를 보는 눈이 둘이라 화면을 나눴다. 열 정의는 `LIST_MODES` · `LIST_COLS`
**한 곳에만** 두고, 화면·인쇄·엑셀·CSV 가 모두 그것을 쓴다 —
화면에서 본 것과 뽑은 종이가 달라지지 않게 하려는 것이다.

| | 조회용 | 수익집계용 |
|---|---|---|
| 보는 것 | 무슨 행사인지 | 돈 |
| 열 | 완료·날짜·요일·시간·시설·구분·신청 단체·목적·인원·**메모** | 완료·날짜·요일·시간·시설·구분·신청 단체·인원·감면·**이용료·부속시설·공과금·합계** |
| 뺀 것 | 감면·이용료·상태 | 메모·목적·상태 |

**공과금은 한 칸으로 보거나 전기료·수도료로 나눠 볼 수 있다** (`공과금 합계` / `전기·수도 따로`).
`view.utilSplit` 이 정하고, 실제로 쓸 열은 `colsOf(mode)` 가 돌려준다 —
화면·인쇄·엑셀·CSV 가 모두 이 함수를 쓴다. **전기 + 수도 는 언제나 공과금과 같다**
(수도료를 나머지로 두어 맞춘다).

- 맨 왼쪽 `완료` 열은 그 날짜를 확인 완료로 표시했는지다 (✓). 달력의 ✓ 와 같은 값이다
- **메모와 시작 시각은 붉은 글씨로 세운다** (`#listBody td.memo` · `.t-start`, 담당자 요청).
  굵기는 600 까지만 쓴다 — 700 이면 표 전체가 시끄러워진다. 새 색을 만들지 않고 `--alert` 를 쓴다.
  시작 시각만 `<b>` 로 감싸고 끝 시각은 그대로 두며, **종이·엑셀·CSV 에는 글자 그대로 나간다**
- **요일 글씨는 달력과 같은 색이다** — 토요일 파랑, 일요일·공휴일 빨강 (`dowClassOf`).
  글씨에만 쓰고 칸 전체를 물들이지 않는다. 요금 관리·날짜별 상세 인쇄·엑셀도 같다.
  공휴일이 토요일이면 **빨강이 이긴다**
- `MERGE_COLS`(완료·날짜·요일)는 날짜 하나에 딸린 값이라 엑셀에서 여러 줄을 하나로 합치고
  CSV 에서는 둘째 줄부터 비운다
- 금액 열이 있는 쪽에는 합계 줄(`tfoot`)이 붙는다
- 열 이름을 `LIST_MODES[...].cols` 에서 바로 읽지 않는다. **`colsOf(mode)` 를 쓴다**
- 부속시설·공과금은 0원이면 칸을 비운다 (`blankZero`). 0 이 줄줄이 찍히면 읽기 나쁘다
- **준비 건은 노란 테두리로만 구분한다** (`tr.is-prep`). 바탕은 다른 줄과 같다.
  (엑셀만 담당자 요청으로 연한 파랑을 칠한다 — 화면 색과 다른 것은 일부러다.)
  구분 열에도 뱃지를 달지 않고 행사와 같이 글씨로만 쓴다 — 테두리가 이미 구분해 준다.
  인쇄와 엑셀에서도 같게 나온다
- 합계 줄의 `N건 합계` 는 금액이 시작되기 전 칸을 `colspan` 으로 합쳐서 넣는다.
  좁은 `완료` 칸에 넣으면 글자가 세로로 접힌다
- 위쪽 요약도 보는 눈에 맞춘다 — 조회용은 건수만, 수익집계용만 금액을 띄운다


### 인쇄 (섹션 9-B)

`인쇄…` 를 누르면 방식을 고른다. 인쇄 직전에 `body` 에 클래스를 붙이고,
`@page` 크기는 `#printPage` 에 그때그때 써넣는다. 끝나면(`afterprint`) 되돌린다.

| 방식 | 용지 | 하는 일 |
|---|---|---|
| 달력 | A4 가로 | **행사 달력과 준비 달력을 따로 찍는다** (기본 2장). `+N건 더` 없이 다 편다.
머리에는 건수만 적고 **이용료는 넣지 않는다** — 현장에 붙여 두는 종이라 금액이 필요 없다.
**인원수를 넣을지 인쇄 창에서 고른다** (기본은 넣지 않음) |
| 날짜별 상세 | A4 세로 | 고른 날짜만 표로 찍는다. **메모까지 나온다** |
| 목록 | A4 가로 | 지금 목록 화면(조회용/수익집계용)과 거르기 조건 그대로 |

**행사와 준비를 한 장에 섞지 않는다.** 현장에 붙여 두는 종이가 다르기 때문이다
(행사 일정표 / 준비 작업표). 인쇄 창에서 `행사+준비(2장)` · `행사만` · `준비만` 을 고른다.

달력·날짜별 상세는 화면을 그대로 찍지 않고 `#printArea` 에 인쇄판을 새로 만들어
`main` 을 감춘다 (`buildPrintCalendar` · `buildPrintDetail`). 목록만 화면 표를 그대로 쓴다.

- **달력 인쇄를 세로 용지로 되돌리지 않는다** — 우측 상세까지 끼어들어 칸이 눌리고
  글씨가 안 보인다는 것이 이 기능을 고친 이유다
- **인쇄판에는 이번 달 날짜만 찍는다.** 앞뒤 달 칸은 자리만 남기고 비운다
  (`.cell.blank`) — 요일 자리가 어긋나지 않게 칸 자체는 지우지 않는다.
  화면 달력은 예전대로 앞뒤 달 날짜를 회색으로 보여준다
- 칸의 가로줄은 **앞칸의 아래 테두리**로 그린다 (칸에는 위 테두리가 없다).
  그래서 요일 머리글(`.dow`)의 아래 테두리를 지우면 첫 주 위의 줄 하나가 통째로 사라진다.
  **`.pc-page .dow` 의 아래 테두리를 없애지 않는다**
- **행사가 많은 달도 한 장에 담는다** (`fitPrintPages`). 내용을 줄이거나 감추지 않고
  글씨째로 조금 축소한다. 90건까지 한 장에 들어가고 빠지는 행사가 없는 것을 확인했다
  - 인쇄판 스타일(`.pc-page …`)을 **`@media print` 밖에 두는 것은 일부러다.**
    화면에서도 인쇄와 같은 크기로 재야 배율을 미리 구할 수 있다.
    `#printArea` 는 화면에서 감춰져 있어 밖에 둬도 화면에 영향이 없다
  - 잴 때는 종이와 같은 폭(283mm)으로 잠깐 펼쳐 놓는다. 폭을 넓히면 글이 덜 접혀
    높이가 줄어들므로, 재고 줄이기를 몇 번 되풀이해 값을 맞춘다
  - **`.pc-page` 에 `overflow:hidden` 을 빼지 않는다.** `transform` 은 그려지는 크기만
    줄이고 자리(레이아웃 높이)는 그대로라서, 잘라내지 않으면 브라우저가 줄어들기 전
    높이를 기준으로 장을 넘겨 버린다 — 축소해도 두 장으로 나온다
  - 배율 하한은 `FIT_MIN`(0.55). 그보다 더 줄여야 하면 글씨를 못 읽으므로
    자르지 않고 두 장으로 넘긴다
- 날짜별 상세는 날마다 표가 따로라 열 폭을 `<colgroup>` 으로 고정해 두었다. 안 그러면 어긋난다

### 내보내기 (섹션 9-C)

`exportTable(fmt, rows, mode, periodLabel)` 하나가 엑셀과 CSV 를 모두 낸다.

- **엑셀은 진짜 `.xlsx`(OOXML) 다.** 정산서(9-D)가 쓰는 `xwNew`·`xwStyle`·`xwPut`·`xwXlsx`
  를 그대로 쓴다 — 라이브러리는 없다. 예전에는 HTML 표를 `.xls` 이름으로 넘겨서
  **"파일 형식과 확장명이 일치하지 않습니다"** 경고가 떴다. **HTML 표로 되돌리지 않는다**
- 같은 날짜의 날짜·요일 칸을 병합하고, 열 너비를 그 열에서 가장 긴 값에 맞춘다
  (한글은 두 칸으로 센다 · `textWidth`). 금액은 글자가 아니라 **숫자**로 넣어 합계를 낼 수 있다
- **완료 칸은 내보내지 않는다** (`cols.filter(k => k !== "done")`). 화면·종이에서만 쓰는 표시다
- **시작 시각과 메모는 화면과 같이 붉은 굵은 글씨다.** 시간 칸은 한 칸 안에서 시작만 굵게
  물들여야 해서 **서식 있는 글**(`cell.rich`, 여러 `<r>` 조각)로 넣는다
- **인원도 숫자로 넣는다** (`col.cls === "num"`). 글자로 넣으면 엑셀이
  「숫자가 텍스트로 저장됨」 초록 세모를 붙여 칸마다 거슬린다
- **날짜 묶음의 마지막 줄 아래에 굵은 줄(medium)을 긋는다** — 날짜끼리 한눈에 갈라 보이게
  하려는 것이다. 세로로 합쳐진 날짜·요일 칸의 아래 테두리는 **맨 아랫줄 칸이 들고 있으므로**,
  앞줄에 합쳐져 글자를 넣지 않는 칸에도 **서식은 넣어야 한다.**
  비워 두면 날짜 칸만 굵은 줄이 빠져 보인다
- 용지는 **가로 · 한 폭에 맞춤**이다 (`wb.page = { orient:"landscape", fitW:1 }`).
  `wb.page` 를 두지 않으면 정산서 쪽 기본값(세로·86%·가운데)이 딸려온다
- **엑셀만 칸 바탕을 칠한다** (담당자 요청 — 종이에서 한눈에 갈라 보여야 한다).
  **화면과 인쇄는 예전대로 테두리와 글자색으로만 구분한다** — 둘을 같이 맞추려 하지 않는다

  | 어떤 줄 | 색 (엑셀 표준 팔레트) |
  |---|---|
  | 준비 건 | `DEEBF7` — 파랑 강조 5, 80% 더 밝게 |
  | 토·일·공휴일 | `FFF2CC` — 황금색 강조 4, 80% 더 밝게 |

  **글자색도 엑셀 표준 색으로 쓴다** (담당자 요청) — 토요일 `0070C0` 파랑,
  일요일·공휴일 `FF0000` 빨강, 시작 시각·메모 `FF0000` 빨강.
  **화면은 예전 색(`--sat`·`--sun`·`--alert`)을 그대로 쓴다**
- **글자 크기는 14pt 다** (`FSZ`, 담당자 요청). 열 너비는 엑셀 기본 글꼴(11pt) 칸 수로
  세므로 `FSZ/11` 만큼 넓혀야 글이 잘리지 않는다. 제목은 18pt, 줄 높이도 따라 키운다

  **준비 파랑이 토·일 금색을 이긴다.** 토요일 준비 건은 파랑이다 —
  요일 글씨가 이미 파랑·빨강이라 무슨 날인지는 그대로 보인다.
  준비 테두리(`YL`)도 같은 계열의 `2E75B6`(파랑 강조 5, 25% 더 어둡게) 이다.
  **화면의 준비 노랑(`--prep-line`)과 다르다** — 담당자가 엑셀 쪽만 이 색으로 고른 것이다
  토·일·공휴일은 **날짜 단위**라 여러 줄에 걸쳐 병합된 날짜 칸까지 칠한다.
  준비는 건마다 갈리므로 병합 칸을 칠하지 않는다 (아래 참고)
- 준비 건의 **테두리는 줄 바깥쪽에만 얇게(1pt)** 두른다. 칸마다 두르면 격자가 겹쳐 보이므로
  위·아래는 모든 칸에, 왼쪽·오른쪽은 양 끝 칸에만 긋는다.
  **여러 줄에 걸쳐 병합된 날짜 칸은 준비 색으로 칠하지도 두르지도 않는다** —
  같은 날 행사 줄까지 통째로 물든다
- **CSV** — 글자만 담는 형식이라 색도 병합도 담을 수 없다. 같은 날짜의 날짜·요일을
  비워 두고 준비 건에 `[준비]` 를 붙이는 것까지만 한다. 서식이 필요하면 엑셀 쪽을 쓴다
- 게시된 화면(아티팩트)에서는 파일 저장이 막혀 있어 붙여넣기용 표로 건넨다

### 행사 정산서 (섹션 9-D)

담당자가 손으로 채우던 **부서 서식 「행사 정산서」** 를 그대로 엑셀로 낸다.
달력 우측 상세나 목록에서 건을 체크하고 `정산서` 를 누르면 파일이 떨어진다.

**서식은 받은 그대로다 — 칸·병합·테두리·문구를 임의로 더하거나 빼지 않는다.**
자동으로 채우는 것은 *내용*뿐이다.

| 칸 | 어디서 오나 |
|---|---|
| 장소 · 행사명 · 대관자 | 고른 예약의 시설명 · 이용 목적 · 신청 단체 (여러 값이면 `,` 로 잇는다) |
| 일자 | **붙어 있는 날짜는 `8.28~8.29` 로 묶는다** (`settleDateLabel`). 떨어져 있으면 `,` 로 잇는다 |
| 장소 · 행사명 · 대관자 | 한 장에는 **한 값씩만** 들어간다 (아래 「한 장에 무엇을 묶나」 참고) |
| ① 관람료 수입 `<A>` | 제12조 계산값 (고교 이하 20% 할인까지). **등록창에서 관람료를 받지 않으므로 새 건은 0 이다** |
| ① 감면 `<C>` | `=(A+B)×감면율` — **수식이라 `<B>` 를 손으로 적으면 따라온다** |
| ⑤ 부속시설 | 별표 6 항목별 단가·수량. 금액은 `=단가×감면율×회수` 수식.<br>**기본료가 붙는 항목(마이크)은 「마이크 기본료 1회」 줄이 따로 선다** |
| 공과금 | 전기료·수도료 (대장이 산정한 감면 반영 금액) |
| 입금 계좌 | `settings.settle` — **⚙ 설정** 창의 「정산서 입금 계좌」 (담당자만 보인다) |
| 대관담당자 | `settings.settle.officer` — 설정에서 고른 이름. 안 골랐으면 비워 둔다 |
| 통제 도장 | 한글 원본의 도형 값을 읽어 내 그린다 (`stampCanvas`). **가운데 연도만 자동, 월·일은 손으로** |
| 허가번호 | **비워 둔다. 바탕은 흰색이다** (원본은 노랑) |
| **⑥ 전용사용료 · 허가일자** | **비워 둔다. 담당자가 손으로 적는 칸이다 (자동 금지)** |
| 허가번호 · 매표 · ②입장료 · ③중계방송료 · ④상행위 | 대장에 없는 값이라 비워 둔다 |

- **진짜 `.xlsx`(OOXML) 로 낸다.** 예전에는 HTML 표를 `.xls` 이름으로 넘겨서
  엑셀이 "파일 형식과 확장명이 일치하지 않습니다" 경고를 띄웠고, 빈 칸에 공백 문자가 들어가
  그 칸을 쓰는 수식이 `#VALUE!` 로 깨졌으며, 날짜 글자가 일련번호(`46260`)로 바뀌었다.
  ZIP(압축 없이 담기만)과 XML 을 직접 만든다 — 라이브러리는 쓰지 않는다 (`zipStore`·`xwXlsx`)
- **서식은 원본 파일에서 통째로 뽑아 쓴다** (`SETTLE_FORM`). 열 너비·줄 높이·병합·테두리·
  글꼴·표시형식·배경색·수식이 원본과 같은 값 그대로 들어간다. 코드는 *내용만* 채운다.
  원본과 칸 1,134개를 대조했을 때 **뜻이 다른 칸은 하나도 없다.** 서식을 손댄 곳은
  네 가지뿐이고, 모두 *보이게 하려고* 손댄 것이다 —
  ① 흰 바탕으로 바꾼 허가번호 칸 1개,
  ② 원본이 **테마·색인 번호**로 적어 둔 색을 같은 색의 **값(rgb)** 으로 풀어 쓴 41개
  (테마 파일을 함께 넣지 않으므로 값으로 박는다. 보이는 색은 같다),
  ③ 칸보다 긴 글이 잘리지 않게 **「칸에 맞춤」** 을 켠 칸,
  ④ 「칸에 맞춤」을 쓸 수 없어 **글씨만 한 호 줄인** 칸 4개 (아래 「긴 글 손보기」 절).
  **열 너비 14개·줄 높이 81개·병합 197개는 전부 원본 그대로다**
- **비율이 적힌 딱지는 설정값을 따라간다** — `감면율(80%)` 처럼 서식에 글자로 박혀 있던 것을
  그 건의 감면율(`H13`·`I15`·`H52`)과 조례 비율(`C9`·`C48`)로 고쳐 넣는다. 글자만 바꾸고 서식은 그대로 둔다
- **인쇄 영역(`A1:N81`)·머리글·쪽 나눔 미리보기도 원본 그대로다.** 서식 밖이 회색으로
  눌려 정산서만 또렷하게 보이고, 장마다 「대관 사용료 정산서」 머리글이 찍힌다
- 열 너비를 뽑을 때 **`<col min="13" max="14">` 처럼 여러 열을 한꺼번에 지정한 것을 놓치지 않는다** —
  N 열을 놓쳐 기본 너비가 되면 오른쪽 글이 서식 밖으로 삐져나온다
- **`SETTLE_FORM` 을 손으로 고치지 않는다.** 서식이 바뀌면 원본 `.xlsx` 에서 다시 뽑아
  그 줄을 통째로 갈아 끼운다
- **한 장에 무엇을 묶나** — 고른 건을 **장소·신청 단체·이용 목적**으로 나눠
  (`settleGroups`) 셋이 모두 같은 건만 한 장으로 묶는다. 같은 행사가 여러 날에
  걸친 경우이고, 일자가 `8.28~8.29` 로 이어 붙는다.
  셋 중 하나라도 다르면 **서로 다른 행사이므로 파일을 따로 낸다.**
  여러 장이 나올 때는 파일 이름 앞에 장소를 붙여 구분한다.
  브라우저가 한꺼번에 여러 개 내려받는 것을 막는 일이 있어 0.35초씩 띄워서 보낸다
- **금액 칸은 값이 아니라 엑셀 수식으로 낸다.** 손으로 적는 칸이 있는 서식이라
  숫자를 박아 넣으면 그 칸을 채워도 ⑦소계·⑧기납부액·금회징수액이 따라오지 않는다
- **두 장이 한 시트에 위아래로 놓인다** — 위가 보관용(결재란·통제), 아래가 제출용(이사장).
  39줄 뒤에 쪽 나눔이 들어가 두 장으로 인쇄된다. 원본과 같은 구조다
- **제출용(2장)은 보관용(1장)을 수식으로 따라간다** (원본이 그렇게 짜여 있다).
  1장에서 손으로 적는 칸(허가번호·전용사용료·허가일자)이 비어 있으면 2장에 `0` 이나
  `1899.12.30` 이 찍히므로, 그 칸들만 `IF(...="","",...)` 로 감쌌다
- **통제 도장은 한글 원본의 도형 값을 읽어 내 그린다** (`stampCanvas`).
  한글 파일(`c565842c…hwp`) 안에는 **그림이 들어 있지 않다** — 원·선·글상자로 된
  벡터 도형이라 꺼내 쓸 그림이 없다. 그래서 파일을 풀어(`BodyText/Section0`·`DocInfo`)
  도형 값을 읽고 같은 자리에 그린다. **눈대중이 아니다:**

  | 무엇 | 값 (7086 × 7086 HWPUNIT · 1 HWPUNIT = 1/7200 인치) |
  |---|---|
  | 원 | 중심 (3543,3543) · 반지름 3543 · 파랑 `#0000FF` · 굵기 34 |
  | 가로줄 | (88,2829)–(7038,2841) · (88,4367)–(6988,4367) |
  | 글씨 | 새굴림 9pt — `통 제`(장평 100%) · 연도(100%) · `체육시설사업팀`(90%, 자간 −10%) |

  글자가 앉는 자리(`base`·`at`·`step`)는 **원본 그림에서 재어** HWPUNIT 으로 적었다.
  PC 마다 공백 폭이 달라도 도장이 흐트러지지 않게 **글자를 하나씩 정해진 자리에 놓는다.**
  **이 값을 눈대중으로 고치지 않는다** — 서식이 바뀌면 한글 원본을 다시 풀어 잰다
- **가운데 줄의 연도만 자동이고 월·일은 손으로 적는다.** 담당자가 한글에서 연도만
  고쳐 쓰던 것을 그대로 옮긴 것이라, 이 자동 처리를 빼지 않는다
- **엑셀 자리(117px)의 여덟 배(936px)로 그려 넣는다** (`STAMP_SCALE`).
  원본 엑셀에 붙어 있던 그림은 95px 짜리를 117px 로 늘려 써서 계단이 보였다.
  크게 그려 넣으면 인쇄해도 또렷하다
- **붙는 자리와 크기는 원본 엑셀 그대로다** (`SETTLE_STAMP_AT`). 원본 `drawing1.xml` 의
  `twoCellAnchor` 값을 그대로 옮겼다 — M36(12열·685800EMU / 35줄·152400EMU) 에서 시작해
  1,114,425 EMU(117px) 사각형이다. **눈대중으로 고치지 않는다.**
  `oneCellAnchor` 로 되돌리지 않는다 — 줄 높이가 조금만 달라져도 자리가 밀린다
- 다른 도장을 쓰려면 **`⚙ 설정 → 통제 도장`** 에 그림을 올린다
  (`settings.settle.stamp` · base64 · 400KB 까지 · PNG·JPG·GIF). 자리·크기는 그대로 맞추되
  **올린 그림은 연도가 자동으로 바뀌지 않는다** (그림에 그려져 있기 때문이다).
  그림을 못 만들면 원본처럼 빈칸으로 둔다 (`settleStamp()`)
- **허가번호 칸(C1:G1)은 흰 바탕으로 낸다.** 원본은 노랑이지만 손으로 적는 칸이라
  바탕색으로 세울 이유가 없다는 담당자 요청이다. 공과금 칸의 노랑은 그대로 둔다
**⑤ 부속시설 — 일곱 줄을 늘 같은 차례로 (`SETTLE_ANNEX_SLOTS`)**

서식의 일곱 줄에 **정해진 차례로** 앉힌다 (2026-09-11 담당자 요청).
쓴 것만 위에서부터 채우던 방식에서 바꿨다 — 종이에서 늘 같은 자리를 보게 하려는 것이다.

| 줄 | 이름 | 설정의 항목 | 비고 |
|---|---|---|---|
| 1 | 집중조명 | `a4` | |
| 2 | **마이크시설** | `a2` | 기본료 + 개당을 **한 줄로 합친다** (`merge`) |
| 3 | 전광판 | `a3` | |
| 4 | 냉난방 | `a6` | |
| 5 | 바닥깔이 | `a10` | |
| 6 | 탁자 | `a9` | **개수 칸을 쓴다** (`ea`) |
| 7 | 의자 | `a8` | 〃 |

- **안 쓴 줄도 이름을 찍고 단가·수량·금액을 0원으로 둔다.** 무엇을 안 썼는지가 종이에 보여야 한다
- **뒤 두 줄(탁자·의자)의 차례를 바꾸지 않는다** — 서식의 개수 칸 표시형식과
  금액 수식(`D*F*G*E`), 제출용 쪽 거울 수식이 그 두 줄에만 걸려 있다.
  차례를 바꾸면 개수가 엉뚱한 줄에 찍힌다
- 설정의 항목은 **`id` 로 먼저 찾고, 없으면 이름에 `key` 가 들어간 항목**을 찾는다 —
  담당자가 이름을 고쳐도 자리를 잃지 않게 하려는 것이다.
  한 항목이 두 자리에 앉지 않게 찾은 것은 `taken` 에 담는다
- 서식에 자리가 없는 항목(예: 조명타워)을 쓰면 **넣지 못했다고 화면에 알린다.**
  줄을 늘리지 않는다 — 늘리면 서식이 어긋난다
- 찍히는 이름은 **서식 쪽 짧은 이름**(`label`)이다. 설정의 긴 이름
  (「냉·난방료 기본료」)을 쓰면 칸보다 길어 0.67배로 줄어들었다

**정산서의 ②③④ 칸 — 서식에 제 칸이 있는 항목 (`SETTLE_OWN_ROWS`)**

입장료·중계방송료·플래카드는 **⑤ 부속시설에 넣지 않는다.** 서식에 제 칸이 따로 있다.

| 항목 | 서식 칸 | 단가 | 수량 | 감면율 | 계 (서식 수식) |
|---|---|---|---|---|---|
| 입장료 | ② | `C14` | `F14` 입장인원 | `H14` | `K14 = C14*F14*H14` |
| 중계방송료 | ③ | `C16` | `F16` | `I16` | `K16 = C16*F16*I16` |
| 플래카드 | ④ 상행위 | `H19` | `J19` | `I19` | `L19 = H19*I19*J19` |

- **계 칸은 서식의 수식이 스스로 곱한다.** 단가와 수량만 넣는다 —
  금액을 박아 넣지 않는다 (손으로 고치면 따라와야 한다)
- 서식에 수량 칸이 하나뿐이라 **수량 × 횟수를 한 숫자로** 넣는다
  (입장료 800명을 두 번 쓰면 1,600)
- 안 쓴 항목은 단가·수량을 0 으로 두어 0원으로 찍힌다
- `②` 의 딱지 「일반(입장인원x200원)」 은 **설정한 입장료 단가를 따라간다**
  (`C13`·`C52` — 감면율 딱지와 같은 방식). 입장료 항목을 지웠으면 서식 글을 그대로 둔다
- 제출용(2장)은 서식이 이미 수식으로 따라가게 짜여 있다 (`C53`·`F53`·`C56`·`H56`·`H59`·`J59`)
- 체크는 `settleSel` 에 담고 **자료에는 넣지 않는다.** 화면을 다시 그려도 남지만 저장되지 않는다.
  **정산서를 내고 나면 체크를 푼다** — 달력과 목록이 같은 목록을 쓰므로 둘 다 함께 풀린다
**숫자·수식 칸은 `###` 로 깨지지 않게 「칸에 맞춤」을 켠다**

엑셀은 **숫자가 칸보다 넓으면 값 대신 `###` 로 덮어 버린다** — 글자와 달리 옆의 빈 칸으로
흘러넘치지 않기 때문이다. 감면을 「해당 없음」(0%)으로 두면 감면율 칸이 `20%` 에서
`100%` 로 한 자 늘고 금액도 다섯 배가 되어 실제로 그 일이 났다 (2026-09-11 담당자 제보).

- **숫자·수식 칸에는 모두 「칸에 맞춤」을 켠다.** 값이 칸에 들어갈 때는 아무것도 바꾸지
  않으므로 켜 두어 손해가 없다. 서식 자체의 칸은 `numStyle()`, 코드가 넣는 칸은
  `atNum()`·`atFml()` 이 건다 — **숫자나 수식을 넣을 때 이 셋 말고 `at()` 을 쓰지 않는다**
- **줄바꿈 칸에는 켜지 않는다** — 엑셀이 둘을 같이 못 쓰고, 거기 걸린 칸
  (`C41` 행사명 거울 · `C69`)은 넓은 병합 칸이라 `###` 이 날 일이 없다
- 열 너비를 넓혀 맞추지 않는다 — 쪽 나눔과 인쇄 영역이 어긋난다

**긴 글 손보기 — 잘리거나 깨알만 해지지 않게**

칸 폭과 병합은 원본 그대로 두고, **글이 칸을 넘치는 자리만** 고친다.
엑셀 규칙 때문에 한 가지 방법으로는 안 된다.

| 엑셀 규칙 | 그래서 |
|---|---|
| 「칸에 맞춤」(shrinkToFit) 과 「줄바꿈」(wrapText) 은 **같이 쓸 수 없다** | 줄바꿈 칸에는 글씨 크기를 줄인다 |
| 「칸에 맞춤」은 **제 칸 폭**에 맞춘다 — 옆의 빈 칸으로 흘러넘치던 글에 켜면 **도리어 깨알만 해진다** | 흘러넘겨야 보이는 줄에는 켜지 않는다 |
| 글이 넘쳐도 **옆 칸이 비어 있으면 그리로 흘러 보인다** | 옆 칸이 막힌 자리만 손본다 |

`settleWorkbook()` 끝에 세 묶음으로 나눠 둔다.

| | 어디 | 무엇을 |
|---|---|---|
| ㉮ | `M12`·`M16`·`M51`·`M55` (④상행위사용료·추가전용사용료) | 옆 칸이 막혀 잘리던 원본 문구 — 「칸에 맞춤」 |
| ㉯ | `I1`·`C3`·`I36`·`I40`·`C42` (장소·대관자·대관담당자와 제출용 거울) | 대장에서 채우는 칸 — 길게 적어도 잘리지 않게 미리 켜 둔다 |
| ㉰ | `J70` 9.5pt · `M20`·`M56`·`M59` 10pt | 「칸에 맞춤」을 쓸 수 없는 칸 — 글씨만 한 호 줄인다 |

- `J70`(※ 입금전 계좌번호 확인 필수…) 은 **옆의 빈 칸으로 흘러넘겨야 다 보이는 줄**이다.
  여기에 「칸에 맞춤」을 켜면 6.8칸짜리 제 칸에 갇혀 읽을 수 없게 된다. **켜지 않는다**
- `M20`·`M56`·`M59`(⑦소계·⑧전용사용료 기납부액) 는 줄바꿈 칸인데 줄 높이가
  한 줄만큼 모자랐다. 줄 높이는 원본 값이라 건드리지 않고 글씨를 줄였다
- **이 값들은 서식의 열 너비·줄 높이에서 재어 넣은 것이다. 눈대중으로 고치지 않는다** —
  서식이 바뀌면 다시 잰다
- 이미 「칸에 맞춤」이 켜져 있던 곳: 부속시설 이름(`B20:B26`·`B60:B66`) ·
  입금 계좌(`M30`·`M31`·`M71`·`M73`) · 조례 안내문(`L28`·`L29`·`M32`·`L68`·`L69`·`M75`)
- 남는 한계: 부속시설 이름 칸은 10.7칸뿐이라 **「냉·난방료 기본료」 는 0.67배로 줄어든다.**
  잘리지는 않는다. 더 키우려면 서식의 열 너비를 바꿔야 해서 하지 않았다.
  (「마이크시설·확성기 기본료」 가 0.44배로 깨알만 하던 것은 **마이크로 합치면서 사라졌다**)

- 목록의 정산 체크 칸은 **`LIST_COLS` 밖에 둔다** — 열 정의를 늘리면 인쇄·엑셀·CSV 까지 따라 늘어난다.
  종이에서는 `@media print` 의 `.selcol` 로 뺀다

### 자료 이관에서 조심할 것

`migrate()` 가 감면 유형 목록을 갈아끼울 때, 예약이 들고 있던 `discountId` 를
**감면율 기준으로 새 목록에 옮긴다.** 같은 id 라도 목록이 바뀌면 뜻이 달라지기 때문이다
(예전 `d1` 은 "공공기관 0%", 그다음 `d1` 은 "국가·시 주최 100% 면제").
그대로 두면 감면이 없던 예약에 100% 면제가 붙는다. 맞는 비율이 새 목록에 없으면
`d0`(해당 없음)으로 되돌린다. **이 처리를 없애지 않는다.**

### 준비시간

행사 전후의 설치·철거 시간. **조례에는 준비시간 규정이 없다** — 기관 내부 기준이다.
`why` 에 "조례에 별도 규정 없음 (기관 내부 기준)" 이 붙는다. 이 표시를 지우지 않는다.

- **준비는 별도 건으로 따로 등록한다.** 등록창에서 `건 구분` 을 `준비` 로 고른다.
  행사 등록창에 있던 `준비시간 함께 등록` 은 담당자 요청으로 뺐다 —
  **되살리지 않는다.** 예전 자료의 `linkId` 연결은 그대로 살려 둔다
  (그 행사를 지우면 딸린 준비 건도 함께 지워진다)
- 시설의 `prep.allowed` 가 false면 등록창에서 "준비" 선택지가 막힌다
- 산정 방식은 기본액의 % / 시간당 별도 단가 / 무료 중 선택.
  **기본값은 기본액의 100% 이고 `prepSurcharge` 는 켜져 있다** — 준비시간도
  그 날 그 시간대의 전용사용료를 그대로 따른다 (담당자 확인).
  토요일 야간이면 행사와 같이 202,500원이 된다
- 요율을 낮추려면 시설 설정의 `기본액 대비(%)` 를 고친다. 계산 구조는 그대로다

## 저장 방식 (섹션 1, 1-B)

같은 코드가 여러 환경에서 다르게 동작한다. `RUNTIME` 이 어느 모드인지 들고 있고,
`boot()` 에서 위에서부터 순서대로 판정한다.

| 모드 | 조건 | 동작 |
|---|---|---|
| server | `serverCfg()` 가 있고 아티팩트가 **아님** | 서버에 저장. 10초마다 다른 사람 변경 확인 |
| shared | `claude.use("artifact")` 가 있음 | 아티팩트에 `data/state.json` 으로 커밋 |
| local | 그 외 (내려받은 파일) | localStorage |
| readonly | 저장이 `not_writer` 로 거부됨 | `body.ro` 를 붙여 쓰기 UI를 감춤 |

**`body.ro` 는 서버 연결 카드(`#serverCard`)를 잠그지 않는다.** 여기까지 잠그면
로그인 입력란을 누를 수 없어서 조회 전용에서 영영 빠져나오지 못한다. 이 예외를 없애지 않는다.

### 서버 모드 (섹션 1-B)

Supabase REST 를 **직접 fetch** 로 부른다. SDK 를 쓰지 않는다 — 단일 파일 제약을 지키기 위해서다.

- 표는 `ledger` 한 개. `id='main'` 한 행에 `state` 전체를 `jsonb` 로 담는다.
  **행 단위로 쪼개지 않는다.** 입력자가 한 명이라 병합할 일이 없고, 지금 구조 그대로 담는 게 단순하다
- 저장은 upsert (`POST` + `Prefer: resolution=merge-duplicates`)
- 폴링은 `updated_at` 만 받아본다 (수십 바이트). 바뀌었을 때만 전체를 받는다.
  **전체를 10초마다 받지 않는다** — 무료 플랜 전송량이 그것 때문에 샌다
- 갱신은 편집 중이 아닐 때만 한다 (`#modal` 이 열려 있으면 건너뜀)
- 권한은 서버의 RLS 가 정한다. 로그인 안 하면 조회만 된다
- **조회 요청은 `sbGet()` 으로 한다.** access token 은 한 시간쯤 지나면 만료되는데,
  그 토큰을 그대로 붙이면 401 이 오고 조회까지 막힌다. `sbGet()` 이 401 을 만나면
  갱신을 한 번 해보고, 그래도 안 되면 **토큰을 버리고 익명으로 다시 읽는다.**
  이 복구가 없으면 오래 켜 둔 PC 에서 "서버 연결 안 됨" 과 함께 자료가 통째로 사라진 것처럼 보인다.
  **`serverLoad`·`serverPoll` 에서 `sbFetch` 를 직접 부르지 않는다**
- **불러오기에 실패하면(`SERVER.on === false`) 저장하지 않는다.** `pushShared()` 가 막는다.
  화면에 있는 옛 자료가 서버의 최신 자료를 덮어쓰기 때문이다. 변경분은 `stashPending()` 으로 보관한다
- 연결 실패 사유는 `SERVER.err` 에 담아 저장 상태와 서버 카드에 함께 띄운다.
  "서버 연결 안 됨" 만 뜨면 무엇을 고쳐야 할지 알 수 없다
- **아티팩트(클로드 공유 링크) 안에서는 서버 모드로 넘어가지 않는다** (`IN_ARTIFACT`).
  그 환경은 CSP 로 외부 fetch 가 막혀 있어서, 서버 모드로 넘어가면 자료를 아예 못 보게 된다.
  대신 설정 카드에 안내를 띄운다. 이 분기를 없애지 않는다
- `SUPABASE_URL`/`SUPABASE_ANON_KEY` 를 코드에 넣어도 된다. 값은 **publishable key**
  (`sb_publishable_...`) 를 넣는다. 옛 `anon` 키(`eyJ...`)도 동작하지만 2026년 말 폐지 예정이다.
  **`sb_secret_` / `service_role` 키는 절대 코드에 넣지 않는다**
- **주소는 프로젝트 기본 주소만 넣는다** (`https://xxxx.supabase.co`).
  `sbFetch()` 와 `serverLogin()` 이 `/rest/v1/...`, `/auth/v1/...` 를 스스로 붙이기 때문에,
  대시보드에서 복사한 `.../rest/v1` 을 그대로 넣으면 `.../rest/v1/auth/v1/token` 이 되어
  로그인이 404 로 실패한다. `normalizeUrl()` 이 뒤에 붙은 경로를 떼어내며,
  저장할 때와 읽을 때 양쪽에서 부른다. **이 정규화를 빼지 않는다**
- **헤더 규칙에 주의.** publishable 키는 `Authorization: Bearer` 로 보내면 거부된다.
  `apikey` 헤더에만 싣고, `Authorization` 은 로그인한 사용자의 access token 이 있을 때만 붙인다
  (`sbFetch()` 참고). 이 규칙을 바꾸면 새 키에서 401 이 난다

설치 절차는 `서버설치안내.md` 참고.

### 저장 시점

- `save()` → 마지막 변경 후 `AUTOSAVE_MS`(2분) 조용하면 자동 저장. 변경이 또 오면 타이머를 민다.
  **길게 잡은 것은 의도된 것이다** — 무료 플랜 전송량을 아끼려고 그렇다.
  예약은 아래처럼 즉시 저장되므로, 이 타이머는 설정값 같은 것에만 걸린다
- `saveNow(silent)` → 저장 버튼 / `Ctrl+S`. 기다리지 않고 즉시.
  `silent` 를 주면 안내를 띄우지 않는다 — 자체 안내가 이미 나가는 곳에서 쓴다.
  **이벤트 리스너에 직접 넘기지 않는다** (`() => saveNow()` 로 감싼다).
  이벤트 객체가 `silent` 자리에 들어가 저장 버튼 안내가 사라진다
- **설정은 즉시 저장되지 않는다.** 요금 기준·계좌·담당자 이름은 `save()` 만 부르므로
  2분 뒤에 올라간다. 지금 올리고 싶으면 **`설정 저장` 단추**를 누른다 —
  `⚙ 설정` 창 아래와 `시설 · 요금 기준` 탭 윗줄 두 곳에 있고, 둘 다 `saveSettingsNow()` 를 부른다.
  누르면 포커스를 한 번 떼어(`blur`) 적던 값까지 반영한 뒤 `saveNow()` 를 부른다.
  담당자가 아니면 단추 자체를 화면에 넣지 않는다 (`body.no-admin`·`body.fee-only`)
- **`⚙ 설정` 창은 아랫줄이 붙박이다** (`#settingsBox>.modal` 이 세로 flex, 가운데만 굴린다).
  창이 길어서 붙박이가 아니면 `설정 저장` 이 화면 밖으로 밀린다.
  왼쪽에 지금 저장 상태(`#setSaveNote`)를 함께 적는다 — `setSaveState()` 가 같이 갱신한다
- **예약 등록·수정·삭제·복제는 `saveNow(true)` 로 바로 올린다.** 달력에 뜬 예약이
  다른 PC 에 안 보이는 시간이 없어야 한다
- 저장 중에 또 고치면, 그 저장이 끝난 뒤 이어서 한 번 더 올린다
- **변경이 없으면 저장하지 않는다.** 주기적으로 무조건 쓰면 이력이 쓰레기가 된다
- 저장 안 된 변경이 있으면 `beforeunload` 로 창 닫기를 막는다
- `save()` 는 어느 모드든 호출하는 단일 진입점이다. 새 기능에서도 이것만 부르면 된다
- 저장 실패 시 변경분을 `stashPending()` 으로 보관했다가 다음에 열 때 복구를 제안한다.
  **저장에 실패했다고 사용자 입력을 버리지 않는다**
- 예약을 지우는 동작(`#clearRes`·`#resetAll`)은 **`clearPending()` 을 함께 부른다.**
  안 부르면 지운 예약이 다음에 열 때 복구 안내로 되살아난다
- **되돌릴 수 없는 일은 `askConfirm()` 을 거친다** — 아래 「지우기 앞의 확인」 절 참고
- 내보내기는 모드에 따라 갈린다. 파일로 열었으면 파일 저장, 아티팩트에서는 복사창

### 지우기 앞의 확인 (`askConfirm`)

예약 전부 지우기·전체 초기화는 되돌릴 수 없다. `confirm()` 창만 띄우면 실수로 한 번
눌렀을 때 그대로 지워지므로, **한 번 더 막는다.**

| 어디서 쓰나 | 무엇을 묻나 |
|---|---|
| 서버에 로그인해 쓰는 자리 | **그 계정의 비밀번호를 다시 받아 서버에 물어본다** (`verifyPassword`) |
| 서버를 안 쓰는 자리 (내려받은 파일·아티팩트) | 정해진 낱말(`예약삭제`·`전체초기화`)을 그대로 치게 한다 |

- `verifyPassword()` 는 **지금 쓰고 있는 토큰을 건드리지 않는다** — 비밀번호가 틀렸을 때
  로그인이 풀리면 안 되기 때문이다. `serverLogin()` 과 달리 `SERVER` 를 고치지 않는다
- 비밀번호를 화면에 저장하거나 자료에 담지 않는다. 확인이 끝나면 입력칸을 비운다
- 취소·`Esc`·`닫기 ✕` 로 언제든 빠져나올 수 있고, 그때는 아무것도 지우지 않는다

## 디자인 규칙

사용자가 명시적으로 요청한 제약이다. 지켜야 한다.

- **색을 적게 쓴다.** 바탕은 흰색·검정 계열 회색조, 강조색 하나(`--accent`, 짙은 청회색),
  경고·중복에만 `--alert`(벽돌색). 여기에 **시설 구분색**과 아래 세 가지가 더해진다
- **시설 구분색** — 달력 칩과 우측 상세에서 어느 시설인지 한눈에 보이게 하는 용도다.
  `--fc-{orange|blue|green|none}-{soft|line|ink}` 세 벌로 정의하고, 요소에는 `data-fc="orange"`
  처럼 붙인다. 실제 색은 시설의 `color` 값이며 **설정 화면에서 고른다 — 이름으로 하드코딩하지 않는다**.
  기본값은 시설 순서대로 주황·파랑·초록 (충무체육관·한밭체육관·야구장)
- **요일·준비 색** (담당자 요청으로 더한 것) —
  `--sat` 토요일 파랑 · `--sun` 일요일·공휴일 빨강 · `--prep-line`/`--prep-soft` 준비 건 노랑.
  **토·일 색은 요일 글자와 날짜 숫자에만 쓴다.** 칸 전체를 물들이지 않는다
- **이 용도 밖으로 색을 늘리지 않는다.** 새 색이 필요하면 팔레트에 한 벌을 더하고
  세 테마 블록(`:root`, 미디어쿼리, `[data-theme="dark"]`)에 **모두** 정의한다
- **달력 칸 바탕은 전부 흰색이다.** 이번 달이 아닌 날(`.cell.out`)만 회색이다
- **상태(신청·승인)는 화면에 나타내지 않는다.** 칩 테두리는 전부 실선이고,
  취소만 흐리게+취소선, 중복만 `--alert` 테두리다. 상태 뱃지도 목록의 상태 열도 없앴다
- 우측 상세는 **행사=시설색 바탕, 준비=흰 바탕.** 행사가 주요 업무이기 때문이다
- 메모는 `--accent` 로 테두리를 두른 상자에 담아 일정 밑에 붙인다 (`.item-memo`).
  준비 강조(노랑)와 뜻이 겹치지 않게 일부러 다른 색을 썼다
- 폰트: 본문 IBM Plex Sans KR, 숫자·시각·금액은 IBM Plex Mono (`.mono`, 자릿수 정렬)
- 색은 전부 `:root` 의 CSS 변수로 정의한다. 밝게/어둡게 두 테마가 모두 동작해야 한다.
  **미디어쿼리나 `[data-theme]` 블록 안에서만 정의되는 색을 만들지 않는다**
- 인쇄 스타일은 `body.print-cal / print-list / print-detail` 로 갈린다. 위 인쇄 절 참고
- **시각 입력은 24시간제 시·분 `<select>` 두 개다** (`fillTimeSelects` · `TIME_FIELDS`).
  등록창의 시작·종료뿐 아니라 **설정 화면의 별표 1의2 시간대도 같은 방식**을 쓴다.
  칸이 좁으면 글자가 잘리는 대신 두 줄로 접힌다.
  `<input type="time">` 은 브라우저 지역 설정에 따라 오전/오후로 나오는데 코드로 막을 수 없고,
  담당자가 헷갈린다고 해서 바꿨다. **`type="time"` 으로 되돌리지 않는다.**
  값은 예전과 같은 `"HH:MM"` 문자열이고, 분은 `TIME_MIN_STEP`(10분) 단위다.
  **종료 시각의 기본값은 18:00 이다**
- **등록창은 바깥을 눌렀다고 닫히지 않는다.** 글자를 끌어 고르다 손을 창 밖에서 떼면
  그것도 바깥 클릭으로 잡혀서 적던 내용이 통째로 날아갔다(‘튕김’).
  닫기는 `닫기 ✕`·`취소`·`Esc` 로만 한다 — **바깥 클릭 닫기를 되살리지 않는다**

## 아직 정해지지 않은 것

- **준비시간 요율**: 조례에 근거가 없다. 담당자 확인으로 **기본액의 100%**(= 행사와 같은 금액)로
  두었다. 요일·시간대 가산도 함께 붙는다. 내부 기준이 달라지면 설정 화면에서 바꾼다
- **1회 기준 시간**: 별표 2 가 충무·한밭체육관·한밭야구장에는 시간을 정하지 않아 0(제한 없음)으로 두었다.
  실무상 허가시간이 있다면 시설 설정에 넣어야 초과사용료가 계산된다
- **일요일**: 별표 2 비고는 "토요일ㆍ공휴일" 이라고만 적었다. 일요일을 공휴일로 보아 가산 대상에
  넣었다 — 담당자 엑셀도 "토,일/공휴일" 로 묶어 쓰고 있었다 (설정에서 끌 수 있음)
- **공휴일 표**: 2026~2040년까지 넣어 두었다. 대체공휴일·임시공휴일은 손으로 넣는다.
  2041년 이후를 쓰게 되면 `LUNAR_HOLIDAYS` 에 줄을 더한다
- **야간·조기 반영 방식**: 조례 문언은 "기본액의 50% 가산" 인데 실무는 당일 주간 사용료에 1.5를
  곱해 왔다. 실무를 기본값으로 두었으나 시 해석을 확인해야 한다 (`surchargeMode`)
- **여러 시간대에 걸친 사용**: 걸치는 시간대마다 1회분을 매겨 합산한다(담당자 확인).
  제10조제2항을 근거로 삼았으나, 시 해석을 문서로 받아두면 좋다
- **배포**: GitHub Pages, `Deploy from a branch` 방식(워크플로 파일 없음). 절차는 `배포안내.md`.
  `SUPABASE_URL`·`SUPABASE_ANON_KEY` 는 `index.html` 에 박아 넣었다 — 각자 입력할 필요가 없다.
  저장소가 공개여도 publishable 키는 안전하지만,
  **신규 가입 차단(설치안내 3단계)이 되어 있어야만** 그렇다

## 변경 후 확인할 것

자동화된 테스트가 없다. 브라우저에서 파일을 열고 직접 본다.

**기본**

- [ ] 콘솔에 에러가 없다 (Google Fonts 연결 실패는 무시)
- [ ] 달력이 그려지고, 날짜를 누르면 오른쪽에 그 날 예약이 나온다
- [ ] 예약 등록 → 달력에 칩이 뜨고 → 새로고침해도 남아 있다
- [ ] 같은 시설·같은 날 시간이 겹치게 등록하면 중복 경고가 뜬다
- [ ] 시설 고르는 차례가 충무체육관 → 한밭체육관 → 한밭야구장 이다
- [ ] 밝게/어둡게 두 테마에서 글자가 다 읽힌다 (우측 상단 ◐)
- [ ] `⚙ 설정` 맨 아래에 판 번호(`APP_VERSION`)가 보인다 — 안내받은 번호와 다르면 옛 파일이다
- [ ] 왼쪽 위 제목을 누르면 **첫 화면으로 새로고침된다** (서버 자료를 다시 받는다)
- [ ] 설정 화면의 시간대가 `05 시 00 분` 처럼 24시간제로 나온다 (AM/PM 아님)

**등록창**

- [ ] 등록창 바깥을 눌러도, 글자를 끌어 고르다 밖에서 손을 떼도 창이 닫히지 않는다
- [ ] 등록창에 `준비시간 함께 등록`·`산정 이용료`·`상태`·**`관람료 수입`** 이 없다
- [ ] 관람료를 적어둔 예전 예약을 고쳐 저장해도 그 금액이 그대로 남는다
- [ ] 종료 시각이 18:00 으로 열린다
- [ ] 야구장을 고르면 "준비" 선택지가 막힌다

**이용료 (조례대로 나오는지)**

- [ ] 충무체육관 평일 주간 체육경기 = 90,000원, 체육경기 외 = 200,000원
- [ ] 토요일로 바꾸면 135,000원, 야간(19시 이후)까지 걸치면 202,500원
- [ ] 토요일 조기(하계 05:00–08:00)는 94,500원 — 당일 주간 135,000 × 0.7
- [ ] 하계 05:00–08:00 은 63,000원(조기), 10:00–13:00 은 90,000원(주간), 19:00–22:00 은 135,000원(야간)
- [ ] 하계 07:00–12:00(조기+주간)은 153,000원, 07:00–23:00(세 시간대)은 288,000원
- [ ] 토요일 07:00–23:00 은 432,000원 (94,500 + 135,000 + 202,500)
- [ ] 동계(1월) 18:30 시작은 야간, 하계(8월) 18:30 시작은 주간
- [ ] 감면 유형은 해당 없음·전부 면제·80% 감경·50% 감경 네 개뿐이다
- [ ] 감면 유형을 고르면 자동으로 빠진다 (80% 고르면 90,000 → 18,000)
- [ ] 감면이 부속시설에는 **늘** 적용된다 (40,000원짜리에 80% 감면이면 8,000원)
- [ ] `시설 · 요금 기준` 탭에 `부속시설에도 감면 적용` **체크가 없다** (안내 글만 있다)
- [ ] 금요일 주간 충무 체육경기 + 집중조명 + 전기 86,580 + 수도 20,880 에
      감면 0/50/80/100% 를 걸면 **부속시설만 줄고 공과금은 107,460원 그대로**다
      (합계 217,460 / 162,460 / 129,460 / 107,460)
- [ ] **감면을 골라도 전기료·수도료는 적은 금액 그대로다** (90,000 + 전기 86,580 + 수도 20,880 에
      80% 감면이면 18,000 + 86,580 + 20,880 = 125,460원)
- [ ] 산정 상세의 ⑨ 감면 줄에 `전기료·수도료(실비)는 감면 없음` 이 보인다
- [ ] 전기료·수도료를 직접 입력하면 그대로 더해진다 (자동 계산하지 않는다)
- [ ] `부속시설 고르기` 를 누르면 별표 6 항목이 펼쳐지고, 수량을 넣어도 그 칸이 화면에서 밀리지 않는다
- [ ] 등록창의 부속시설 합계 밑에 **감면 후 금액 안내**가 뜬다
      (감면 유형을 고른 경우만. `해당 없음` 이면 안내가 없다)
- [ ] 부속시설 목록에 `마이크시설·확성기 기본료` 가 없고 **`마이크` 하나뿐이다**
- [ ] 마이크 1개를 넣으면 부속시설 합계가 **35,000원**(기본료 30,000 + 5,000), 2개면 40,000원이다
- [ ] `산정 상세` 에 `⑥ 부속시설 · 마이크 기본료 1회` 와 `⑥ 부속시설 · 마이크 2개` 두 줄이 서고,
      기본료 줄의 근거가 `별표 6 · 마이크시설·확성기 기본료 (1회)` 다
- [ ] 예전 자료(마이크시설 기본료 1 + 마이크 2)를 열면 마이크 2개로 바뀌고 금액이 그대로 40,000원이다
- [ ] 부속시설 목록에 `권투링`·`인조잔디 관수료`·`롤러스케이트 대여`·`빙상스케이트 대여` 가 **없다**
- [ ] 그중 하나를 쓰던 예약이 있으면 그 항목만 목록에 남고 금액도 그대로다
- [ ] 부속시설 목록에 `플래카드 60,000원 / 1회`·`중계방송료 81,000원 / 1회`·`입장료 100원 / 명` 이 있다
- [ ] 입장료 칸에 800 을 넣으면 `⑥ 부속시설 · 입장료 800명 = 80,000원` 이 된다
- [ ] 세 항목에 감면이 적용된다 (80% 감면이면 1/5 로 줄어든다)
- [ ] 산정 상세의 세 항목 근거에 `담당자 확인 금액 (조례 조문 미확인)` 이 보인다
- [ ] 설정 화면 부속시설 항목의 단위 고르기에 `1회·1일·개·명` 네 가지가 있다
- [ ] 부속시설 차례가 **집중조명 · 마이크 · 전광판 · 냉난방 · 바닥깔이 · 탁자 · 의자** 다
      (설정 화면·등록창 둘 다. 조명타워·플래카드·중계방송료·입장료가 그 뒤에 온다)
- [ ] 「개」·「명」 항목에 `수량`·`횟수` 두 칸이, 「1회」·「1일」 항목에 `횟수` 한 칸이 있다
- [ ] 마이크 2개 · 횟수 3 을 넣으면 기본료 90,000 + 마이크 30,000 = **120,000원** 이다
- [ ] 횟수를 비워 두면 예전과 값이 같다 (마이크 2개 = 40,000원)
- [ ] 산정 상세에 `⑥ 부속시설 · 마이크 2개 × 3회` 처럼 횟수가 함께 나온다

**우측 상세 · 요약**

- [ ] `산정 상세` 를 누르면 근거 조문(`why`)과 함께 내역이 나온다
- [ ] 산정 내역에 `＝ 당일 주간 전용사용료` 소계가 보인다
- [ ] 메모를 적은 예약은 일정 바로 밑에 강조된 상자로 메모가 보인다
- [ ] 요약에 `승인 대기` 가 없고 `공과금 (전기+수도)` 가 있다

**달력**

- [ ] 날짜를 누르고 `확인` 을 누르면 그 칸 우측 아래에 ✓ 가 붙고, 목록 맨 왼쪽에도 ✓ 가 붙는다.
      다시 누르면 풀린다. ✓ 는 빨간색이다
- [ ] 칸 바탕이 모두 흰색이고, 앞뒤 달 날짜만 회색이다
- [ ] 토요일은 `토` 와 날짜 숫자가 파랑, 일요일은 빨강이다
- [ ] 공휴일(예: 2026-08-15 광복절, 2026-09-25 추석)에 이름이 뜨고 숫자가 빨강이다
- [ ] 아무 날이나 눌러 `공휴일 지정` 을 누르면 공휴일이 되고 이용료에 가산이 붙는다. 다시 누르면 풀린다
- [ ] 칩 테두리가 상태와 상관없이 전부 실선이다 (신청도 파선이 아니다)
- [ ] 날짜 옆에 행사·준비 건수가 나뉘어 보인다
- [ ] 달력·우측 상세에서 시설별 색이 구분된다 (충무=주황, 한밭=파랑, 야구장=초록)
- [ ] 칩 아랫줄에 **이용 목적**이 나온다. 목적을 안 적었으면 신청 단체가 나온다
- [ ] 긴 행사명도 잘리지 않고 줄이 바뀐다 (화면·인쇄 모두)
- [ ] 목록·요금 관리의 요일 글씨가 토=파랑, 일·공휴일=빨강이다
- [ ] 한 날에 여러 건이 있으면 충무 → 한밭체육관 → 한밭야구장 차례로 보인다 (시간순 아님)

**설정 · 권한**

- [ ] 윗줄 `⚙ 설정` 로 창이 열리고, 로그인·데이터 관리·**정산서 입금 계좌**가 거기 있다
- [ ] `시설 · 요금 기준` 탭 윗줄 오른쪽 끝과 `⚙ 설정` 창 아래에 `설정 저장` 단추가 있다
- [ ] 설정을 고치고 `설정 저장` 을 누르면 2분을 기다리지 않고 바로 저장된다
- [ ] 고친 것이 없을 때 누르면 `저장할 변경이 없습니다` 가 뜬다
- [ ] `⚙ 설정` 창을 아래로 굴려도 `설정 저장` 단추가 늘 보인다
- [ ] 담당자가 아니면 `설정 저장` 단추가 보이지 않는다
- [ ] `시설 · 요금 기준` 탭 윗줄에서 세 묶음을 전환할 수 있고, 한 번에 하나만 보인다
- [ ] 시설·부속시설·감면 목록이 옆으로 늘어서서 화면이 짧아졌다
- [ ] 로그인하지 않은 사람에게는 요금 담당 계정·데이터 관리 칸이 **아예 없다**
      (개발자도구로 봐도 이메일이 안 보인다)
- [ ] 로그인하지 않으면 `요금 관리`·`시설 · 요금 기준` 탭이 없고, 달력 요약에 금액이 없다
- [ ] 로그인하지 않으면 **달력 부제가 `행사 N건 · 준비 N건` 뿐이다** (월간 이용료가 없다)
- [ ] 로그인하지 않고 `날짜별 상세로 인쇄` 를 하면 **금액 열도 날짜별 이용료도 없다**
- [ ] 로그인하지 않으면 우측 상세에 금액·`산정 상세`·`수정`·`정산서` 가 없다
- [ ] 로그인하지 않으면 목록이 `조회용` 뿐이고 `수익집계용` 단추가 없다
- [ ] 로그인하지 않은 화면을 개발자도구로 봐도 **금액이 소스에 없다**
- [ ] 로그인하면 위의 것이 모두 다시 보인다
- [ ] `예약만 전부 지우기`·`전체 데이터 초기화` 를 누르면 **비밀번호를 다시 묻는다**
- [ ] 비밀번호를 틀리게 넣으면 "맞지 않습니다" 가 뜨고 **아무것도 지워지지 않는다**
- [ ] 틀린 비밀번호를 넣어도 **로그인이 풀리지 않는다**
- [ ] 취소·`Esc` 로 닫으면 아무것도 지워지지 않는다
- [ ] 내려받은 파일(서버 없이)에서는 `예약삭제`·`전체초기화` 낱말을 치게 한다
- [ ] 로그인하지 않아도 로그인 칸은 쓸 수 있다
- [ ] 담당자가 아니면 `시설 · 요금 기준` 탭이 잠기고, 위에 조회 전용 안내가 뜬다
- [ ] 요금 기준은 잠겨 있어도 **볼 수는 있다** (이 도구의 목적이다)

**목록**

- [ ] 목록에 `취소` 거르기 선택창이 없다
- [ ] `조회용` 에는 메모가 보이고 감면·이용료·상태 열이 없다. 위쪽 요약도 건수만 나온다
- [ ] `수익집계용` 에는 이용료·부속시설·공과금·합계가 따로 보이고 메모·목적·상태 열이 없다
- [ ] 각 줄의 이용료 + 부속시설 + 공과금 = 합계 다
- [ ] `전기·수도 따로` 를 누르면 공과금 열이 전기료·수도료 두 칸으로 갈리고,
      전기 + 수도 = 공과금 이다. 엑셀·CSV·인쇄도 따라간다
- [ ] `조회용` 에서는 공과금 전환 버튼이 보이지 않는다
- [ ] 목록의 메모와 시작 시각이 붉은 글씨로 보인다 (끝 시각은 보통 글씨)
- [ ] 목록 인쇄·엑셀·CSV 의 시간 칸은 `09:00–18:00` 글자 그대로다

**요금 관리**

- [ ] `요금 관리` 탭에 전기료·수도료가 따로 보인다
- [ ] 줄의 `산정 상세` 를 누르면 근거 조문과 함께 내역이 펼쳐진다
- [ ] 전기료·수도료 칸 안에도 체크가 있고, 이용료 입금과 따로 켜진다
- [ ] 0원인 전기료·수도료 칸에는 체크가 없다
- [ ] 셋을 다 켜야 줄에 색이 들어가고 `입금 완료만` 에 걸린다
- [ ] 위쪽 요약의 입금 금액이 켠 항목만큼만 늘어난다
- [ ] 새로고침해도 입금 표시가 남아 있다. 예약을 수정해도 지워지지 않는다
- [ ] `미입금만`·`입금 완료만` 으로 걸러진다
- [ ] 설정에 요금 담당 계정을 넣고 그 계정으로 로그인하면
      `요금 담당` 뱃지가 뜨고 예약 등록·수정과 설정이 잠긴다. 입금 체크는 된다
- [ ] 로그아웃하면 입금 체크가 잠긴다 (조회는 된다)
- [ ] 맨 아래 합계 줄의 금액이 위 숫자들의 합과 맞는다
- [ ] 준비 건 줄은 노란 테두리만 있고 바탕색은 다른 줄과 같다
- [ ] 구분 열에 `준비` 가 뱃지 없이 글씨로만 나온다
- [ ] 맨 아래 `N건 합계` 가 한 줄로 눕는다 (세로로 접히지 않는다)

**요금 계산**

아래 값은 등록창으로 같은 조건을 넣었을 때와 **한 원도 다르지 않아야 한다.**
(2026-09-21 브라우저로 여덟 가지를 확인했다. 조례를 고쳤으면 다시 확인한다)

- [ ] 윗줄에 `요금 계산` 탭이 있고, 열면 오늘 날짜·09:00~18:00 로 시작한다
- [ ] 충무체육관 평일 주간(2026-09-22 10:00–13:00) 체육경기 = **90,000원**, 체육경기 외 = **200,000원**
- [ ] 토요일(2026-09-12)로 바꾸면 **135,000원**, 야간(19:00–22:00)이면 **202,500원**
- [ ] 평일 07:00–23:00 은 **288,000원**, 토요일 07:00–23:00 은 **432,000원**
- [ ] 감면 `80% 감경` 을 고르면 90,000 → **18,000원**
- [ ] 추석(2026-09-25)을 고르면 날짜 밑에 `금요일 · 추석` 이 뜨고 **135,000원**이 된다
- [ ] 금액 아래에 **근거 조문이 그대로 나온다** (`별표 2`·`＝ 당일 주간 전용사용료` 까지)
- [ ] 맨 아래에 **부속시설·전기·수도가 빠졌다는 안내**가 보인다
- [ ] 야구장을 고르면 `준비` 가 막히고 `한밭야구장은 준비시간 없음` 이 뜬다
- [ ] 종료를 시작보다 이르게 두면 **붉은 경고**가 뜬다
- [ ] 여기서 무엇을 고쳐도 **달력·목록의 예약이 바뀌지 않는다** (저장하지 않는다)
- [ ] 로그인하지 않으면 **탭이 없고**, 개발자도구로 `setPane("quote")` 를 불러도 달력으로 되돌아간다
- [ ] 로그인하지 않은 화면을 개발자도구로 봐도 **계산 결과가 소스에 없다**
- [ ] 밝게/어둡게 두 테마에서 금액과 근거가 다 읽힌다

**AI 도우미**

설치가 안 돼 있으면 **단추가 안 보이는 것이 정상**이다 (`AI설치안내.md`).

- [ ] 키·함수를 올리기 전에는 `AI 도우미` 단추와 `쉬운 말로 설명` 이 **안 보인다**
- [ ] 그 상태에서도 달력·요금 계산·저장이 지금 그대로 된다
- [ ] 로그인하지 않으면 단추가 없고, 개발자도구로 `openAi()` 를 불러도 창이 안 열린다
- [ ] 설치한 뒤 로그인하면 단추가 생기고, 예시 질문을 누르면 답이 온다
- [ ] 우측 상세의 `쉬운 말로 설명` 을 누르면 그 건의 금액이 쉬운 말로 풀려 나오고 **근거 조문이 함께** 나온다
- [ ] **`10월 태권도협회 대관 내역 알려줘` 로 그 달의 해당 건이 날짜 차례로 나온다**
- [ ] 창 위에 `예약 N건 (범위) · 금액 포함 · 메모 제외` 가 보인다. 범위를 바꾸면 따라 바뀐다
- [ ] 개발자도구 네트워크에서 `ask` 요청을 열면 **단체명·목적·금액은 있고 메모는 없다**
- [ ] `메모도 보내기` 를 켜면 메모가 들어가고, **그 안의 전화번호는 `[연락처 지움]` 으로 바뀐다**
- [ ] 같은 요청에 **키가 들어 있지 않다** (브라우저에는 키가 없다)
- [ ] 「이렇게 쓰면 얼마인가요」 처럼 **새 금액을 묻는 질문**에는 계산하지 않고 「요금 계산」 탭으로 안내한다
- [ ] 이미 등록된 건의 금액을 물으면 **대장에 적힌 그 금액을 그대로** 말한다
- [ ] 범위를 `이 달` 로 두고 다음 달을 물으면 **「없습니다」 가 아니라 「범위를 넓혀 보세요」** 라고 답한다
- [ ] 예약을 여러 건 물으면 **글자가 아니라 진짜 표로** 나온다 (테두리가 있고 금액이 오른쪽에 붙는다)
- [ ] 긴 행사명이 든 표도 **오른쪽이 잘리지 않는다**
- [ ] 표가 든 답에만 우측 위에 **`⤓ 엑셀`** 이 붙는다. 표 없는 답과 내 질문에는 없다
- [ ] 그 단추를 누르면 **그 답의 표만** `.xlsx` 로 떨어진다 (다른 답은 안 들어간다)
- [ ] 받은 파일을 엑셀에서 열면 **확장명 경고가 없고**, 금액 칸을 드래그하면 **합계가 잡힌다**
- [ ] 날짜가 `46260` 같은 숫자로 바뀌지 않는다
- [ ] 첫 줄에 제목, 둘째 줄에 **물어본 질문**이 적혀 있다
- [ ] 키를 일부러 지우면 붉은 말풍선으로 **무엇이 잘못됐는지** 나온다 (그냥 "실패" 가 아니다)
- [ ] 로그아웃하면 단추가 사라지고 열려 있던 창도 닫힌다
- [ ] 창을 닫았다 열면 **주고받은 말이 남아 있지 않다** (저장하지 않는다)
- [ ] 새로고침해도 예약 자료에 AI 가 한 말이 섞여 있지 않다

**분석**

- [ ] 윗줄에 `분석` 탭이 있고, 시설 셋의 그래프가 **각각 따로** 나온다
- [ ] 세 그래프의 **세로 눈금이 같다** (높이를 견줄 수 있다)
- [ ] 아무 그래프에나 마우스를 올리면 **그 달의 세 시설 값이 한꺼번에** 뜨고 세로선이 선다
- [ ] 위 요약 카드에 대관 횟수·수익금·인원이 **한꺼번에** 보인다
- [ ] `수익금` 이 **전기료·수도료를 뺀 금액**이다 (목록 수익집계용의 이용료+부속시설 합과 맞는다)
- [ ] `올해`·`최근 12개월`·`전체 기간` 과 `행사만`·`준비만` 이 걸러진다
- [ ] 달 이름이 겹쳐 찍히지 않는다 (`11월 12월` 이 포개지지 않는다)
- [ ] 아래 표의 합계가 요약 카드 숫자와 맞는다
- [ ] 밝게/어둡게 두 테마에서 선·눈금·글자가 다 읽히고, **세그먼트 단추가 흰 상자로 깨지지 않는다**
- [ ] 로그인하지 않으면 탭이 없고, 개발자도구로 열어도 그래프·표가 그려지지 않는다
- [ ] 세로 눈금이 `0 1 2 3 4` 처럼 **고르게** 찍힌다 (`0 1 3 4 5` 로 건너뛰지 않는다)
- [ ] `한눈에` 를 누르면 세 시설이 한 판에 나오고, **선 모양·점 모양·선 끝 이름**이 다 다르다
- [ ] 범례에도 같은 선·점 모양이 그려진다 (색 동그라미만 있지 않다)
- [ ] 선 끝 이름이 서로 겹치지 않고, **값이 낮은 시설이 아래**에 온다
- [ ] 「한눈에」 판에서도 마우스를 올리면 크로스헤어가 **그 달 자리에** 서고 값이 맞는다
- [ ] 표에 세로줄이 없고 `2026년 1월` 처럼 적히며, 칸 뒤에 옅은 막대가 깔린다
- [ ] 줄에 마우스를 올려도 **막대가 사라지지 않는다**
- [ ] 0 인 달이 `–` 로 흐리게 나온다

**엑셀 가져오기**

- [ ] `⚙ 설정 → 데이터 관리` 에 `엑셀·CSV 로 예약 가져오기` 가 있다 (담당자만 보인다)
- [ ] 로그인하지 않거나 요금 담당이면 창이 열리지 않고, 코드로 `impApply()` 를 불러도 안 들어간다
- [ ] 아무 양식의 `.xlsx` 를 올리면 시트·머리글 줄이 잡히고 열이 **저절로 맞춰진다**
- [ ] 머리글이 `월일` 처럼 낯선 말이어도 **값을 보고** 날짜 열을 찾는다
- [ ] 시트 이름에 연도가 있으면 **기준 연도**에 그 값이 들어간다 (`3.5` → `2019-03-05`)
- [ ] `09:00~18:00` 한 칸도, 시작·종료 두 칸도 읽는다
- [ ] `담당자 연락처` 같은 열은 **고를 수 없고**, 「가져오지 않는 열」 로 화면에 뜬다
- [ ] 비고에 적힌 전화번호·주민번호가 `[연락처 지움]`·`[주민번호 지움]` 으로 바뀐다
- [ ] 모르는 시설 이름은 「시설 이름 맞추기」 에 모이고, 고르면 그 줄이 들어온다
- [ ] 고르지 않은 줄은 이유와 함께 **건너뜀**으로 나온다
- [ ] 감면 `80%` 가 `80% 감경` 으로 들어간다 (`해당 없음` 이 되지 않는다)
- [ ] 「구분」 칸에 `준비` 라고 적힌 줄이 준비 건으로 들어간다.
      야구장이면 「준비시간 없어 행사로 넣습니다」 안내가 뜬다
- [ ] 엑셀 금액 열을 고르면 **조례 계산과 몇 건 다른지** 안내가 뜬다
- [ ] 기본값(`조례대로 다시 계산`)으로 넣으면 `feeOverride` 가 비어 있다
- [ ] 같은 파일을 다시 올리면 **전부 「이미 있어 건너뜀」** 이 되고 단추가 잠긴다
- [ ] `확인` 을 누르기 전에는 달력·목록이 바뀌지 않는다
- [ ] 가져온 뒤 새로고침해도 남아 있다 (서버에 바로 올라간다)
- [ ] cp949 로 저장된 CSV 의 한글이 깨지지 않는다
- [ ] `.xls` 를 올리면 「.xlsx 로 저장해 올리라」 는 안내가 뜬다
- [ ] AI 가 꺼져 있으면 `AI 로 열 맞추기` 단추가 **안 보이고**, 그래도 손으로 다 된다

**인쇄 · 내보내기**

- [ ] `인쇄…` → `달력으로 인쇄` 미리보기에서 가로 용지에 칸이 크고 글씨가 읽힌다
- [ ] 행사 달력과 준비 달력이 **따로 한 장씩** 나온다 (`행사만`·`준비만` 도 고를 수 있다)
- [ ] 인쇄 창의 `인원수` 를 `인원수도 함께 인쇄` 로 고르면 칩의 이용 목적 뒤에 `120명` 이 붙는다.
      `인원수 없이 인쇄`(기본) 로 두면 예전과 똑같이 나온다. 인원을 안 적은 건에는 아무것도 안 붙는다
- [ ] 인원수를 넣어도 행사명이 잘리지 않는다 (아랫줄이라 줄이 바뀐다)
- [ ] 한 달이 한 장에 들어간다. 행사가 60건 넘게 많은 달도 마찬가지다
      (글씨가 조금 작아질 뿐 빠지는 행사가 없어야 한다)
- [ ] 인쇄판에 앞뒤 달 날짜가 나오지 않는다 (칸은 비어 있고 요일 자리는 그대로)
- [ ] 요일 머리글 바로 아래에 가로줄이 있다 (줄이 하나 빠지지 않는다)
- [ ] 한 칸에 5건 넘게 있는 날도 `+N건 더` 없이 다 보인다
- [ ] 확인 표시한 날짜는 인쇄판에도 ✓ 가 찍힌다
- [ ] `날짜별 상세로 인쇄` 에서 날짜를 고르면 그 날짜만, 메모까지 찍힌다
- [ ] `목록으로 인쇄` 는 지금 고른 목록 종류·거르기 조건 그대로 나온다
- [ ] `이 달 엑셀` 로 받은 파일을 엑셀에서 열면 같은 날짜의 날짜·요일이 합쳐져 있고,
      열 너비가 값에 맞고, 준비 건 줄이 노랗게 칠해져 있다
- [ ] `이 달 CSV` 는 같은 날짜의 날짜·요일이 비어 있고 준비 건에 `[준비]` 가 붙는다
- [ ] 목록 엑셀·CSV 에 `정산` 체크 칸이 끼어들지 않는다 (열 수가 예전 그대로다)
- [ ] 목록 엑셀 파일 이름이 `.xlsx` 이고 **확장명 경고가 뜨지 않는다**
- [ ] 목록 엑셀에 **완료 칸이 없다**
- [ ] 목록 엑셀의 시작 시각과 메모가 붉은 굵은 글씨다 (끝 시각은 보통 글씨)
- [ ] 목록 엑셀의 글자가 14pt 이고 열 너비가 그에 맞게 넓다 (글이 잘리지 않는다)
- [ ] 목록 엑셀의 요일 글씨가 토=파랑(`0070C0`), 일·공휴일=빨강(`FF0000`) 이다
- [ ] 목록 엑셀이 가로 용지에 한 폭으로 들어간다
- [ ] 인원 칸에 **초록 세모(숫자가 텍스트로 저장됨)가 없다** — 숫자로 들어간다
- [ ] 날짜가 바뀌는 자리마다 **굵은 가로줄**이 표 전체 폭으로 그어진다 (날짜·요일 칸까지)
- [ ] 준비 건 줄이 엑셀에서 **연한 파랑**으로 칠해져 한눈에 띈다 (화면·인쇄는 테두리 그대로)
- [ ] 토·일·공휴일 줄이 엑셀에서 **연한 금색**으로 칠해지고, 날짜·요일 칸까지 이어진다
- [ ] 토요일 준비 건은 **파랑**이다 (준비가 이긴다)

**정산서**

- [ ] ⑤부속시설에 `마이크 기본료 30,000원 1회` 와 `마이크 5,000원 2개` 가 따로 들어간다
- [ ] ⑤부속시설 일곱 줄이 **늘 집중조명·마이크시설·전광판·냉난방·바닥깔이·탁자·의자 차례**다
- [ ] 안 쓴 줄도 이름이 찍히고 단가·수량·금액이 **0원**이다
- [ ] 마이크 2개를 쓰면 ⑤에 `마이크시설 40,000원 × 감면율 × 1회` **한 줄**로 나온다
      (`마이크 기본료` 줄이 따로 서지 않는다)
- [ ] ⑤부속시설에 `플래카드`·`중계방송료`·`입장료` 가 **없다**
- [ ] ②입장료 칸에 단가 100원·입장인원 800명이 들어가고 딱지가 `일반(입장인원x100원)` 이다
- [ ] ③중계방송료 칸에 81,000원이, ④상행위 칸에 플래카드 60,000원이 들어간다
- [ ] ②③④의 계 칸이 수식이라 단가를 손으로 고치면 금액이 따라 바뀐다
- [ ] 조명타워처럼 서식에 자리가 없는 항목을 쓰면 「빠졌습니다」 안내가 뜬다
- [ ] `마이크시설·확성기 기본료` 처럼 **깨알만 하게 줄어든 글자가 없다**
- [ ] `※ 입금전 계좌번호 확인 필수(금액별로 해당 계좌로 입금)` 가 끝까지 다 보인다
- [ ] `④상행위사용료`·`추가전용사용료` 가 잘리지 않는다
- [ ] `⑦소계(징수대상총액)`·`⑧전용사용료 기납부액` 의 아랫줄이 잘리지 않는다 (두 장 모두)
- [ ] 장소·대관자·대관담당자를 길게 적어도 잘리지 않는다
- [ ] ①관람료 수입 칸이 0 이고 `#VALUE!` 가 나지 않는다
- [ ] **감면을 「해당 없음」(0%) 으로 두고 뽑아도 `###` 이 뜨는 칸이 없다**
      (감면율 칸이 `100%` 로 늘고 금액이 다섯 배가 된다)
- [ ] ⑤부속시설의 회수 칸에 횟수가, 개수 칸에 수량이 따로 들어간다
      (탁자 5개 × 2회면 개수 5 · 회수 2)

- [ ] 목록 맨 왼쪽 `정산` 에 체크하거나, 달력에서 날짜를 누르고 우측 상세의 `정산서` 에 체크하면
      단추 글씨가 `정산서 N건` 으로 바뀐다
- [ ] 아무것도 안 고르고 누르면 "먼저 체크하세요" 안내가 뜬다
- [ ] 엑셀에서 열 때 **"파일 형식과 확장명이 일치하지 않습니다" 경고가 뜨지 않는다** (`.xlsx` 다)
- [ ] 칸 크기·테두리·배경색이 원본 서식과 같다 (시설사용료 하늘색, 공과금 노랑).
      허가번호만 흰 바탕이다
- [ ] 서식 밖이 회색으로 눌려 정산서만 또렷하게 보인다 (인쇄 영역 `A1:N81`)
- [ ] `예금주:…`·`의거 아래금액을…`·계좌번호가 서식 안에 다 들어간다 (잘리지 않는다)
- [ ] `#VALUE!` 나 `46260` 같은 이상한 값이 없다
- [ ] 받은 파일을 인쇄하면 **위가 보관용, 아래가 제출용** 두 장으로 나온다
- [ ] 장소·행사명·대관자가 자동으로 채워져 있다
- [ ] 이틀에 걸친 행사를 함께 고르면 일자가 `8.28~8.29` 로 나온다 (한 파일)
- [ ] 장소·신청 단체·이용 목적 중 하나라도 다른 건을 함께 고르면 **파일이 따로 나온다**
      (이름 앞에 장소가 붙는다)
- [ ] **⑥전용사용료와 허가일자 칸이 비어 있다** (손으로 적는 칸)
- [ ] 전용사용료를 손으로 적으면 ⑦소계·⑧기납부액·금회징수액이 저절로 바뀐다
- [ ] ⑤부속시설에 쓴 항목과 수량이 들어가 있고 금액이 `단가×감면율×회수` 로 계산된다
- [ ] 감면 50% 인 건은 딱지도 `감면율(50%)` 로 나온다 (80% 로 고정돼 있지 않다)
- [ ] 전기료·수도료가 공과금 칸에 들어가 있다
- [ ] 설정의 `정산서 입금 계좌` 에 넣은 값이 시설사용료·공과금 칸 옆에 `은행:계좌번호` 로 찍힌다
- [ ] 첫 장 우측 아래에 통제 도장이 **원본과 같은 자리·크기**로 찍힌다 (월·일은 빈칸)
- [ ] 가운데 줄이 **올해 연도**다
- [ ] 크게 확대해도 글자와 원이 또렷하다 (936px 로 그려 넣는다)
- [ ] `⚙ 설정 → 통제 도장` 에 지금 쓰는 도장이 보이고, 그림을 올리면 그것이 들어간다
- [ ] `기본 그림으로 되돌리기` 를 누르면 다시 그린 도장으로 돌아간다
- [ ] **허가번호 칸이 흰 바탕이다** (공과금 칸은 노랑 그대로)
- [ ] 설정에서 고른 **대관담당자 이름**이 들어가 있다
- [ ] 정산서를 내고 나면 달력·목록의 체크가 모두 풀린다
- [ ] 부속시설의 「개」 로 세는 항목(의자·탁자)이 아래 두 줄에 들어가고 개수가 두 장 모두에 나온다
- [ ] 목록 인쇄에 `정산` 칸이 나오지 않는다

**서버**

- [ ] 주소·키를 입력하지 않은 새 브라우저에서도 서버에 연결된다
- [ ] 로그인 안 하면 조회 전용, 로그인하면 저장된다
- [ ] 세션이 만료된 PC 에서도 자료가 보인다 (조회 전용으로 내려갈 뿐)
- [ ] 불러오기에 실패하면 저장이 막히고 그 이유가 화면에 뜬다
- [ ] 다른 창에서 저장하면 10초 안에 이쪽 화면이 갱신된다
- [ ] 저장 버튼을 누르면 기다리지 않고 즉시 저장된다

## 하지 말 것

- 프레임워크·번들러·CDN 라이브러리 도입 (단일 파일 제약이 깨진다. supabase-js SDK 도 포함)
- 서버 자료를 주기적으로 통째로 내려받기 (전송량이 샌다. `updated_at` 만 확인할 것)
- 입금 표시를 합치지 않고 그냥 올리기 (`mergeRemotePayments` 를 빼면 남의 체크가 지워진다)
- 조회 요청에서 401 을 그냥 실패로 처리하기 (토큰을 버리고 익명으로 다시 읽어야 한다)
- 서버에서 불러오지 못한 상태로 저장하기 (남의 최신 자료를 덮어쓴다)
- `sb_secret_` / `service_role` 키를 코드나 저장소에 넣기 (대화창에 붙여넣는 것도 안 된다)
- 공개용 키를 `Authorization: Bearer` 헤더로 보내기 (새 키에서 거부된다)
- 요금 값 하드코딩 (`BYULPYO2`·`DISCOUNTS_13` 은 새 저장소용 기본치일 뿐, 계산은 `state` 로 한다)
- 기본 전용사용료를 "기본료 + 시간당 단가" 로 되돌리기 (조례는 1회 금액 구조다)
- 가산·감산을 직전 소계에 곱하기 (전부 기본액 기준이다)
- 개인정보 입력란 추가
- 계산 근거(`why`) 없이 계산 항목만 추가
- **우측 상세의 `산정 상세` 나 목록 수익집계용을 없애기** (등록창에서 금액을 뺀 뒤로
  이 둘이 "왜 이 금액인지" 를 보여주는 유일한 곳이다)
- 감면 유형을 호별로 늘리기 (조례가 정한 비율은 100%·80%·50% 셋뿐이다)
- 산정 내역을 다시 그릴 때 `keepFocusInPlace()` 를 빼기 (입력칸이 손가락 밑에서 밀린다)
- 전기료·수도료를 자동으로 계산하기 (사용량을 재야 나오는 실비다. 직접 입력만 받는다)
- **전기료·수도료에 감면을 적용하기** (이미 할인이 반영된 금액으로 청구가 온다)
- **부속시설 감면을 켜고 끄는 설정(`discountAnnex`)을 되살리기** —
  이름을 어떻게 붙여도 담당자가 끄면 부속시설이 감면에서 빠진다. 실제로 그 사고가 났다.
  감면 대상은 코드가 정한다 (①~⑤ + ⑥, ⑦⑧만 제외)
- `esc()` 없이 사용자 입력을 `innerHTML` 에 넣기
- 시설 구분·요일·준비 밖의 색상 추가
- **상태를 색이나 파선으로 나타내기** (상태는 화면에서 쓰지 않기로 했다)
- **달력 칸에 주말 배경 되살리기** (쉬는 날이 없어서 전부 흰색으로 했다)
- **등록창을 바깥 클릭으로 닫기** (적던 내용이 날아간다)
- **등록창에 `준비시간 함께 등록`·산정 이용료 박스·`상태` 칸 되살리기**
- **취소 거르기 선택창 되살리기** (취소를 쓰지 않는다)
- 시각 입력을 `<input type="time">` 으로 되돌리기 (등록창·설정 화면 **둘 다**. AM/PM 으로 나온다)
- **화면·인쇄**의 준비 건에 바탕색 깔기 (거기서는 테두리로만 구분한다.
  엑셀만 담당자 요청으로 칠한다)
- 여러 줄에 걸쳐 병합된 날짜 칸을 준비 색으로 칠하기 (행사 줄까지 물든다.
  토·일·공휴일 색은 날짜 단위라 칠해도 된다)
- **판 번호(`APP_VERSION`) 올리는 것을 빼먹기** — 고쳤는데 안 바뀐 것처럼 보인다.
  판 번호는 `⚙ 설정` 맨 아래에 뜨며, 조회하는 사람도 볼 수 있게 관리자 전용 칸 바깥에 둔다
- 목록 열을 `LIST_COLS` 밖에서 따로 만들기 (화면과 인쇄·엑셀이 어긋난다)
- 달력 칸 만드는 코드를 인쇄용으로 따로 두기 (`calCellsHtml()` 하나만 쓴다)
- 인쇄판 스타일을 `@media print` 안으로 옮기기 (배율을 미리 잴 수 없게 된다)
- `.pc-page` 의 `overflow:hidden` 빼기 (축소해도 두 장으로 나온다)
- **행사와 준비를 한 장에 섞어서 인쇄하기** (붙여 두는 종이가 다르다)
- 통제 도장의 도형 값(원·가로줄·글자 자리)을 **눈대중으로 고치기** — 한글 원본에서 다시 잰다
- 통제 도장의 앵커를 `oneCellAnchor` 로 되돌리거나 좌표를 눈대중으로 고치기
- 통제 도장의 연도 자동 처리를 빼기 (담당자가 한글에서 연도만 고쳐 쓰던 것이다)
- 도장을 엑셀 자리 크기(117px) 그대로 그려 넣기 (늘어나서 계단이 보인다)
- **정산서 서식을 고치기** — 칸·병합·테두리·문구를 더하거나 빼지 않는다.
  받은 서식 그대로 두고 *내용*만 채운다
- **정산서의 전용사용료·허가일자를 자동으로 채우기** (담당자가 손으로 적는 칸이다)
- 옆 칸으로 흘러넘겨야 다 보이는 줄(`J70`)에 **「칸에 맞춤」 켜기** (제 칸에 갇혀 깨알만 해진다)
- 줄바꿈 칸에 「칸에 맞춤」 켜기 (엑셀이 둘을 같이 못 쓴다. 글씨 크기를 줄인다)
- 긴 글을 맞추려고 **서식의 열 너비·줄 높이 바꾸기** (쪽 나눔과 인쇄 영역이 어긋난다)
- **마이크를 「기본료」 와 「개당」 두 항목으로 되돌리기** (확성기를 쓰지 않아 합쳤다.
  기본료는 `annexItems` 의 `base` 가 든다)
- **쓰고 있는 예약이 있는 부속시설 항목을 이관에서 지우기** (그 건의 금액이 소리 없이 줄어든다)
- 플래카드·중계방송료·입장료의 `조례 조문 미확인` 표시를 **조문을 확인하지도 않고 지우기**
  (담당자가 준 금액이라는 것을 숨기게 된다)
- 입장료 수량을 예약의 `headcount` 로 **자동으로 채우기** (참가 인원과 유료 입장 인원이 다르다)
- `r.annex` 를 직접 읽어 금액을 매기기 (**`annexUse()` 를 쓴다** — 횟수가 빠진다)
- `readAnnexTimes()` 가 **1회도 자료에 담게 만들기** (예전 자료와 값이 달라 보인다)
- 정산서에서 숫자·수식을 **`at()` 으로 그냥 넣기** (`###` 로 깨진다.
  `atNum()`·`atFml()` 을 쓴다)
- 부속시설 차례를 `BYULPYO6` 에 적힌 순서로 되돌리기 (**`ANNEX_ORDER` 가 정한다**)
- 입장료·중계방송료·플래카드를 **정산서 ⑤부속시설에 넣기** (서식에 ②③④ 제 칸이 있다)
- ⑤부속시설의 **탁자·의자 자리(여섯째·일곱째 줄)를 옮기기**
  (서식의 개수 칸·금액 수식·제출용 거울이 그 두 줄에만 걸려 있다)
- ⑤부속시설을 **쓴 것만 위에서부터 채우기** 로 되돌리기 (차례가 고정이라는 것이 담당자 요청이다)
- 정산서 ②③④ 의 계를 **수식이 아니라 숫자로** 넣기 (단가를 손으로 고쳐도 안 따라온다)
- **등록창에 관람료 수입 칸 되살리기** (관람료를 받는 행사가 없다.
  계산(`calcFee` ⑤)과 정산서 ① 칸은 그대로 두었으므로 필요해지면 칸만 되살리면 된다)
- 정산서 금액을 수식이 아니라 숫자로 박아 넣기 (손으로 적는 칸을 채워도 합계가 안 따라온다)
- **`SETTLE_FORM` 을 손으로 고치기** (원본 `.xlsx` 에서 다시 뽑아 통째로 갈아 끼운다)
- 정산서를 HTML 표로 되돌리기 (확장명 경고 · `#VALUE!` · 날짜가 숫자로 바뀌는 문제가 되살아난다)
- 무늬 채우기(`gray0625`)에 바탕색을 억지로 넣기 (칸이 새까맣게 칠해진다)
- `xwNew` 의 0번 기본 서식을 빼기 (맨 처음 등록한 글꼴이 빈 칸에까지 딸려간다)
- 정산 체크 칸을 `LIST_COLS` 에 넣기 (인쇄·엑셀·CSV 까지 따라 늘어난다)
- 목록 엑셀을 HTML 표 `.xls` 로 되돌리기 (확장명 경고가 되살아난다)
- 목록 엑셀에 완료 칸 넣기 (화면·종이에서만 쓰는 표시다)
- 인원을 글자로 내보내기 (엑셀이 초록 세모를 붙인다)
- 병합된 날짜 칸의 이어지는 줄에 서식을 넣지 않기 (날짜 묶음 굵은 줄이 끊긴다)
- 허가번호 칸을 노란 바탕으로 되돌리기 (담당자 요청으로 흰 바탕이다)
- `wb.page` 없이 새 통합문서를 만들기 (정산서의 세로·86%·가운데 설정이 딸려온다)
- 지우기·초기화를 `confirm()` 하나로만 막기 (실수로 한 번 누르면 그대로 지워진다)
- `verifyPassword()` 가 `SERVER.token` 을 갈아끼우게 만들기 (틀린 비밀번호에 로그인이 풀린다)
- 장소·단체·목적이 다른 건을 한 장에 묶기 (서로 다른 행사다. 파일을 따로 낸다)
- 요금 담당 계정 구분을 **보안 경계처럼 설명하기** (화면에서 가르는 것뿐이다)
- 관리자 전용 칸을 `hidden` 으로만 감추기 (이메일이 화면 소스에 남는다)
- **로그인한** 직원에게서 요금 기준 화면을 감추기 (왜 이 금액인지 보여주는 것이 이 도구의 목적이다).
  로그인하지 않은 사람에게 감추는 것은 담당자 결정이므로 되돌리지 않는다
- 로그인하지 않은 사람 화면에 금액을 `hidden` 으로만 감추기 (개발자도구로 보인다. 아예 그리지 않는다)
- 금액을 새로 띄우는 자리를 만들면서 `currentRole() !== "viewer"` 확인을 빠뜨리기 —
  달력 부제(`#calSubtitle`)와 날짜별 상세 인쇄가 실제로 그렇게 새고 있었다 (2026-09-18)
- 로그인해야 열리는 탭을 더하면서 **`LOGIN_PANES` 에 안 적기** (`setPane` 과 `applyRole` 이
  같은 목록을 쓴다. 한쪽만 고치면 구멍이 난다)
- **요금 계산 화면에 계산을 따로 구현하기** — `calcFee()` 에 가짜 예약 객체를 넣는다.
  여기서 따로 계산하면 대장 금액과 어긋나고, 그 순간 이 화면을 못 믿게 된다
- **요금 계산 화면이 `state` 를 고치거나 `save()` 를 부르게 만들기** (조건을 넣어 보는 곳이다.
  예약으로 남기려면 등록창을 쓴다)
- 요금 계산 화면에서 **부속시설·전기·수도가 빠졌다는 안내를 지우기**
  (빠진 금액이 있다는 것을 숨기게 된다)
- **요금 계산 화면을 비로그인에게 임의로 열기** — 요금 기준은 조례라서 공개해도 되는 값이지만,
  여는 것은 담당자 결정이다 (2026-09-21 로그인 전용으로 정했다)
- **AI 키를 `index.html`·설정 화면·`ledger` 표에 넣기** — 이 대장은 로그인 없이도 조회가 된다.
  설정에 넣는 것은 **공개와 같다.** 키는 Supabase Edge Function Secrets 에만 둔다
- **AI 에게 금액을 계산시키기** (계산은 `calcFee()` 가 한다. AI 는 이미 나온 숫자를 풀어 쓰기만 한다)
- **메모를 기본으로 보내지게 만들기** (`#aiMemo` 는 꺼진 채로 열려야 한다.
  연락처가 적히는 칸이라 담당자가 켤 때만 들어간다)
- `aiSafe()` 의 전화번호·주민번호 지우기를 빼기 (단체명에 든 사람 이름까지는 못 막아도
  번호는 여기서 걸린다. 마지막 그물이다)
- AI 에게 보내는 자료의 **범위를 안 적기** (자료 밖을 「없습니다」 라고 단정하게 된다)
- **AI 가 조례의 가산·감산을 적용해 요금을 산출하게 만들기** (이미 나온 금액을 인용하는 것과
  다르다. 새 금액을 내는 것은 `calcFee()` 와 「요금 계산」 탭이 한다)
- 월별 합계를 미리 더해 넣지 않고 **AI 에게 더하게 시키기**
- **AI 답을 `innerHTML` 에 그냥 넣어 표를 그리기** — `aiBlocks()` 로 갈라서 **칸마다 `esc()`** 를
  거친다. 살리는 서식은 `**굵게**` 하나뿐이고, 링크·그림은 살리지 않는다
- 엑셀 내보내기를 **창 전체 한 번에** 로 바꾸기 (답마다 따로 내는 것이 담당자 요청이다)
- 답의 표를 엑셀로 낼 때 금액을 **글자로** 넣기 (합계를 못 낸다).
  반대로 날짜까지 숫자로 바꾸지도 않는다
- **AI 응답을 `state` 에 저장하기** (저장하는 순간 그 문장이 "대장에 적힌 것" 이 된다)
- Edge Function 의 `verify_jwt` 끄기 (주소를 아는 누구나 우리 요금을 태운다)
- AI 응답을 `esc()` 없이 `innerHTML` 에 넣기 (업체가 무엇을 돌려줄지 우리가 정하지 못한다)
- 시스템 프롬프트(규칙)를 **브라우저에서 보내게 만들기** — 규칙은 함수 안에만 둔다.
  보낼 수 있게 하면 개발자도구로 바꿔 끼울 수 있다
- `pickText()` 의 여러 형식 처리를 하나로 줄이기 (업체마다 응답 모양이 다르다)
- **`index.html` 에 업체 이름을 박아 넣기** (모델·주소·형식은 전부 함수의 환경변수다.
  업체가 바뀌어도 대장은 안 고친다)
- AI 가 꺼졌을 때 **화면이 멈추게 만들기** (단추만 사라지고 나머지는 그대로 돌아야 한다)
- **분석 그래프에 세 시설을 한 판에 겹치기** — 주황과 초록이 적록색약에서 구분되지 않는다.
  시설마다 따로 그리고, 비교는 마우스를 올렸을 때 뜨는 값으로 한다
- 분석 그래프를 **시설마다 다른 세로 눈금**으로 그리기 (높이를 견줄 수 없게 된다)
- 분석의 **수익금에 전기료·수도료를 넣기** (실비로 받는 것이라 수익이 아니다)
- 그래프 크기·여백을 `stChart` 와 `stHover` 에 **따로 적기** (`ST_GEO` 한 곳에 둔다)
- 그래프의 **점마다 숫자를 붙이기** (끝점에만 적는다)
- 추이에서 **빈 달을 건너뛰기** (0 으로 채워야 추이가 거짓말을 하지 않는다)
- 그래프만 두고 **표를 빼기** (마우스를 못 쓰는 사람도 값에 닿아야 한다)
- **「한눈에」 에서 선 모양·점 모양·선 끝 이름 가운데 하나라도 빼기** —
  색만 남으면 적록색약에서 주황과 초록이 갈리지 않는다. 색은 거들 뿐이다
- 그래프 판을 `card.querySelector("svg")` 로 찾기 (**`svg.st-plot`** 을 쓴다.
  범례의 작은 svg 가 앞에 있어 크로스헤어가 엉뚱한 데로 간다)
- 세로 눈금을 **높이를 넷으로 나눠** 만들기 (`0 1 3 4 5` 로 찍힌다. `niceScale` 을 쓴다)
- 분석 표의 줄 강조에 `background` 를 통째로 주기 (칸 뒤의 막대가 지워진다.
  `background-color` 만 바꾼다)
- 엑셀 가져오기에서 **연락처·성명 열을 고를 수 있게 만들기** (`IMP_BLOCK` 을 줄이지 않는다.
  고를 수 있게 두면 언젠가 들어온다 — 개인정보를 담지 않는 구조가 이 도구의 바탕이다)
- 가져온 값에서 **`aiSafe()` 를 빼기** (비고에 적힌 번호가 그대로 대장에 들어온다)
- **엑셀 금액을 자동 산정값 자리에 넣기** — 엑셀 금액은 `feeOverride`(직접 입력) 로만 들어간다.
  기본은 **조례대로 다시 계산**이고, 몇 건이 다른지 미리 알려 준다
- 미리보기 없이 **바로 집어넣기** (확인을 누르기 전에는 `state` 를 건드리지 않는다)
- 이미 있는 건을 **또 넣기** (날짜·시설·시각·단체가 같으면 건너뛴다)
- 엑셀 가져오기에 **정해진 서식을 요구하기** (양식이 부서마다 다르다.
  열을 맞춰 보여주고 확인받는 것이 이 화면의 방식이다)
- **AI 가 맞춘 열을 확인 없이 그대로 쓰기** (AI 는 번호만 짚는다. 담당자가 미리보기로 본다)
- AI 없이는 엑셀 가져오기가 **안 되게 만들기** (AI 는 거들 뿐이고, 손으로 다 된다)
- 엑셀 읽기에 **라이브러리를 들이기** (`DecompressionStream`·`DOMParser` 로 한다.
  단일 파일 제약은 읽기 쪽에도 그대로다)
- 정산서 입금 계좌를 `시설 · 요금 기준` 탭으로 되돌리기 (계좌는 요금 기준이 아니고, 담당자만 볼 것이다)
- 정산서의 인쇄 영역·머리글 빼기 (서식 밖이 회색으로 눌리지 않아 지저분해진다)
- 달력을 다시 시간순으로 되돌리기 (시설 차례가 담당자 요청이다)
- 달력 칩 아랫줄을 한 줄로 자르기 (`text-overflow:ellipsis`) — 행사명이 잘려 못 알아본다
- 요일 색을 칸 배경에 쓰기 (글씨에만 쓴다)
- `body.fee-only` 로 서버 연결 카드까지 잠그기 (로그아웃을 못 하게 된다)
- `⚙ 설정` 창의 아랫줄을 붙박이에서 되돌리기 (`설정 저장` 이 화면 밖으로 밀린다)
