# 요금 담당 계정 만들기와 관리

요금 관리 탭에서 **입금 확인만** 할 수 있는 전용 계정을 만드는 절차입니다.
이 계정으로 로그인하면 예약 등록·수정과 설정 화면이 잠기고, 요금 관리만 열립니다.

> **먼저 읽어 주세요 — 이 구분은 화면에서 가르는 것입니다.**
> 서버가 막아 주는 것이 아닙니다. 자세한 내용은 맨 아래 「알고 계셔야 할 한계」 를 보세요.

---

## 한눈에 보기

계정 하나를 쓰려면 **두 곳을 모두** 손봐야 합니다. 한쪽만 하면 안 됩니다.

| | 어디서 | 무엇을 |
|---|---|---|
| 1단계 | Supabase 대시보드 | 로그인할 계정(이메일·비밀번호)을 만든다 |
| 2단계 | 대장 화면 → 설정 탭 | 그 이메일을 **요금 담당으로 등록**한다 |
| 3단계 | 대장 화면 | 그 계정으로 로그인해서 잠기는지 확인한다 |

**2단계를 빠뜨리면 그 계정은 담당자와 똑같은 권한을 갖습니다.**
대장은 "요금 담당 목록에 없는 로그인 계정 = 담당자" 로 보기 때문입니다.
계정을 만들었으면 **바로 이어서** 2단계를 하세요.

---

## 1단계 — Supabase 에서 계정 만들기

1. https://supabase.com 에 담당자 계정으로 로그인합니다
2. 프로젝트 **`facility-ledger`** 를 엽니다
3. 왼쪽 메뉴에서 **Authentication**(자물쇠 모양) → **Users**
4. 오른쪽 위 **Add user** → **Create new user**
5. 아래를 채웁니다

   | 칸 | 넣을 값 |
   |---|---|
   | Email | 요금 담당이 쓸 이메일 (예: `fee@djsiseol.or.kr`) |
   | Password | 비밀번호. **여기서 정한 값을 그대로 알려줘야 합니다** |
   | Auto Confirm User | **반드시 켭니다** |

6. **Create user**

**Auto Confirm User 를 켜지 않으면** 확인 메일을 눌러야 로그인이 되는데,
기관 메일함으로 확인 메일이 안 오는 경우가 많아 로그인이 막힙니다.
켜는 것을 잊었다면 만들어진 사용자를 지우고 다시 만드는 쪽이 빠릅니다.

> 실제로 존재하는 메일 주소가 아니어도 됩니다. 로그인 아이디로만 쓰이며
> 비밀번호를 대시보드에서 직접 정하기 때문입니다.
> 다만 비밀번호 재설정 메일을 쓰려면 받을 수 있는 주소여야 합니다.

### 신규 가입 차단은 그대로 두세요

**Authentication → Sign In / Providers** 의 신규 가입(Enable sign ups)은
**꺼진 상태로 둡니다.** 대시보드에서 만드는 계정은 이 설정과 상관없이 만들어집니다.
켜면 주소를 아는 누구나 스스로 계정을 만들어 자료를 고칠 수 있습니다.

---

## 2단계 — 대장에 요금 담당으로 등록

**담당자 계정으로** 대장에 로그인한 상태에서 합니다.

1. 대장 화면 → **시설 · 요금 기준** 탭
2. **서버 연결** 칸을 아래로 내려 **`요금 담당 계정 (이메일, 한 줄에 하나)`** 을 찾습니다
3. 1단계에서 만든 이메일을 적습니다. 여러 명이면 **한 줄에 하나씩**

   ```
   fee@djsiseol.or.kr
   fee2@djsiseol.or.kr
   ```

4. 적는 즉시 저장됩니다 (대문자로 적어도 알아서 소문자로 맞춥니다)

---

## 3단계 — 제대로 됐는지 확인

요금 담당 계정으로 로그인해서 아래를 봅니다.
담당자 계정은 로그아웃하거나, 시크릿 창에서 하면 편합니다.

- [ ] 화면 오른쪽 위에 **`요금 담당`** 뱃지가 뜬다
- [ ] **`+ 예약 등록`** 버튼이 사라졌다
- [ ] 달력에서 날짜를 눌러도 `수정`·`복제`·`확인`·`공휴일 지정` 이 없다
- [ ] **시설 · 요금 기준** 탭의 입력칸이 흐리게 잠겨 있다 (로그인 칸만 살아 있음)
- [ ] **요금 관리** 탭에서 **입금 체크가 눌린다**
- [ ] 체크한 뒤 새로고침해도 표시가 남아 있다

뱃지가 안 뜨고 예약 등록 버튼이 그대로면 **2단계가 안 된 것**입니다.
설정 탭의 이메일 철자를 다시 확인하세요.

---

## 평소 관리

### 비밀번호를 바꾸고 싶을 때 / 잊었을 때

Supabase → **Authentication → Users** → 해당 사용자 줄 오른쪽 **⋯** 메뉴

- **Reset password** 또는 **Send password recovery** — 그 주소로 재설정 메일이 갑니다
  (받을 수 있는 실제 메일 주소일 때만 쓸 수 있습니다)
- 메일을 못 받는 주소라면, 사용자를 **지우고 1단계부터 다시 만드는 것**이 확실합니다.
  대장 자료에는 아무 영향이 없습니다 — 계정은 로그인 수단일 뿐입니다

### 담당 직원이 바뀔 때

**비밀번호만 바꾸지 말고 계정을 새로 만드는 쪽**을 권합니다.
누가 언제 쓰던 계정인지 섞이지 않습니다.

1. 새 사람 계정을 1단계대로 만든다
2. 설정 탭의 목록에서 **옛 이메일을 지우고 새 이메일을 적는다**
3. Supabase 에서 옛 사용자를 **Delete user** 로 지운다

### 요금 담당 권한만 떼고 싶을 때

설정 탭의 목록에서 그 이메일을 **지웁니다.**

⚠️ **주의** — 목록에서 지우기만 하면 그 계정은 **담당자 권한이 됩니다.**
권한을 없애려는 것이라면 Supabase 에서 **사용자 자체를 삭제**해야 합니다.

### 여러 명에게 줄 때

한 줄에 하나씩 적으면 됩니다. 사람 수만큼 Supabase 계정도 따로 만드세요.
**계정 하나를 여러 명이 돌려 쓰지 마세요** — 누가 확인했는지 알 수 없어집니다.

### 담당자 계정은 어떻게 되나

담당자(`hanjeong0303@djsiseol.or.kr`)는 이 목록에 **넣지 않습니다.**
넣으면 담당자 본인이 예약을 못 고치게 됩니다.

---

## 자주 나오는 문제

| 증상 | 원인 |
|---|---|
| 로그인은 되는데 예약 등록 버튼이 보인다 | 설정 탭의 요금 담당 목록에 그 이메일이 없습니다 (2단계) |
| `Invalid login credentials` | 비밀번호가 다르거나, **Auto Confirm** 없이 만든 계정입니다 |
| 로그인 후 아무것도 못 고친다 (조회 전용) | 서버 연결이 끊긴 상태입니다. 설정 탭의 연결 상태를 보세요 |
| 입금 체크가 눌리지 않는다 | 로그인이 풀렸습니다. 한 시간쯤 지나면 자동으로 조회 전용으로 내려갑니다 — 다시 로그인하세요 |
| 뱃지는 뜨는데 요금 관리 탭이 안 보인다 | 요금 관리 탭은 누구에게나 보입니다. 화면을 새로고침해 보세요 |

---

## 알고 계셔야 할 한계

**요금 담당 계정은 화면에서 역할을 가르는 장치이지, 서버가 막아 주는 보안 경계가 아닙니다.**

이 대장은 자료 전체가 서버의 **한 행에 통째로** 담겨 있습니다.
그래서 서버는 "이 사람이 입금 표시만 고쳤는지, 예약까지 고쳤는지" 를 가려낼 수 없습니다.
로그인만 되면 기술적으로는 브라우저 개발자도구로 예약도 고칠 수 있습니다.

즉 이 기능이 하는 일은 이렇습니다.

- ✅ 요금 담당이 **실수로** 예약을 건드리는 것을 막는다
- ✅ 누가 무슨 일을 하는 사람인지 화면에서 분명히 한다
- ❌ 나쁜 마음을 먹은 사람을 막지는 **못한다**

그래서 **믿을 수 있는 직원에게만 계정을 주세요.**

진짜로 서버에서 막으려면 예약 표와 입금 표를 나누고 열 단위 권한 정책을 걸어야 하는데,
그러려면 "파일 하나로 돌아간다" 는 이 도구의 전제를 버려야 합니다.
꼭 필요해지면 그때 구조를 다시 잡아야 합니다.
