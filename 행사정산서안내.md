# 행사 정산서 뽑기

부서에서 쓰던 「행사 정산서」 서식을 그대로 엑셀 파일로 받는 기능입니다.
**서식(칸·테두리·문구)은 예전 그대로**이고, 대장에 들어 있는 값만 자동으로 채워집니다.

## 1. 먼저 — 입금 계좌를 한 번만 넣어 둡니다

`시설 · 요금 기준` 화면 맨 아래 **「정산서 입금 계좌」** 에 넣습니다.

| 칸 | 예 |
|---|---|
| 시설사용료 — 은행 | `하나은행` |
| 시설사용료 — 계좌번호 | `123-456789-01234` |
| 공과금 — 은행 | `농협` |
| 공과금 — 계좌번호 | `301-0000-1111-11` |

정산서에는 **`하나은행:123-456789-01234`** 처럼 한 줄로 찍힙니다.
넣은 값은 바로 아래에 미리 보여 주니 모양을 확인하세요.

- 담당자 계정으로 로그인해야 고칠 수 있습니다.
- 비워 두면 그 칸이 **빈칸으로 나와** 손으로 적을 수 있습니다.

## 2. 정산할 건을 고릅니다

두 곳 어디서 골라도 같습니다. 골라 놓은 것은 두 화면이 함께 셉니다.

- **목록** — 맨 왼쪽 `정산` 칸에 체크
- **달력** — 날짜를 누르고 우측 상세에서 일정마다 있는 `정산서` 에 체크

**이틀 이상 이어지는 행사는 그 날짜들을 모두 체크하세요.**
일자가 `8.28~8.29` 로 묶여서 한 장에 나옵니다.
(떨어진 날짜면 `8.1~8.2, 8.5` 처럼 쉼표로 이어집니다.)

## 3. `정산서` 를 누릅니다

체크한 건수가 단추에 나옵니다 (`정산서 2건`).
누르면 `정산서_행사명_8.28~8.29.xls` 파일이 내려받아집니다.

엑셀에서 열면 **위가 보관용, 아래가 제출용** — 인쇄하면 두 장으로 나옵니다.

## 4. 손으로 적는 칸

아래 두 칸은 **일부러 비워 둡니다.** 대장이 아니라 허가 서류에서 나오는 값이라
자동으로 채우지 않기로 했습니다.

| 칸 | |
|---|---|
| **⑥ 전용사용료** | 허가할 때 이미 받은 금액 |
| **허가일자** | 허가서에 적힌 날짜 |

**전용사용료를 적으면 ⑦소계 · ⑧전용사용료 기납부액 · 금회징수액이 저절로 바뀝니다.**
금액 칸이 엑셀 수식으로 들어가 있어서 그렇습니다. 손으로 다시 더할 필요가 없습니다.

허가번호 · 매표수 · ②입장료 · ③중계방송료 · ④상행위 사용료도 대장에 없는 값이라
비워 두었습니다. 필요하면 엑셀에서 그대로 적으면 됩니다 (계도 수식이라 따라옵니다).

## 5. 자동으로 채워지는 것

| 칸 | 어디서 오나 |
|---|---|
| 장소 | 예약의 시설 |
| 행사 명 | 이용 목적 (안 적었으면 신청 단체) |
| 대 관 자 | 신청 단체 |
| 일 자 | 체크한 날짜 (이어지면 `8.28~8.29`) |
| ① 관람료 수입 `<A>` | 조례 제12조 계산값 (고등학교 이하 경기 20% 할인까지) |
| ① 감면 `<C>` | 예약에 고른 감면율로 자동 계산 |
| ⑤ 부속시설 사용료 | 예약에 넣은 별표 6 항목·수량. 단가와 감면율까지 |
| 공과금 (전기료·수도료) | 예약에 넣은 실비 |
| 입금 계좌 | 위 1번에서 넣은 값 |

## 6. 통제 도장

첫 장 우측 아래 통제 도장 자리는 **원본 서식처럼 비어 있습니다.**
지금까지 하시던 대로 한글에서 만든 도장을 붙여 넣으시면 됩니다.

(담당자가 통제 도장 한글 원본을 주시면, 그 모양 그대로 엑셀에 넣고
날짜만 고쳐 쓸 수 있게 바꿀 예정입니다.)

## 자주 나오는 것

**감면율이 서로 다른 건을 함께 골랐을 때**
정산서는 감면율 칸이 하나뿐이라 **첫 건의 감면율**을 씁니다.
그때는 "감면율이 서로 달라 첫 건의 감면율을 썼습니다" 안내가 뜹니다.
감면이 다르면 나눠서 뽑는 것이 맞습니다.

**부속시설을 8개 넘게 썼을 때**
서식의 부속시설 칸이 **일곱 줄뿐**이라 여덟 번째부터는 들어가지 않습니다.
그럴 때는 "○○ 은(는) 빠졌습니다" 안내가 뜨니 **엑셀에서 손으로 넣어** 주세요.
(서식을 임의로 늘리지 않기로 했습니다.)

**의자·탁자 같은 「개」 단위 항목**
서식이 아래 두 줄을 그 자리로 쓰도록 짜여 있어, 개수로 세는 항목은 자동으로 그 줄에 들어갑니다.

**파일 형식**
`.xlsx` 로 나오므로 엑셀에서 바로 열립니다. 예전에 뜨던
"파일 형식 및 확장명이 일치하지 않습니다" 경고는 나오지 않습니다.
