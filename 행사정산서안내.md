# 행사 정산서 뽑기

> **로그인이 필요합니다.** 로그인하지 않으면 정산서 단추와 체크 칸이 보이지 않습니다.
> 윗줄 `⚙ 설정` 에서 담당자 계정으로 로그인하세요.

부서에서 쓰던 「행사 정산서」 서식을 그대로 엑셀 파일로 받는 기능입니다.
**서식(칸·테두리·문구)은 예전 그대로**이고, 대장에 들어 있는 값만 자동으로 채워집니다.

## 1. 먼저 — 입금 계좌를 한 번만 넣어 둡니다

윗줄 **`⚙ 설정`** 을 눌러 창을 열면 **「정산서 입금 계좌」** 가 있습니다.

| 칸 | 예 |
|---|---|
| 시설사용료 — 은행 | `하나은행` |
| 시설사용료 — 계좌번호 | `123-456789-01234` |
| 공과금 — 은행 | `농협` |
| 공과금 — 계좌번호 | `301-0000-1111-11` |

정산서에는 **`하나은행:123-456789-01234`** 처럼 한 줄로 찍힙니다.
넣은 값은 바로 아래에 미리 보여 주니 모양을 확인하세요.

- 담당자 계정으로 로그인해야 고칠 수 있습니다.
- 비워 두면 그 칸이 **빈칸으로 나와** 손으로 적을 수 있습니다.
- 다 넣었으면 창 아래 **`설정 저장`** 을 누르세요. 안 눌러도 2분 뒤에 저장되지만,
  바로 창을 닫고 다른 PC 에서 열어 볼 생각이면 눌러 두는 것이 확실합니다.
  단추 왼쪽에 지금 저장 상태가 적혀 있습니다.

## 2. 정산할 건을 고릅니다

두 곳 어디서 골라도 같습니다. 골라 놓은 것은 두 화면이 함께 셉니다.

- **목록** — 맨 왼쪽 `정산` 칸에 체크
- **달력** — 날짜를 누르고 우측 상세에서 일정마다 있는 `정산서` 에 체크

**이틀 이상 이어지는 행사는 그 날짜들을 모두 체크하세요.**
일자가 `8.28~8.29` 로 묶여서 한 장에 나옵니다.
(떨어진 날짜면 `8.1~8.2, 8.5` 처럼 쉼표로 이어집니다.)

### 여러 행사를 한꺼번에 체크해도 됩니다

**장소 · 신청 단체 · 이용 목적**이 모두 같은 건만 한 장으로 묶이고,
하나라도 다르면 **서로 다른 행사로 보아 파일이 따로 나옵니다.**

| 함께 체크한 것 | 결과 |
|---|---|
| 충무체육관 · 태권도협회 · 품새대회 (8.28, 8.29) | 한 장 (`8.28~8.29`) |
| 위 + 충무체육관 · 태권도협회 · **승급심사** (8.30) | 두 장 |
| 위 + **한밭체육관** · 태권도협회 · 품새대회 (8.28) | 세 장 |

여러 장이 나올 때는 파일 이름 앞에 장소가 붙습니다
(`정산서_충무체육관_품새대회_8.28~8.29.xlsx`).
한 장만 나올 때는 예전처럼 장소 없이 나옵니다.

> 파일이 여러 개 내려받아질 때 브라우저가 "여러 파일을 다운로드하시겠습니까?" 라고 물으면
> **허용**을 눌러 주세요.

## 3. `정산서` 를 누릅니다

체크한 건수가 단추에 나옵니다 (`정산서 2건`).
누르면 `정산서_행사명_8.28~8.29.xls` 파일이 내려받아집니다.

엑셀에서 열면 **위가 보관용, 아래가 제출용** — 인쇄하면 두 장으로 나옵니다.

## 4. 손으로 적는 칸

아래 두 칸은 **일부러 비워 둡니다.** 대장이 아니라 허가 서류에서 나오는 값이라
자동으로 채우지 않기로 했습니다.

| 칸 | |
|---|---|
| **⑥ 전용사용료** | 허가할 때 이미 받은 금액 |
| **허가일자** | 허가서에 적힌 날짜 |

**전용사용료를 적으면 ⑦소계 · ⑧전용사용료 기납부액 · 금회징수액이 저절로 바뀝니다.**
금액 칸이 엑셀 수식으로 들어가 있어서 그렇습니다. 손으로 다시 더할 필요가 없습니다.

허가번호 · 매표수 · ②입장료 · ③중계방송료 · ④상행위 사용료도 대장에 없는 값이라
비워 두었습니다. 필요하면 엑셀에서 그대로 적으면 됩니다 (계도 수식이라 따라옵니다).
**허가번호 칸은 흰 바탕으로 나옵니다** — 원본 서식의 노란 칠은 넣지 않았습니다.

## 5. 자동으로 채워지는 것

| 칸 | 어디서 오나 |
|---|---|
| 장소 | 예약의 시설 |
| 행사 명 | 이용 목적 (안 적었으면 신청 단체) |
| 대 관 자 | 신청 단체 |
| 일 자 | 체크한 날짜 (이어지면 `8.28~8.29`) |
| ① 관람료 수입 `<A>` | 조례 제12조 계산값 (고등학교 이하 경기 20% 할인까지) |
| ① 감면 `<C>` | 예약에 고른 감면율로 자동 계산 |
| ⑤ 부속시설 사용료 | 예약에 넣은 별표 6 항목·수량. 단가와 감면율까지 |
| 공과금 (전기료·수도료) | 예약에 넣은 실비 |
| 입금 계좌 | 위 1번에서 넣은 값 |

## 6. 통제 도장 · 대관담당자

**통제 도장은 자동으로 찍힙니다.**
보내 주신 **한글 도장 파일의 도형 값을 그대로 읽어** 같은 자리·같은 크기로 그립니다.
**가운데 줄의 연도는 올해로 자동**이고, **월·일만 손으로** 적으시면 됩니다.
한글에서 연도만 고쳐 붙이시던 그 일을 프로그램이 대신 하는 것입니다.

원본 정산서에 붙어 있던 도장 그림은 95픽셀짜리를 늘려 쓴 것이라 확대하면 계단이 보였습니다.
지금은 **여덟 배 크기로 그려 넣어** 인쇄하거나 확대해도 또렷합니다.

### 다른 도장을 쓰시려면

`⚙ 설정` → **「통제 도장」** → `그림 올리기` 로 그림 파일(PNG·JPG)을 올리세요.
자리와 크기는 그대로 맞춥니다. 되돌리려면 `기본 그림으로 되돌리기` 를 누르세요.

**올린 그림은 연도가 자동으로 바뀌지 않습니다** — 연도가 그림에 그려져 있기 때문입니다.
연도를 자동으로 두시려면 그림을 올리지 않는 편이 낫습니다.

**대관담당자 이름**도 자동으로 들어갑니다.
`⚙ 설정` → 「대관담당자」 에 이름을 한 줄에 하나씩 적어 두고,
그 옆에서 이번에 낼 정산서에 넣을 사람을 고르세요.
담당이 바뀌면 고르는 것만 바꾸면 됩니다. 안 고르면 빈칸으로 나와 손으로 적을 수 있습니다.

## 7. 정산서를 내면 체크가 풀립니다

파일이 떨어지면 달력·목록의 체크가 모두 자동으로 풀립니다.
다음 행사를 고를 때 앞의 체크가 섞이지 않습니다.

## 자주 나오는 것

**감면율 딱지**
서식의 `감면율(80%)` 딱지는 그 건의 실제 감면율을 따라갑니다.
50% 감경이면 `감면율(50%)`, 감면이 없으면 `감면율(0%)` 로 나옵니다.

**감면율이 서로 다른 건을 함께 골랐을 때**
정산서는 감면율 칸이 하나뿐이라 **첫 건의 감면율**을 씁니다.
그때는 "감면율이 서로 달라 첫 건의 감면율을 썼습니다" 안내가 뜹니다.
감면이 다르면 나눠서 뽑는 것이 맞습니다.

**부속시설을 8개 넘게 썼을 때**
서식의 부속시설 칸이 **일곱 줄뿐**이라 여덟 번째부터는 들어가지 않습니다.
그럴 때는 "○○ 은(는) 빠졌습니다" 안내가 뜨니 **엑셀에서 손으로 넣어** 주세요.
(서식을 임의로 늘리지 않기로 했습니다.)

**의자·탁자 같은 「개」 단위 항목**
서식이 아래 두 줄을 그 자리로 쓰도록 짜여 있어, 개수로 세는 항목은 자동으로 그 줄에 들어갑니다.

**파일 형식**
`.xlsx` 로 나오므로 엑셀에서 바로 열립니다. 예전에 뜨던
"파일 형식 및 확장명이 일치하지 않습니다" 경고는 나오지 않습니다.
